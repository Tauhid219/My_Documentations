আমার সাইটের ট্যাবে document লেখাটি উঠে থাকে, এবং laravel logo শো করতে থাকে। এটা কিভাবে ঠিক করবো? 


তোমার সাইটের ট্যাবে **"Document"** লেখাটি এবং Laravel এর ডিফল্ট লোগো দেখানোর সমস্যাটি ঠিক করতে হলে তোমার Laravel অ্যাপ্লিকেশনের **title** এবং **favicon** সেট করতে হবে। এটি করতে হলে কয়েকটি ধাপ ফলো করতে হবে:

---

### ১. **Title (ট্যাবে "Document" পরিবর্তন করা):**
Laravel এর ডিফল্ট টেমপ্লেটে ট্যাবের **title** সাধারণত `resources/views/layouts/app.blade.php` বা `resources/views/layouts/guest.blade.php` ফাইলে নির্ধারিত থাকে।

#### সমাধান:
`<title>` ট্যাগে ডাইনামিক বা স্ট্যাটিক টাইটেল সেট করুন। উদাহরণস্বরূপ:

```html
<!-- resources/views/layouts/app.blade.php -->
<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{{ config('app.name', 'My Application') }}</title> <!-- টাইটেল সেট করা -->
    <link rel="icon" href="{{ asset('favicon.ico') }}"> <!-- ফেভিকন সেট করা -->
    
    @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body>
    {{ $slot }}
</body>
</html>
```

#### কাস্টম টাইটেল সেট করতে চাইলে:
পেজ-ভিত্তিক টাইটেল সেট করতে `@section` ব্যবহার করতে পারো:

```php
<!-- Example: Welcome Blade -->
@extends('layouts.app')

@section('title', 'Welcome to My LMS')

@section('content')
    <h1>Welcome!</h1>
@endsection
```

### ২. **Favicon (Laravel logo পরিবর্তন করা):**
ডিফল্ট Laravel লোগো পরিবর্তন করতে তোমাকে একটি কাস্টম **favicon** ব্যবহার করতে হবে।

#### নতুন ফেভিকন যোগ করার ধাপ:
1. **Favicon ফাইল প্রস্তুত করুন:**
   - ফেভিকন ফাইল হতে হবে `.ico`, `.png`, বা `.svg` ফরম্যাটে।
   - ফাইলের নাম দিন `favicon.ico` (অথবা যা ইচ্ছা)।

2. **ফাইলটি অ্যাপের পাবলিক ফোল্ডারে রাখুন:**
   - ফাইলটি `public/` ডিরেক্টরিতে আপলোড করুন।
   উদাহরণ: `public/favicon.ico`

3. **Blade টেমপ্লেটে ফেভিকন লিঙ্ক করুন:**
   `resources/views/layouts/app.blade.php` বা অন্যান্য লেআউট ফাইলের `<head>` অংশে এটি যোগ করুন:

```html
<link rel="icon" href="{{ asset('favicon.ico') }}" type="image/x-icon">
```

---

### ৩. **.env ফাইলে অ্যাপের নাম আপডেট করুন:**
`APP_NAME` এর মান পরিবর্তন করলে ডিফল্ট টাইটেলও আপডেট হবে।

```env
APP_NAME="My LMS"
```

---

### ফলাফল:
1. **ট্যাবে টাইটেল পরিবর্তন হবে।** ডিফল্ট "Document" এর পরিবর্তে তোমার কাস্টম নাম দেখাবে।
2. **Laravel লোগোর পরিবর্তে তোমার কাস্টম ফেভিকন প্রদর্শিত হবে।**

### **যদি আরও সহায়তা প্রয়োজন হয়, জানাও! 😊**










একটু সমস্যা হচ্ছে, error দিচ্ছে: 
The environment file is invalid!
  Failed to parse dotenv file. Encountered unexpected whitespace at [Scratch Bangladesh].


`.env` ফাইলে `APP_NAME` সেট করার সময় এই ত্রুটি সাধারণত ঘটে, যদি তোমার অ্যাপের নামের মধ্যে স্পেস থাকে এবং তা কোটেশনের মধ্যে রাখা না হয়। 

Laravel এর `.env` ফাইল স্পেস বা বিশেষ চরিত্র সমর্থন করে, তবে সেই ক্ষেত্রে নামটি **ডাবল কোটেশন** (`"`) বা সিঙ্গেল কোটেশন (`'`) দিয়ে ঘিরে দিতে হয়। 

---

### সঠিক সমাধান:
`.env` ফাইলে নিচের মতো করে অ্যাপের নাম সেট করো:

```env
APP_NAME="Scratch Bangladesh"
```

---

### কেন ত্রুটি হচ্ছিল:
- `.env` ফাইল পার্স করার সময় স্পেসযুক্ত মানগুলো কোটেশনের অভাবে বিভ্রান্তি তৈরি করে।
- উদাহরণস্বরূপ, নিচের কোডটি ভুল:
  ```env
  APP_NAME=Scratch Bangladesh
  ```
  এটি "Scratch" কে `APP_NAME` হিসেবে গণ্য করবে এবং "Bangladesh" কে আলাদা শব্দ হিসেবে ধরবে, যা সঠিক নয়।

---

### পরবর্তী ধাপ:
`.env` ফাইল সঠিকভাবে সেভ করার পর, নিম্নলিখিত কমান্ড চালাও যাতে ক্যাশ ক্লিয়ার হয় এবং পরিবর্তন কার্যকর হয়:

```bash
php artisan config:clear
php artisan config:cache
```

---

এখন তোমার অ্যাপ ট্যাবে সঠিক অ্যাপ নাম দেখাবে। 😊 