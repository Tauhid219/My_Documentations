PHP-তে HTTP Request Handling শেখার জন্য নিচের বিষয়গুলো স্টেপ বাই স্টেপ বোঝা দরকার:

---

### 1. **HTTP Request কী?**
HTTP (HyperText Transfer Protocol) হলো ওয়েব সার্ভার এবং ক্লায়েন্ট (ব্রাউজার বা API) এর মধ্যে যোগাযোগের মাধ্যম। HTTP request বিভিন্ন মেথড ব্যবহার করে তথ্য পাঠায় ও গ্রহণ করে।

---

### 2. **HTTP Request Methods**
PHP-তে সাধারণত পাঁচটি HTTP মেথড বেশি ব্যবহৃত হয়:

1. **GET** – ডাটা রিকুয়েস্ট করার জন্য।
2. **POST** – ডাটা সাবমিট বা সেন্ড করার জন্য।
3. **PUT** – ডাটা আপডেট করার জন্য।
4. **DELETE** – ডাটা ডিলিট করার জন্য।
5. **PATCH** – নির্দিষ্ট অংশের ডাটা আপডেট করার জন্য।

---

### 3. **PHP-তে HTTP Request Handling**
PHP-তে HTTP request প্রোসেস করার জন্য `$_GET`, `$_POST`, `$_REQUEST`, এবং `$_SERVER` সুপারগ্লোবাল ভেরিয়েবল ব্যবহার করা হয়।

#### **(1) `$_GET` – GET Request Handle করা**
যখন ব্রাউজারে কোনো URL-এর query parameter দিয়ে request করা হয়, তখন `$_GET` ব্যবহার করা হয়।

```php
// URL: example.com/index.php?name=Hasan&age=25
$name = $_GET['name'];
$age = $_GET['age'];

echo "Name: $name, Age: $age";
```

#### **(2) `$_POST` – POST Request Handle করা**
যখন ফর্ম থেকে ডাটা পাঠানো হয়, তখন `$_POST` ব্যবহার করা হয়।

```php
// form.html
<form action="submit.php" method="post">
    Name: <input type="text" name="name">
    <button type="submit">Submit</button>
</form>
```

```php
// submit.php
$name = $_POST['name'];
echo "Hello, $name!";
```

#### **(3) `$_REQUEST` – GET ও POST উভয়ই Handle করা**
`$_REQUEST` GET ও POST উভয় মেথড থেকে ডাটা নিতে পারে।

```php
$name = $_REQUEST['name'];
echo "Hello, $name!";
```

#### **(4) `$_SERVER` – Server সংক্রান্ত তথ্য পাওয়া**
`$_SERVER` ব্যবহার করে বিভিন্ন সার্ভার তথ্য পাওয়া যায়।

```php
echo $_SERVER['REQUEST_METHOD'];  // GET বা POST দেখাবে
echo $_SERVER['REMOTE_ADDR'];  // ইউজারের IP Address দেখাবে
```

---

### 4. **PHP-তে File Upload Handling**
ফাইল আপলোড করার জন্য `$_FILES` ব্যবহার করা হয়।

```php
// upload.html
<form action="upload.php" method="post" enctype="multipart/form-data">
    Select File: <input type="file" name="file">
    <button type="submit">Upload</button>
</form>
```

```php
// upload.php
if ($_FILES['file']['error'] == 0) {
    $file_name = $_FILES['file']['name'];
    move_uploaded_file($_FILES['file']['tmp_name'], "uploads/$file_name");
    echo "File Uploaded Successfully!";
}
```

---

### 5. **PHP-তে JSON Request Handling**
কখনো কখনো API থেকে JSON ফরম্যাটে ডাটা আসে, তখন `file_get_contents("php://input")` ব্যবহার করতে হয়।

```php
// JSON Request Handling
$data = json_decode(file_get_contents("php://input"), true);
echo "Received: " . json_encode($data);
```

---

### 6. **PHP-তে HTTP Headers Handling**
কখনো কখনো হেডারস নিয়ে কাজ করতে হয়, যেমনঃ

```php
// কাস্টম হেডার পাঠানো
header("Content-Type: application/json");

// হেডার রিসিভ করা
$headers = getallheaders();
echo $headers['User-Agent'];
```

---

এগুলোই PHP-তে HTTP Request Handling-এর মূল বিষয়বস্তু। এখন তুমি চাইলে AJAX, API, এবং Laravel Request Handling নিয়েও জানতে পারো। 🚀