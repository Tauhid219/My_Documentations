PHP-তে **variables এবং scope** বোঝা খুব গুরুত্বপূর্ণ, কারণ এটি নির্ধারণ করে যে কোন ভেরিয়েবল কোথায় অ্যাক্সেস করা যাবে। আসো, ধাপে ধাপে শিখি।  

---

## **১. PHP Variables (ভেরিয়েবল)**
PHP-তে ভেরিয়েবল ডিক্লেয়ার করার জন্য **`$`** সাইন ব্যবহার করা হয়।  

🔹 **উদাহরণ:**  
```php
<?php
$name = "John";  // একটি স্ট্রিং ভেরিয়েবল
$age = 25;       // একটি সংখ্যা
$price = 99.99;  // দশমিক সংখ্যা
?>
```

🔹 **PHP Variables-এর নিয়ম:**  
- `$` চিহ্নের পর ভেরিয়েবল নাম লিখতে হয়।  
- ভেরিয়েবল নাম **সংখ্যা দিয়ে শুরু করা যায় না** (যেমন: `$1name` ❌)।  
- ভেরিয়েবল নাম **case-sensitive** (যেমন `$name` এবং `$Name` আলাদা)।  

---

## **২. PHP Variable Scope (ভেরিয়েবলের কার্যকারিতা সীমা)**
PHP-তে **৪ ধরনের scope** আছে:  
1. **Local Scope**  
2. **Global Scope**  
3. **Static Scope**  
4. **Function Parameter Scope**  

### **(১) Local Scope**
যদি কোনো ভেরিয়েবল **একটি ফাংশনের ভিতরে ডিক্লেয়ার করা হয়**, তবে সেটি **শুধুমাত্র সেই ফাংশনের ভিতরে অ্যাক্সেস করা যাবে**।  

🔹 **উদাহরণ:**  
```php
<?php
function testFunction() {
    $x = 10; // Local Scope
    echo "Inside function: $x"; 
}

testFunction();

// বাইরে ব্যবহার করলে error হবে
echo "Outside function: $x"; // এটি কাজ করবে না
?>
```
👉 `$x` শুধুমাত্র `testFunction()` এর ভিতরে কাজ করবে, বাইরে **error** হবে।

---

### **(২) Global Scope**
যদি কোনো ভেরিয়েবল **ফাংশনের বাইরে ডিক্লেয়ার করা হয়**, তবে সেটি ফাংশনের ভিতরে সরাসরি ব্যবহার করা যাবে না।  

🔹 **ভুল ব্যবহার:**  
```php
<?php
$y = 50; // Global Scope

function display() {
    echo $y; // এটি কাজ করবে না ❌
}

display();
?>
```
🔹 **সঠিক ব্যবহার (global কীওয়ার্ড দিয়ে):**  
```php
<?php
$y = 50; // Global Scope

function display() {
    global $y; // global কীওয়ার্ড ব্যবহার করে অ্যাক্সেস করতে হবে
    echo $y;
}

display(); // Output: 50
?>
```
👉 `global` কীওয়ার্ড ব্যবহার করলে আমরা ফাংশনের ভিতরে বাইরের ভেরিয়েবল ব্যবহার করতে পারি।

---

### **(৩) Static Scope**
কোনো ফাংশন চালু হলে **সাধারণত তার ভিতরের ভেরিয়েবল মেমোরি থেকে মুছে যায়**। কিন্তু **`static`** কীওয়ার্ড ব্যবহার করলে ভেরিয়েবলটি মেমোরিতে সংরক্ষিত থাকে।  

🔹 **উদাহরণ:**  
```php
<?php
function counter() {
    static $count = 0; // Static Scope
    $count++;
    echo $count . "<br>";
}

counter(); // Output: 1
counter(); // Output: 2
counter(); // Output: 3
?>
```
👉 `static` ভেরিয়েবল শুধুমাত্র প্রথমবার ইনিশিয়ালাইজ হয় এবং ফাংশন যতবারই কল করা হোক, তার আগের মান সংরক্ষণ করে।

---

### **(৪) Function Parameter Scope**
যদি কোনো ভেরিয়েবল ফাংশনের **প্যারামিটার হিসেবে পাস করা হয়**, তবে সেটি শুধুমাত্র সেই ফাংশনের ভিতরে ব্যবহার করা যাবে।  

🔹 **উদাহরণ:**  
```php
<?php
function greet($name) { // $name শুধুমাত্র ফাংশনের ভিতরে কাজ করবে
    echo "Hello, $name!";
}

greet("Alice"); // Output: Hello, Alice!
greet("Bob");   // Output: Hello, Bob!
?>
```
👉 এখানে `$name` ভেরিয়েবলটি শুধুমাত্র `greet()` ফাংশনের ভিতরে কাজ করবে।

---

## **সংক্ষেপে গুরুত্বপূর্ণ পয়েন্ট**
✅ **Local Scope** - ফাংশনের ভিতরে ডিক্লেয়ার করা ভেরিয়েবল শুধুমাত্র ফাংশনের ভিতরে কাজ করে।  
✅ **Global Scope** - বাইরের ভেরিয়েবল ফাংশনের ভিতরে `global` কীওয়ার্ড ছাড়া ব্যবহার করা যায় না।  
✅ **Static Scope** - `static` কীওয়ার্ড ব্যবহার করলে ভেরিয়েবল মেমোরিতে সংরক্ষিত থাকে।  
✅ **Function Parameter Scope** - ফাংশনের প্যারামিটার হিসেবে দেওয়া ভেরিয়েবল শুধুমাত্র সেই ফাংশনের ভিতরে কাজ করে।  

---

### **পরবর্তী কী শিখতে চাও?**  
PHP-এর আরও কোনো বিষয় বুঝতে চাইলে বলো! 😊