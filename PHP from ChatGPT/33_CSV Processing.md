PHP-তে CSV (Comma-Separated Values) ফাইল প্রসেস করা খুবই দরকারি স্কিল, বিশেষ করে ডাটা ইমপোর্ট/এক্সপোর্ট করার জন্য। CSV প্রসেসিং মূলত তিনটি ধাপে করা হয়: 

1. **CSV ফাইল পড়া (Reading CSV)**
2. **CSV ফাইলে লেখা (Writing CSV)**
3. **CSV ফাইল মডিফাই করা (Modifying CSV)**

আমি ধাপে ধাপে এগিয়ে যাবো।  

---

### ১. CSV ফাইল পড়া (Reading CSV)  

PHP-তে `fgetcsv()` ফাংশন ব্যবহার করে CSV ফাইলের লাইন বাই লাইন ডাটা পড়া যায়।  

#### **ধাপ ১:** একটা `data.csv` ফাইল তৈরি করুন  

```csv
ID,Name,Email
1,John Doe,john@example.com
2,Jane Doe,jane@example.com
3,Bob Smith,bob@example.com
```

#### **ধাপ ২:** PHP কোড দিয়ে CSV পড়ুন  

```php
<?php
$filename = "data.csv";
if (($handle = fopen($filename, "r")) !== FALSE) {
    while (($data = fgetcsv($handle, 1000, ",")) !== FALSE) {
        echo "ID: " . $data[0] . " | Name: " . $data[1] . " | Email: " . $data[2] . "<br>";
    }
    fclose($handle);
}
?>
```

**ফলাফল:**  
```
ID: 1 | Name: John Doe | Email: john@example.com  
ID: 2 | Name: Jane Doe | Email: jane@example.com  
ID: 3 | Name: Bob Smith | Email: bob@example.com  
```

---

### ২. CSV ফাইলে লেখা (Writing CSV)  

PHP-এর `fputcsv()` ফাংশন ব্যবহার করে নতুন CSV ফাইল তৈরি করা যায়।  

#### **ধাপ ১:** নতুন CSV ফাইল তৈরি করুন  

```php
<?php
$data = [
    ["ID", "Name", "Email"],
    [1, "John Doe", "john@example.com"],
    [2, "Jane Doe", "jane@example.com"],
    [3, "Bob Smith", "bob@example.com"]
];

$filename = "new_data.csv";
$handle = fopen($filename, "w");

foreach ($data as $row) {
    fputcsv($handle, $row);
}

fclose($handle);
echo "CSV ফাইল সফলভাবে লেখা হয়েছে!";
?>
```

**ফলাফল:** `new_data.csv` ফাইল তৈরি হবে এবং ডাটা থাকবে।  

---

### ৩. CSV ফাইল মডিফাই করা (Modifying CSV)  

ধরা যাক, আমরা `data.csv` ফাইল আপডেট করতে চাই।  

#### **ধাপ ১:** CSV ফাইল লোড করুন, পরিবর্তন করুন এবং সংরক্ষণ করুন  

```php
<?php
$filename = "data.csv";
$rows = [];

if (($handle = fopen($filename, "r")) !== FALSE) {
    while (($data = fgetcsv($handle, 1000, ",")) !== FALSE) {
        if ($data[0] == "2") { // যদি ID 2 হয়, তাহলে নাম পরিবর্তন করবো
            $data[1] = "Jane Doe (Updated)";
        }
        $rows[] = $data;
    }
    fclose($handle);
}

$handle = fopen($filename, "w");
foreach ($rows as $row) {
    fputcsv($handle, $row);
}
fclose($handle);

echo "CSV ফাইল সফলভাবে আপডেট হয়েছে!";
?>
```

**ফলাফল:** `data.csv` ফাইলে **ID 2** এর জন্য নাম পরিবর্তন হয়ে যাবে।  

---

### ৪. CSV ডাটা ব্রাউজারে ডাউনলোড করানো  

```php
<?php
$data = [
    ["ID", "Name", "Email"],
    [1, "John Doe", "john@example.com"],
    [2, "Jane Doe", "jane@example.com"],
    [3, "Bob Smith", "bob@example.com"]
];

header('Content-Type: text/csv');
header('Content-Disposition: attachment; filename="download.csv"');

$output = fopen("php://output", "w");

foreach ($data as $row) {
    fputcsv($output, $row);
}
fclose($output);
?>
```

এই স্ক্রিপ্ট রান করলে ব্রাউজারে **download.csv** নামে ফাইল ডাউনলোড হবে।

---

### ৫. CSV ডাটা PHP অ্যারে তে কনভার্ট করা  

```php
<?php
$filename = "data.csv";
$csvData = [];

if (($handle = fopen($filename, "r")) !== FALSE) {
    $headers = fgetcsv($handle); // প্রথম লাইনটি হেডার হিসেবে ধরবো
    while (($row = fgetcsv($handle, 1000, ",")) !== FALSE) {
        $csvData[] = array_combine($headers, $row);
    }
    fclose($handle);
}

print_r($csvData);
?>
```

**ফলাফল:**  
```php
Array
(
    [0] => Array ( [ID] => 1 [Name] => John Doe [Email] => john@example.com )
    [1] => Array ( [ID] => 2 [Name] => Jane Doe [Email] => jane@example.com )
    [2] => Array ( [ID] => 3 [Name] => Bob Smith [Email] => bob@example.com )
)
```

---

### 📌 শেষ কথা  

✅ **CSV ফাইল পড়া:** `fgetcsv()`  
✅ **CSV ফাইলে লেখা:** `fputcsv()`  
✅ **CSV ফাইল আপডেট করা:** পড়ার পর `array` আপডেট করে লিখতে হবে  
✅ **CSV ফাইল ব্রাউজারে ডাউনলোড করানো:** `header()` ব্যবহার করে  
✅ **CSV ডাটা PHP অ্যারেতে কনভার্ট করা:** `array_combine()` ব্যবহার করে  

এখন বলো, তুমি কি আরও গভীরে শিখতে চাও, নাকি কিছু নির্দিষ্ট ফাংশন বা ইউজকেসে ফোকাস করতে চাও? 😊