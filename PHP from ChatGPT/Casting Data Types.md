## **PHP-তে Casting (Type Casting) শিখি**  

PHP একটি **loosely typed language**, তাই এটি স্বয়ংক্রিয়ভাবে ভেরিয়েবলের টাইপ নির্ধারণ করে। তবে অনেক সময় আমাদের **একটি ডাটা টাইপকে অন্য টাইপে রূপান্তর (casting) করতে হয়**।  

PHP-তে **Type Casting** মানে হলো **একটি ভেরিয়েবলকে জোরপূর্বক অন্য ডাটা টাইপে পরিবর্তন করা**।  

---

## **PHP-তে Casting করার উপায়**
PHP-তে দুটি উপায়ে Type Casting করা যায়:
1. **Manual Casting (হাত দিয়ে টাইপ পরিবর্তন করা)**
2. **Automatic Type Juggling (PHP স্বয়ংক্রিয়ভাবে টাইপ পরিবর্তন করে)**  

আমরা এখানে **Manual Casting** নিয়ে বিস্তারিত আলোচনা করব।  

---

## **১. Manual Type Casting (টাইপ পরিবর্তন করা)**
Manual Type Casting করতে **(type)** ব্যবহার করা হয়।  

🔹 **PHP-তে যেসব টাইপে Casting করা যায়:**  
| **Casting Format**                | **ব্যাখ্যা**        |
| --------------------------------- | --------------- |
| `(int)` বা `(integer)`             | পূর্ণসংখ্যায় রূপান্তর   |
| `(float)` বা `(double)` বা `(real)` | দশমিক সংখ্যায় রূপান্তর |
| `(bool)` বা `(boolean)`            | বুলিয়ান মানে রূপান্তর   |
| `(string)`                        | স্ট্রিং-এ রূপান্তর     |
| `(array)`                         | অ্যারেতে রূপান্তর      |
| `(object)`                        | অবজেক্টে রূপান্তর     |
| `(unset)`                         | NULL-এ রূপান্তর    |

---

### **২. Integer Casting (পূর্ণসংখ্যায় রূপান্তর)**
🔹 **Syntax:**  
```php
(int) $var  // অথবা  (integer) $var
```

🔹 **কীভাবে কাজ করে?**  
- দশমিক সংখ্যার **মাত্র পূর্ণসংখ্যার অংশ নেয়**  
- স্ট্রিং থেকে সংখ্যা থাকলে শুধুমাত্র সংখ্যার অংশ নেয়  
- `true` → `1`, `false` → `0`  
- `NULL` → `0`  

🔹 **উদাহরণ:**  
```php
<?php
$num1 = 10.99;
$num2 = "25 apples";
$bool = true;

echo (int)$num1; // Output: 10
echo (int)$num2; // Output: 25
echo (int)$bool; // Output: 1
?>
```
---

### **৩. Float Casting (দশমিক সংখ্যায় রূপান্তর)**
🔹 **Syntax:**  
```php
(float) $var  // অথবা  (double) $var  অথবা (real) $var
```

🔹 **উদাহরণ:**  
```php
<?php
$num1 = "12.34 kg";
$num2 = 25;

echo (float)$num1; // Output: 12.34
echo (float)$num2; // Output: 25
?>
```
---

### **৪. Boolean Casting (বুলিয়ান মানে রূপান্তর)**
🔹 **Syntax:**  
```php
(bool) $var  // অথবা (boolean) $var
```
✅ **যা `false` হবে:**  
| **Value**           | **Boolean Value** |
| ------------------- | ----------------- |
| `0`                 | false             |
| `0.0`               | false             |
| `""` (Empty String) | false             |
| `"0"`               | false             |
| `NULL`              | false             |
| `[]` (Empty Array)  | false             |

✅ **বাকি সব `true` হবে!**  

🔹 **উদাহরণ:**  
```php
<?php
$num = 10;
$str = "";
$zero = 0;
$nullVal = NULL;

echo (bool)$num;    // Output: 1 (true)
echo (bool)$str;    // Output:  (false)
echo (bool)$zero;   // Output:  (false)
echo (bool)$nullVal; // Output:  (false)
?>
```
---

### **৫. String Casting (স্ট্রিং-এ রূপান্তর)**
🔹 **Syntax:**  
```php
(string) $var
```
✅ **যা স্ট্রিং-এ রূপান্তর হবে:**  
- `true` → `"1"`  
- `false` → `""` (empty string)  
- `NULL` → `""`  
- সংখ্যা → `"সংখ্যা"`  

🔹 **উদাহরণ:**  
```php
<?php
$num = 100;
$bool = true;
$nullVar = NULL;

echo (string)$num;    // Output: "100"
echo (string)$bool;   // Output: "1"
echo (string)$nullVar; // Output: "" (empty string)
?>
```
---

### **৬. Array Casting (অ্যারেতে রূপান্তর)**
🔹 **Syntax:**  
```php
(array) $var
```
✅ **যা অ্যারেতে রূপান্তর হবে:**  
- স্ট্রিং, সংখ্যা, বুলিয়ান → **একটি অ্যারের প্রথম মান হয়ে যাবে**  
- `NULL` → খালি অ্যারে  

🔹 **উদাহরণ:**  
```php
<?php
$str = "Hello";
$num = 50;

$arr1 = (array)$str;
$arr2 = (array)$num;

print_r($arr1); // Output: Array ( [0] => Hello )
print_r($arr2); // Output: Array ( [0] => 50 )
?>
```
---

### **৭. Object Casting (অবজেক্ট-এ রূপান্তর)**
🔹 **Syntax:**  
```php
(object) $var
```
✅ **যা অবজেক্টে রূপান্তর হবে:**  
- স্ট্রিং, সংখ্যা → একটি অবজেক্ট তৈরি হবে  
- অ্যারে → অবজেক্টে কনভার্ট হবে  

🔹 **উদাহরণ:**  
```php
<?php
$num = 100;
$obj = (object)$num;

echo $obj->scalar; // Output: 100
?>
```

🔹 **Associative Array থেকে Object:**  
```php
<?php
$arr = ["name" => "Alice", "age" => 25];
$obj = (object)$arr;

echo $obj->name; // Output: Alice
?>
```
---

### **৮. NULL Casting (NULL-এ রূপান্তর)**
🔹 **Syntax:**  
```php
(unset) $var
```
✅ **PHP 7-এর পর থেকে এটি ব্যবহার না করাই ভালো**।  
```php
<?php
$num = 100;
$var = (unset)$num;
var_dump($var); // Output: NULL
?>
```
---

## **সংক্ষেপে গুরুত্বপূর্ণ পয়েন্ট**
✅ PHP-তে **Type Casting** করতে **(type) ব্যবহার করা হয়**  
✅ PHP স্বয়ংক্রিয়ভাবে টাইপ পরিবর্তন করতে পারে, তবে কাস্টিং করলে নির্দিষ্ট টাইপ ফোর্স করা যায়  
✅ `int`, `float`, `bool`, `string`, `array`, `object`, এবং `NULL`-এ কাস্ট করা যায়  

---

## **পরবর্তী কী শিখতে চাও?**
PHP-এর **Type Juggling (স্বয়ংক্রিয় টাইপ পরিবর্তন)**, অথবা **Type Hinting (ফাংশনে টাইপ নির্দিষ্ট করা)** শিখতে চাও? 😊