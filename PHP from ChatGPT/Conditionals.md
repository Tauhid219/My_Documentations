# **🚀 PHP-এর Conditionals (শর্তবাক্য) সম্পূর্ণ গাইড**  

PHP-তে **Conditionals** ব্যবহার করে আমরা **if, else, elseif, switch** ইত্যাদি শর্তবাক্য লিখে প্রোগ্রামের প্রবাহ নিয়ন্ত্রণ করতে পারি।  

---

## **📌 1️⃣ if Statement (একটি শর্ত পরীক্ষা করা)**  
`if` স্টেটমেন্ট ব্যবহার করে আমরা নির্দিষ্ট শর্ত পূরণ হলে কোড এক্সিকিউট করতে পারি।  

### **🎯 Syntax:**  
```php
if (শর্ত) {
    // শর্ত সত্য হলে এটি চলবে
}
```

### **✅ উদাহরণ:**  
```php
<?php
$age = 18;

if ($age >= 18) {
    echo "You are eligible to vote.";
}
?>
```
🔹 যদি `$age` **১৮ বা তার বেশি** হয়, তাহলে **"You are eligible to vote."** প্রিন্ট হবে।  

---

## **📌 2️⃣ if...else Statement (দুইটি শর্ত)**  
`if`-এর শর্ত যদি মিথ্যা হয়, তাহলে `else` ব্লক চলে।  

### **🎯 Syntax:**  
```php
if (শর্ত) {
    // শর্ত সত্য হলে এটি চলবে
} else {
    // শর্ত মিথ্যা হলে এটি চলবে
}
```

### **✅ উদাহরণ:**  
```php
<?php
$age = 16;

if ($age >= 18) {
    echo "You are eligible to vote.";
} else {
    echo "You are not eligible to vote.";
}
?>
```
🔹 `$age` **১৮-এর কম হলে** "You are not eligible to vote." প্রিন্ট হবে।  

---

## **📌 3️⃣ if...elseif...else Statement (একাধিক শর্ত পরীক্ষা করা)**  
যদি প্রথম শর্ত সত্য না হয়, তাহলে `elseif` ব্লক পরীক্ষা করবে, আর যদি কোনো শর্তই সত্য না হয়, তাহলে `else` ব্লক চলবে।  

### **🎯 Syntax:**  
```php
if (শর্ত1) {
    // শর্ত1 সত্য হলে এটি চলবে
} elseif (শর্ত2) {
    // শর্ত2 সত্য হলে এটি চলবে
} else {
    // উপরের কোনো শর্ত সত্য না হলে এটি চলবে
}
```

### **✅ উদাহরণ:**  
```php
<?php
$marks = 75;

if ($marks >= 80) {
    echo "You got A+.";
} elseif ($marks >= 70) {
    echo "You got A.";
} elseif ($marks >= 60) {
    echo "You got B.";
} else {
    echo "You failed.";
}
?>
```
🔹 `$marks = 75` হওয়ায় **"You got A."** প্রিন্ট হবে।  

---

## **📌 4️⃣ Nested if (if-এর ভিতরে if)**  
একটি `if` ব্লকের মধ্যে আরেকটি `if` ব্লক থাকলে সেটাকে **Nested if** বলে।  

### **✅ উদাহরণ:**  
```php
<?php
$age = 20;
$hasID = true;

if ($age >= 18) {
    if ($hasID) {
        echo "You can enter.";
    } else {
        echo "You need an ID card.";
    }
} else {
    echo "You are underaged.";
}
?>
```
🔹 যদি **বয়স ১৮ বা তার বেশি হয় এবং আইডি থাকে**, তাহলে "You can enter." প্রিন্ট হবে।  

---

## **📌 5️⃣ Ternary Operator (সংক্ষেপে if...else)**  
`if...else` কে সংক্ষেপে **Ternary Operator** দিয়ে লেখা যায়।  

### **🎯 Syntax:**  
```php
(শর্ত) ? সত্য হলে চলবে : মিথ্যা হলে চলবে;
```

### **✅ উদাহরণ:**  
```php
<?php
$age = 20;
$message = ($age >= 18) ? "You can vote." : "You cannot vote.";
echo $message;
?>
```
🔹 `$age >= 18` সত্য হওয়ায় "You can vote." প্রিন্ট হবে।  

---

## **📌 6️⃣ Switch Case (একাধিক মান পরীক্ষা করা)**  
যখন একটি ভেরিয়েবলের **একাধিক সম্ভাব্য মান** পরীক্ষা করতে হয়, তখন `switch` ব্যবহার করা হয়।  

### **🎯 Syntax:**  
```php
switch (ভেরিয়েবল) {
    case মান1:
        // যদি মান1 মিলে যায়
        break;
    case মান2:
        // যদি মান2 মিলে যায়
        break;
    default:
        // যদি কোনো মানই মিলে না যায়
}
```

### **✅ উদাহরণ:**  
```php
<?php
$day = "Monday";

switch ($day) {
    case "Saturday":
        echo "It's weekend!";
        break;
    case "Sunday":
        echo "It's weekend!";
        break;
    case "Monday":
        echo "It's a weekday!";
        break;
    default:
        echo "Invalid day!";
}
?>
```
🔹 `$day = "Monday"` হওয়ায় **"It's a weekday!"** প্রিন্ট হবে।  

---

## **📌 7️⃣ Match Expression (PHP 8+ এ নতুন)**  
`match` হলো `switch` এর মতো, কিন্তু এটি `break` ব্যবহার করতে হয় না এবং এটি **strict comparison (===)** করে।  

### **✅ উদাহরণ:**  
```php
<?php
$grade = "B";

$result = match ($grade) {
    "A+" => "Excellent!",
    "A" => "Very Good!",
    "B" => "Good!",
    default => "Needs Improvement."
};

echo $result;
?>
```
🔹 `$grade = "B"` হওয়ায় **"Good!"** প্রিন্ট হবে।  

---

## **📌 8️⃣ `isset()` এবং `empty()` ফাংশন**  
### ✅ `isset()` ফাংশন চেক করে ভেরিয়েবল **সেট করা আছে কিনা**  
```php
<?php
$name = "Rahim";

if (isset($name)) {
    echo "The variable is set.";
}
?>
```
🔹 **$name ভেরিয়েবল সেট থাকায় এটি প্রিন্ট হবে।**  

### ✅ `empty()` ফাংশন চেক করে ভেরিয়েবল **ফাঁকা কিনা**  
```php
<?php
$name = "";

if (empty($name)) {
    echo "The variable is empty.";
}
?>
```
🔹 **$name ফাঁকা হওয়ায় এটি প্রিন্ট হবে।**  

---

## **🎯 অনুশীলনের জন্য চ্যালেঞ্জ 🚀**  
```php
<?php
$temperature = 35;

if ($temperature > 30) {
    echo "It's a hot day!";
} elseif ($temperature >= 20) {
    echo "It's a pleasant day!";
} else {
    echo "It's a cold day!";
}
?>
```
❓ **উপরের কোড রান করলে কী আউটপুট আসবে?** নিজে রান করে দেখো! 😃  

---

## **🚀 উপসংহার: আপনি এখন PHP Conditionals শিখে ফেলেছেন! 🎉**  
✔ **`if, else, elseif`** ব্যবহার করে শর্ত পরীক্ষা করা যায়।  
✔ **`switch` এবং `match`** দিয়ে একাধিক মান পরীক্ষা করা যায়।  
✔ **Ternary Operator** দিয়ে `if...else` সংক্ষেপে লেখা যায়।  
✔ **`isset()` ও `empty()`** ব্যবহার করে ভেরিয়েবল চেক করা যায়।  

---

## **➡ পরবর্তী কি শিখতে চান?**  
✅ **PHP-এর Loops (for, while, foreach) শিখতে চান? 🚀**