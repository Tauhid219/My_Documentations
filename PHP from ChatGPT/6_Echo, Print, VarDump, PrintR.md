# **PHP-তে echo, print, var_dump, print_r শেখা**  

PHP-তে **ডাটা আউটপুট** বা **ডিবাগিং-এর জন্য** বিভিন্ন ফাংশন আছে। সবচেয়ে বেশি ব্যবহৃত চারটি হল:  

1️⃣ `echo` – এক বা একাধিক ভ্যালু প্রিন্ট করার জন্য  
2️⃣ `print` – একটিমাত্র ভ্যালু প্রিন্ট করার জন্য  
3️⃣ `var_dump()` – ভ্যারিয়েবলের **ডাটা টাইপ ও মান বিস্তারিত দেখানোর জন্য**  
4️⃣ `print_r()` – অ্যারে বা অবজেক্টের **স্ট্রাকচার সুন্দরভাবে প্রিন্ট করার জন্য**  

---

## **1️⃣ PHP echo – সহজে আউটপুট দেখানোর জন্য**  
🔹 **`echo` হল সবচেয়ে বেশি ব্যবহৃত আউটপুট ফাংশন।** এটি এক বা একাধিক স্ট্রিং প্রিন্ট করতে পারে।  

🔹 **সিনট্যাক্স:**  
```php
echo "Hello, World!";
echo "Hello", " ", "PHP"; // একাধিক ভ্যালু দেওয়া যায়
```

🔹 **উদাহরণ:**  
```php
<?php
$name = "Alice";
$age = 25;

echo "Name: $name <br>";
echo "Age: " . $age . "<br>";
?>
```
✅ `echo` **ফাস্ট ও ইফিশিয়েন্ট, কারণ এটি কোনো রিটার্ন ভ্যালু দেয় না।**  
❌ `echo` **কোনো ভ্যালু রিটার্ন করতে পারে না।**  

---

## **2️⃣ PHP print – echo-র বিকল্প, কিন্তু ধীরগতির**  
🔹 `print` প্রায় `echo`-র মতোই কাজ করে, কিন্তু এটি **শুধুমাত্র একটি ভ্যালু প্রিন্ট করতে পারে এবং সবসময় `1` রিটার্ন করে।**  

🔹 **সিনট্যাক্স:**  
```php
print "Hello, World!";
print("Hello PHP"); // ব্র্যাকেট ব্যবহার করা যায়
```

🔹 **উদাহরণ:**  
```php
<?php
$name = "Bob";
$age = 30;

print "Name: $name <br>";
print "Age: " . $age . "<br>";

// print একটি ভ্যালু রিটার্ন করে, তাই এটি এক্সপ্রেশন হিসেবে ব্যবহার করা যায়
$val = print("Hello, PHP!<br>"); 
echo "Return Value: " . $val; // Output: Return Value: 1
?>
```
✅ `print` **এক্সপ্রেশন হিসেবে ব্যবহার করা যায়।**  
❌ `print` **শুধুমাত্র একটিমাত্র আর্গুমেন্ট নেয়, একাধিক ভ্যালু দিলে error হবে।**  
❌ `print` **`echo`-র তুলনায় কিছুটা ধীরগতির।**  

---

## **3️⃣ PHP var_dump() – ভ্যারিয়েবলের বিস্তারিত তথ্য দেখানোর জন্য**  
🔹 `var_dump()` **একটি ভ্যারিয়েবলের টাইপ ও মান দেখানোর জন্য সবচেয়ে ভালো ফাংশন।**  
🔹 এটি **প্রতিটি ভেরিয়েবলের ডাটা টাইপ ও ভ্যালু বিস্তারিত দেখায়।**  

🔹 **সিনট্যাক্স:**  
```php
var_dump($variable);
```

🔹 **উদাহরণ:**  
```php
<?php
$number = 42;
$text = "Hello";
$floatNum = 3.14;
$boolValue = true;
$arrayData = [1, 2, "PHP", false];

var_dump($number);  // int(42)
var_dump($text);    // string(5) "Hello"
var_dump($floatNum);// float(3.14)
var_dump($boolValue);// bool(true)
var_dump($arrayData);
/*
array(4) {
  [0]=> int(1)
  [1]=> int(2)
  [2]=> string(3) "PHP"
  [3]=> bool(false)
}
*/
?>
```
✅ `var_dump()` **সবচেয়ে ভালো ডিবাগিং টুল কারণ এটি টাইপ ও ভ্যালু দুইটাই দেখায়।**  
❌ এটি **সাজানো আউটপুট দেয় না, বিশেষত অ্যারে ও অবজেক্টের ক্ষেত্রে।**  

---

## **4️⃣ PHP print_r() – অ্যারে বা অবজেক্ট সুন্দরভাবে প্রিন্ট করার জন্য**  
🔹 `print_r()` **একটি অ্যারে বা অবজেক্ট প্রিন্ট করতে ভালো কাজ করে।**  
🔹 এটি `var_dump()` এর চেয়ে কম তথ্য দেয়, কিন্তু আউটপুট **আরো পাঠযোগ্য (readable)।**  

🔹 **সিনট্যাক্স:**  
```php
print_r($variable);
```

🔹 **উদাহরণ:**  
```php
<?php
$arrayData = [
    "name" => "Charlie",
    "age" => 28,
    "skills" => ["PHP", "JavaScript", "Laravel"]
];

print_r($arrayData);

/*
Output:
Array (
    [name] => Charlie
    [age] => 28
    [skills] => Array (
        [0] => PHP
        [1] => JavaScript
        [2] => Laravel
    )
)
*/
?>
```
✅ `print_r()` **অ্যারে ও অবজেক্টকে সুন্দরভাবে প্রিন্ট করে।**  
❌ এটি **টাইপ বা বিস্তারিত তথ্য দেখায় না (যেমন `var_dump()` করে)।**  

---

## **⚡ `echo` vs `print` vs `var_dump()` vs `print_r()` – পার্থক্য টেবিলে**  

| ফাংশন          | ব্যবহার              | টাইপ দেখায়? | একাধিক ভ্যালু নিতে পারে?     | রিটার্ন ভ্যালু আছে?     |
| ------------ | ------------------ | -------- | ------------------- | ---------------- |
| `echo`       | আউটপুট দেখানোর জন্য     | ❌ না      | ✅ হ্যাঁ                | ❌ না              |
| `print`      | আউটপুট দেখানোর জন্য     | ❌ না      | ❌ না (শুধু একটিমাত্র ভ্যালু) | ✅ হ্যাঁ (1 রিটার্ন করে) |
| `var_dump()` | ডিবাগিং-এর জন্য         | ✅ হ্যাঁ     | ✅ হ্যাঁ                | ❌ না              |
| `print_r()`  | অ্যারে/অবজেক্ট দেখানোর জন্য | ❌ না      | ✅ হ্যাঁ                | ❌ না              |

---

## **🎯 কখন কোনটি ব্যবহার করব?**  
✅ শুধু **টেক্সট প্রিন্ট করতে চাইলে** – `echo`  
✅ যদি **একটিমাত্র ভ্যালু প্রিন্ট করতে চাই** – `print`  
✅ যদি **ভ্যারিয়েবলের টাইপ ও ডাটা বিশ্লেষণ করতে চাই** – `var_dump()`  
✅ যদি **অ্যারে বা অবজেক্টের পাঠযোগ্য আউটপুট চাই** – `print_r()`  

---

## **🎯 চ্যালেঞ্জ: নিজে চেষ্টা করো!**  
👉 নিচের কোডটি রান করলে কী হবে?  
```php
<?php
$data = [
    "name" => "David",
    "age" => 35,
    "height" => 5.9,
    "married" => false
];

echo "Data: ";
print_r($data);
echo "<br>";

var_dump($data);
?>
```
✅ নিজে কোড রান করে দেখো, পার্থক্য অনুভব করো! 😊  

---

## **পরবর্তী কী শিখতে চাও?**  
PHP-এর **Superglobals ($_GET, $_POST, $_SESSION, $_COOKIE)** শেখা লাগবে? 😃