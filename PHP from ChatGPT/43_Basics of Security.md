## **🔒 PHP Security Basics: ওয়েব অ্যাপ সুরক্ষিত রাখার গাইড**  

PHP ওয়েব অ্যাপ্লিকেশন **হ্যাকিংয়ের প্রধান লক্ষ্য** হতে পারে, যদি নিরাপত্তা ব্যবস্থা না নেওয়া হয়। এখানে **সাধারণ দুর্বলতাগুলো** এবং **কীভাবে প্রতিরোধ করা যায়**, তা বিস্তারিত শেখানো হলো।

---

## **✅ 1️⃣ SQL Injection প্রতিরোধ**  
SQL ইনজেকশন হলো এমন একটি আক্রমণ, যেখানে হ্যাকার **ইনপুট ফিল্ডের মাধ্যমে SQL কোয়েরি পরিবর্তন করতে পারে**।  

### **🚨 দুর্বল কোড (❌ Avoid This)**  
```php
$username = $_POST['username'];
$password = $_POST['password'];
$sql = "SELECT * FROM users WHERE username='$username' AND password='$password'";
$result = mysqli_query($conn, $sql);
```
✅ **সমস্যা:** ইউজার যদি `admin' --` টাইপ করে, তাহলে SQL ইনজেকশন হয়ে যাবে!  

### **✅ সঠিক সমাধান (Prepared Statements ব্যবহার করা)**  
```php
$stmt = $conn->prepare("SELECT * FROM users WHERE username=? AND password=?");
$stmt->bind_param("ss", $username, $password);
$stmt->execute();
$result = $stmt->get_result();
```
✅ **Prepared Statements ব্যবহার করলে SQL ইনজেকশন প্রতিরোধ করা যায়।**  

---

## **✅ 2️⃣ Cross-Site Scripting (XSS) প্রতিরোধ**  
XSS আক্রমণে **হ্যাকার স্ক্রিপ্ট ইনজেক্ট করে** ব্রাউজারে রান করায়, যা **Cookies চুরি, ফর্ম ম্যানিপুলেশন, ফিশিং ইত্যাদি করতে পারে**।  

### **🚨 দুর্বল কোড (❌ Avoid This)**  
```php
echo "<h1>Welcome, " . $_GET['name'] . "</h1>";
```
✅ **সমস্যা:** ইউজার যদি `<script>alert('Hacked!');</script>` টাইপ করে, তাহলে স্ক্রিপ্ট চালু হবে!  

### **✅ সঠিক সমাধান (HTML Entities ব্যবহার করা)**  
```php
echo "<h1>Welcome, " . htmlspecialchars($_GET['name'], ENT_QUOTES, 'UTF-8') . "</h1>";
```
✅ **`htmlspecialchars()` দিয়ে HTML Encode করলে XSS প্রতিরোধ করা যায়।**  

---

## **✅ 3️⃣ CSRF (Cross-Site Request Forgery) প্রতিরোধ**  
CSRF হলো **একটি আক্রমণ**, যেখানে হ্যাকার ইউজারকে **অজান্তে ক্ষতিকর অনুরোধ পাঠাতে বাধ্য করে**।  

### **🚨 দুর্বল ফর্ম (❌ Avoid This)**  
```html
<form method="POST" action="update.php">
    <input type="text" name="email">
    <button type="submit">Update Email</button>
</form>
```
✅ **সমস্যা:** হ্যাকার ইউজারকে ফিশিং লিংকে ক্লিক করিয়ে ইমেইল পরিবর্তন করিয়ে ফেলতে পারে।  

### **✅ সঠিক সমাধান (CSRF Token ব্যবহার করা)**  
```php
session_start();
$_SESSION['csrf_token'] = bin2hex(random_bytes(32));
?>

<form method="POST" action="update.php">
    <input type="hidden" name="csrf_token" value="<?php echo $_SESSION['csrf_token']; ?>">
    <input type="text" name="email">
    <button type="submit">Update Email</button>
</form>
```
**🔹 `update.php`-তে টোকেন যাচাই করা:**  
```php
session_start();
if ($_POST['csrf_token'] !== $_SESSION['csrf_token']) {
    die("CSRF attack detected!");
}
```
✅ **CSRF Token ব্যবহার করলে হ্যাকারের পক্ষে অনুরোধ পাঠানো সম্ভব হয় না।**  

---

## **✅ 4️⃣ Password Hashing (পাসওয়ার্ড এনক্রিপশন)**  
কখনো **পাসওয়ার্ড প্লেইন-টেক্সট আকারে সংরক্ষণ করা যাবে না!**  

### **🚨 দুর্বল পাসওয়ার্ড স্টোরিং (❌ Avoid This)**  
```php
$password = $_POST['password'];
$sql = "INSERT INTO users (password) VALUES ('$password')";
```
✅ **সমস্যা:** ডাটাবেস লিক হলে, সব পাসওয়ার্ড ফাঁস হয়ে যাবে!  

### **✅ সঠিক সমাধান (Password Hashing ব্যবহার করা)**  
```php
$hashed_password = password_hash($_POST['password'], PASSWORD_BCRYPT);
$sql = "INSERT INTO users (password) VALUES ('$hashed_password')";
```
✅ **`password_hash()` ফাংশন ব্যবহার করলে পাসওয়ার্ড এনক্রিপ্ট হয়ে যাবে।**  

### **🔹 লগইন যাচাই করা (Password Verify ব্যবহার করা)**  
```php
if (password_verify($_POST['password'], $db_hashed_password)) {
    echo "Login successful!";
} else {
    echo "Invalid password!";
}
```
✅ **`password_verify()` দিয়ে ইউজারের ইনপুট পাসওয়ার্ড মিলানো হয়।**  

---

## **✅ 5️⃣ File Upload Security (ফাইল আপলোড নিরাপত্তা)**  
**হ্যাকাররা PHP Shell আপলোড করে সার্ভার নিয়ন্ত্রণ নিতে পারে!**  

### **🚨 দুর্বল ফাইল আপলোড (❌ Avoid This)**  
```php
move_uploaded_file($_FILES["file"]["tmp_name"], "uploads/" . $_FILES["file"]["name"]);
```
✅ **সমস্যা:** `.php` ফাইল আপলোড হলে, সার্ভার হ্যাক হতে পারে!  

### **✅ সঠিক সমাধান (File Type & Extension যাচাই করা)**  
```php
$allowed_types = ['image/jpeg', 'image/png', 'image/gif'];
$file_type = mime_content_type($_FILES["file"]["tmp_name"]);

if (!in_array($file_type, $allowed_types)) {
    die("Invalid file type!");
}

$filename = uniqid() . "_" . basename($_FILES["file"]["name"]);
move_uploaded_file($_FILES["file"]["tmp_name"], "uploads/" . $filename);
```
✅ **Mime Type চেক করলে PHP Shell আপলোড প্রতিরোধ করা সম্ভব।**  

---

## **✅ 6️⃣ HTTPS বাধ্যতামূলক করা**  
সাইটে **HTTPS (SSL Certificate) চালু না থাকলে**, **Cookies চুরি বা MITM (Man-In-The-Middle) আক্রমণ হতে পারে!**  

### **✅ `.htaccess` ফোর্স HTTPS Redirect:**  
```apache
RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```
✅ **এটি অটোমেটিক HTTP থেকে HTTPS-এ রিডাইরেক্ট করবে।**  

---

## **✅ 7️⃣ Secure Headers ব্যবহার করা**  
### **👉 HTTP Security Headers সেট করা (Prevent XSS, Clickjacking)**  
```php
header("X-Frame-Options: DENY");
header("X-XSS-Protection: 1; mode=block");
header("X-Content-Type-Options: nosniff");
header("Strict-Transport-Security: max-age=31536000; includeSubDomains");
```
✅ **এটি বিভিন্ন হ্যাকিং আক্রমণ প্রতিরোধে সহায়ক।**  

---

## **🚀 সারসংক্ষেপ**  
| **সিকিউরিটি ঝুঁকি**                   | **প্রতিরোধের উপায়**                        |
| ------------------------------ | ------------------------------------- |
| **SQL Injection**              | **Prepared Statements ব্যবহার করা**      |
| **XSS (Cross-Site Scripting)** | **`htmlspecialchars()` ব্যবহার করা**     |
| **CSRF Attack**                | **CSRF Token ব্যবহার করা**               |
| **Weak Password Storage**      | **`password_hash()` ব্যবহার করা**        |
| **File Upload Vulnerability**  | **Mime Type & File Extension যাচাই করা** |
| **Session Hijacking**          | **HTTPS & Secure Cookies ব্যবহার করা**   |

---

## **🎯 পরবর্তী ধাপ**
✅ **আরও গভীরভাবে Security শিখতে চাও?**  
✅ **Laravel Security নিয়েও জানতে চাও?**  

👉 আমাকে বলো, **কোন বিষয়টি আরও ভালোভাবে ব্যাখ্যা করবো?** 😊