## **🚀 PHP-এর if/else শর্তবাক্য (Conditionals) সম্পূর্ণ গাইড**  

PHP-তে **if/else** শর্তবাক্য ব্যবহার করে আমরা **নির্দিষ্ট শর্তের ভিত্তিতে প্রোগ্রামের প্রবাহ নিয়ন্ত্রণ** করতে পারি।  

---

## **📌 1️⃣ if Statement (একটি শর্ত পরীক্ষা করা)**  
`if` স্টেটমেন্ট ব্যবহার করে আমরা **নির্দিষ্ট শর্ত** পূরণ হলে কোড এক্সিকিউট করতে পারি।  

### **🎯 Syntax:**  
```php
if (শর্ত) {
    // শর্ত সত্য হলে এটি চলবে
}
```

### **✅ উদাহরণ:**  
```php
<?php
$age = 20;

if ($age >= 18) {
    echo "You are eligible to vote.";
}
?>
```
🔹 এখানে `$age = 20` হওয়ায় শর্ত **সত্য**, তাই `"You are eligible to vote."` প্রিন্ট হবে।  

---

## **📌 2️⃣ if...else Statement (দুইটি শর্ত)**  
যদি `if`-এর শর্ত মিথ্যা হয়, তাহলে `else` ব্লক চালানো হবে।  

### **🎯 Syntax:**  
```php
if (শর্ত) {
    // শর্ত সত্য হলে এটি চলবে
} else {
    // শর্ত মিথ্যা হলে এটি চলবে
}
```

### **✅ উদাহরণ:**  
```php
<?php
$age = 16;

if ($age >= 18) {
    echo "You are eligible to vote.";
} else {
    echo "You are not eligible to vote.";
}
?>
```
🔹 এখানে `$age = 16`, যা ১৮-এর কম, তাই `"You are not eligible to vote."` প্রিন্ট হবে।  

---

## **📌 3️⃣ if...elseif...else Statement (একাধিক শর্ত পরীক্ষা করা)**  
যদি প্রথম শর্ত সত্য না হয়, তাহলে `elseif` ব্লক পরীক্ষা করবে।  

### **🎯 Syntax:**  
```php
if (শর্ত1) {
    // শর্ত1 সত্য হলে এটি চলবে
} elseif (শর্ত2) {
    // শর্ত2 সত্য হলে এটি চলবে
} else {
    // উপরের কোনো শর্ত সত্য না হলে এটি চলবে
}
```

### **✅ উদাহরণ:**  
```php
<?php
$marks = 75;

if ($marks >= 80) {
    echo "You got A+.";
} elseif ($marks >= 70) {
    echo "You got A.";
} elseif ($marks >= 60) {
    echo "You got B.";
} else {
    echo "You failed.";
}
?>
```
🔹 এখানে `$marks = 75`, তাই `"You got A."` প্রিন্ট হবে।  

---

## **📌 4️⃣ Nested if (if-এর ভিতরে if)**  
একটি `if` ব্লকের মধ্যে আরেকটি `if` ব্লক থাকতে পারে।  

### **✅ উদাহরণ:**  
```php
<?php
$age = 20;
$hasID = true;

if ($age >= 18) {
    if ($hasID) {
        echo "You can enter.";
    } else {
        echo "You need an ID card.";
    }
} else {
    echo "You are underaged.";
}
?>
```
🔹 যদি **বয়স ১৮ বা তার বেশি হয় এবং আইডি থাকে**, তাহলে `"You can enter."` প্রিন্ট হবে।  

---

## **📌 5️⃣ Ternary Operator (সংক্ষেপে if...else)**  
`if...else` কে সংক্ষেপে **Ternary Operator** দিয়ে লেখা যায়।  

### **🎯 Syntax:**  
```php
(শর্ত) ? সত্য হলে চলবে : মিথ্যা হলে চলবে;
```

### **✅ উদাহরণ:**  
```php
<?php
$age = 20;
$message = ($age >= 18) ? "You can vote." : "You cannot vote.";
echo $message;
?>
```
🔹 `$age >= 18` সত্য হওয়ায় **"You can vote."** প্রিন্ট হবে।  

---

## **🎯 অনুশীলনের জন্য চ্যালেঞ্জ 🚀**  
```php
<?php
$temperature = 35;

if ($temperature > 30) {
    echo "It's a hot day!";
} elseif ($temperature >= 20) {
    echo "It's a pleasant day!";
} else {
    echo "It's a cold day!";
}
?>
```
❓ **উপরের কোড রান করলে কী আউটপুট আসবে?** নিজে রান করে দেখো! 😃  

---

## **🚀 উপসংহার: আপনি এখন PHP-এর if/else শিখে ফেলেছেন! 🎉**  
✔ **`if, else, elseif`** ব্যবহার করে শর্ত পরীক্ষা করা যায়।  
✔ **Ternary Operator** দিয়ে `if...else` সংক্ষেপে লেখা যায়।  
✔ **Nested if** দিয়ে **শর্তের ভিতরে শর্ত** চেক করা যায়।  

---

## **➡ পরবর্তী কি শিখতে চান?**  
✅ **PHP-এর Loops (for, while, foreach) শিখতে চান? 🚀**