PHP-তে **State Management** হলো ইউজারের ডাটা সংরক্ষণ এবং পুনরায় ব্যবহারের প্রক্রিয়া, যেহেতু HTTP একটি **stateless** প্রোটোকল। এটি সাধারণত **session**, **cookies**, এবং **hidden fields** ব্যবহার করে করা হয়।  

আমি ধাপে ধাপে **PHP State Management** শেখাবো।  

---

## 1️⃣ **PHP State Management কী এবং কেন প্রয়োজন?**  
🔹 **HTTP Stateless:** HTTP কোনো পূর্ববর্তী অনুরোধের তথ্য মনে রাখে না, তাই ইউজারের তথ্য সংরক্ষণ করা দরকার।  
🔹 **State Management:** PHP-তে ইউজারের তথ্য ধরে রাখার জন্য **Session, Cookies, Hidden Fields, এবং URL Parameters** ব্যবহৃত হয়।  

---

## 2️⃣ **PHP Cookies**
Cookies হলো ছোট ডাটা, যা ব্রাউজারে সংরক্ষণ করা হয় এবং পরবর্তীতে সার্ভার থেকে পড়া যায়। এটি সাধারণত ইউজারের পছন্দ সংরক্ষণ বা লগইন ব্যবস্থায় ব্যবহৃত হয়।  

### **👉 কুকি সেট করা**  
```php
setcookie("username", "JohnDoe", time() + (86400 * 7), "/"); // 7 দিন থাকবে
```
**ব্যাখ্যা:**  
- `"username"` → কুকির নাম  
- `"JohnDoe"` → কুকির ভ্যালু  
- `time() + (86400 * 7)` → কুকির মেয়াদ ৭ দিন  
- `"/"` → সাইটের সব জায়গায় অ্যাক্সেসযোগ্য  

### **👉 কুকি রিড করা**  
```php
if (isset($_COOKIE["username"])) {
    echo "Welcome, " . $_COOKIE["username"];
} else {
    echo "Cookie is not set!";
}
```

### **👉 কুকি ডিলিট করা**  
```php
setcookie("username", "", time() - 3600, "/"); // কুকি মুছে ফেলবে
```

✅ **Cookies প্রধানত ব্যবহার করা হয়:**  
- ইউজার প্রেফারেন্স সংরক্ষণ  
- অটো-লগইন বা রিমেম্বার মি ফিচার  

---

## 3️⃣ **PHP Sessions**
Session হলো **সার্ভার-সাইড স্টোরেজ**, যেখানে ইউজারের তথ্য সংরক্ষণ করা হয়। কুকির তুলনায় এটি বেশি সুরক্ষিত, কারণ তথ্য ব্রাউজারের পরিবর্তে সার্ভারে থাকে।  

### **👉 সেশন শুরু করা**  
```php
session_start(); // সেশন শুরু
$_SESSION["username"] = "JohnDoe"; // ডাটা সেট
```

### **👉 সেশন ডাটা রিড করা**  
```php
session_start();
if (isset($_SESSION["username"])) {
    echo "Welcome, " . $_SESSION["username"];
} else {
    echo "Session not set!";
}
```

### **👉 সেশন ডিলিট করা**  
```php
session_start();
session_unset(); // সব সেশন ভ্যারিয়েবল মুছে ফেলা
session_destroy(); // সম্পূর্ণ সেশন ধ্বংস করা
```

✅ **Session সাধারণত ব্যবহার করা হয়:**  
- ইউজার লগইন/অথেনটিকেশন ম্যানেজ করতে  
- ওয়েবসাইটের কার্ট/শপিং ব্যাগ ম্যানেজ করতে  

---

## 4️⃣ **PHP Hidden Fields (ফর্মের মাধ্যমে ডাটা সংরক্ষণ)**
Hidden Fields ব্যবহার করে **ফর্মের মাধ্যমে ইউজারের ডাটা পাস** করা যায়, যা **সেশন ও কুকির বিকল্প হিসেবে কাজ করে**।  

### **👉 Hidden Field ব্যবহার করে ডাটা পাঠানো**
```html
<form method="post" action="process.php">
    <input type="hidden" name="user_id" value="12345">
    <button type="submit">Submit</button>
</form>
```

### **👉 PHP-তে Hidden Field রিড করা**
```php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $user_id = $_POST["user_id"];
    echo "User ID: " . $user_id;
}
```
✅ **Hidden Fields প্রধানত ব্যবহার করা হয়:**  
- ফর্ম সাবমিশনের সময় ডাটা ট্র্যাক রাখতে  
- পাসওয়ার্ড রিসেট বা টোকেন স্টোর করতে  

---

## 5️⃣ **PHP URL Parameters (Query String)**
URL-এ ডাটা পাঠানোর সহজ উপায় হলো Query String (**GET Method**)।  

### **👉 URL-এ ডাটা পাঠানো**
```html
<a href="profile.php?username=JohnDoe">View Profile</a>
```

### **👉 PHP-তে URL ডাটা রিড করা**
```php
if (isset($_GET["username"])) {
    echo "Welcome, " . $_GET["username"];
}
```

✅ **URL Parameters সাধারণত ব্যবহার করা হয়:**  
- সার্চ রেজাল্ট দেখাতে  
- ফিল্টারিং ও পেজিনেশন ম্যানেজ করতে  

---

## **🎯 সারসংক্ষেপ**
| **State Management পদ্ধতি** | **কোথায় সংরক্ষণ হয়?** | **প্রধান ব্যবহার**             |
| ------------------------- | ----------------- | -------------------------- |
| **Cookies**               | ব্রাউজার             | ইউজার প্রেফারেন্স সংরক্ষণ, অটো-লগইন |
| **Sessions**              | সার্ভার              | লগইন ব্যবস্থাপনা, শপিং কার্ট       |
| **Hidden Fields**         | ফর্ম ডাটা            | ফর্ম সাবমিশনে ডাটা ট্র্যাক          |
| **URL Parameters**        | URL               | সার্চ রেজাল্ট, ফিল্টারিং, পেজিনেশন      |

---

## ✅ **পরবর্তী ধাপ**
এখন তুমি চাইলে **Session & Cookie Security**, **JWT Token Authentication**, এবং **Laravel State Management** নিয়েও জানতে পারো! 🚀