## **🔒 PHP Password Hashing: সুরক্ষিত পাসওয়ার্ড সংরক্ষণ**

পাসওয়ার্ড সুরক্ষা একটি গুরুত্বপূর্ণ নিরাপত্তা বিষয়, এবং পাসওয়ার্ডগুলি সরাসরি ডাটাবেসে সংরক্ষণ করা একেবারে বিপজ্জনক। এর পরিবর্তে, **পাসওয়ার্ড হ্যাশিং** ব্যবহার করে পাসওয়ার্ডগুলো সুরক্ষিতভাবে সংরক্ষণ করা উচিত। 

### **🚨 কেন পাসওয়ার্ড হ্যাশিং প্রয়োজন?**

- **সরাসরি পাসওয়ার্ড সংরক্ষণ করা**: যদি আপনি সরাসরি পাসওয়ার্ড ডাটাবেসে সংরক্ষণ করেন, তবে আক্রমণকারীরা যদি ডাটাবেসে প্রবেশ করে, তাদের পাসওয়ার্ড সহজেই বের করতে পারে।
- **পাসওয়ার্ড হ্যাশিং**: হ্যাশিং একটি **একপথীয় (one-way)** প্রক্রিয়া, যেখানে পাসওয়ার্ড একটি নির্দিষ্ট লম্বা স্ট্রিংয়ে রূপান্তরিত হয়, যা পূর্বাবস্থায় ফিরিয়ে আনা সম্ভব নয়।

---

## **✅ 1️⃣ পাসওয়ার্ড হ্যাশিং এর মৌলিক ধারণা**

### **🚀 হ্যাশিং কি?**
হ্যাশিং হল এমন একটি প্রক্রিয়া যার মাধ্যমে ইনপুট (যেমন পাসওয়ার্ড) একটি নির্দিষ্ট আউটপুটে রূপান্তরিত হয়। এই আউটপুটটি নির্দিষ্ট আছেঃ 

- **একপথীয়**: অর্থাৎ, আপনি হ্যাশ থেকে আসল পাসওয়ার্ড বের করতে পারবেন না।
- **নির্দিষ্ট আউটপুট**: প্রতিটি ইনপুটের জন্য একটি নির্দিষ্ট আউটপুট হয়।
- **নিরাপত্তা**: হ্যাশটি আক্রমণকারীদের জন্য প্রায় অপরিবর্তনীয়, তাই এটি নিরাপদ।

### **🚀 হ্যাশিং ব্যবহার করার পদ্ধতি:**
PHP তে পাসওয়ার্ড হ্যাশ করার জন্য **`password_hash()`** ফাংশন ব্যবহার করা হয়।

---

## **✅ 2️⃣ PHP তে পাসওয়ার্ড হ্যাশিং**

### **🚀 `password_hash()` ফাংশন:**
**`password_hash()`** ফাংশনটি একটি পাসওয়ার্ড গ্রহণ করে এবং তার জন্য একটি **হ্যাশ তৈরি** করে। এর মধ্যে প্রথম আর্গুমেন্ট হিসেবে পাসওয়ার্ড এবং দ্বিতীয় আর্গুমেন্ট হিসেবে হ্যাশিং এলগরিদম পাস করতে হয়।

#### **Syntax:**
```php
$password = "user_password";
$hashed_password = password_hash($password, PASSWORD_DEFAULT);
```

**`PASSWORD_DEFAULT`** মানে, PHP সর্বশেষ সেরা হ্যাশিং এলগরিদম ব্যবহার করবে। সাধারণত এটি **bcrypt** হয়।

#### **Example:**
```php
<?php
$password = "user_password"; // Original password
$hashed_password = password_hash($password, PASSWORD_DEFAULT); // Hashed password
echo $hashed_password; // Output the hashed password
?>
```

**Note:** **bcrypt** হ্যাশিং পদ্ধতি অত্যন্ত নিরাপদ এবং এটি **salted hash** তৈরি করে, যাতে হ্যাশটি একে অপর থেকে আলাদা হয়।

---

## **✅ 3️⃣ পাসওয়ার্ড যাচাই করা**

আপনি যখন পাসওয়ার্ড যাচাই করতে চান, তখন **`password_verify()`** ফাংশন ব্যবহার করবেন। এই ফাংশনটি ইনপুট পাসওয়ার্ড এবং ডাটাবেসে সংরক্ষিত হ্যাশের সাথে তুলনা করে।

#### **Syntax:**
```php
if (password_verify($password, $hashed_password)) {
    echo "Password is correct!";
} else {
    echo "Incorrect password!";
}
```

#### **Example:**
```php
<?php
$password = "user_password"; // User input
$hashed_password = '$2y$10$wCjE6POYqV1XfxFgF3GTZeMdbLRdy5kEdj7WbMTeXeq7kpXa9VVni'; // Stored hashed password

if (password_verify($password, $hashed_password)) {
    echo "Password is correct!";
} else {
    echo "Incorrect password!";
}
?>
```
✅ **এই পদ্ধতি ব্যবহারকারী যখন লগইন করেন, তখন ডাটাবেসে থাকা হ্যাশের সাথে ইনপুট পাসওয়ার্ড মিলিয়ে সঠিকতা যাচাই করা হয়।**

---

## **✅ 4️⃣ Password Hashing এর গুণাবলি**

1. **Salted Hash**: **`password_hash()`** স্বয়ংক্রিয়ভাবে **salt** যোগ করে, যাতে একই পাসওয়ার্ডের জন্য ভিন্ন ভিন্ন হ্যাশ তৈরি হয়।
2. **Work Factor (Cost)**: `bcrypt` এলগরিদমের কাজ করার সময় ব্যয় নিয়ন্ত্রণ করার জন্য **work factor** ব্যবহার হয়। এটি সিস্টেমে আক্রমণকারীকে **brute force** আক্রমণ করতে কঠিন করে তোলে।

---

## **✅ 5️⃣ পাসওয়ার্ড আপডেট করা**

যখন আপনি নতুন হ্যাশিং এলগরিদম ব্যবহার করতে চান, তখন আপনার পাসওয়ার্ড হ্যাশটি আপডেট করতে হবে। এর জন্য **`password_needs_rehash()`** ফাংশন ব্যবহার করা হয়।

#### **Example:**
```php
<?php
$password = "user_password";
$hashed_password = password_hash($password, PASSWORD_DEFAULT);

// Check if the password needs rehashing (due to new algorithm or cost)
if (password_needs_rehash($hashed_password, PASSWORD_DEFAULT)) {
    $hashed_password = password_hash($password, PASSWORD_DEFAULT);
    echo "Password rehashed!";
}
?>
```

✅ **এটি নিশ্চিত করে যে, আপনি সর্বশেষ নিরাপত্তা পদ্ধতি ব্যবহার করছেন।**

---

## **✅ 6️⃣ PHP পাসওয়ার্ড হ্যাশিং টিপস**

1. **কখনো পাসওয়ার্ড মজা করে রাখবেন না**: পাসওয়ার্ড শুধু হ্যাশ করুন, কোন অবস্থাতেই সরাসরি ডাটাবেসে সংরক্ষণ করবেন না।
2. **`password_hash()` ও `password_verify()` ব্যবহার করুন**: এগুলি ব্যবহার করে সহজে নিরাপদ পাসওয়ার্ড স্টোর এবং যাচাই করুন।
3. **হ্যাশ আপডেট**: যতটুকু সম্ভব, পাসওয়ার্ড হ্যাশিংয়ের সময়পথ পরিবর্তন করতে নতুন হ্যাশিং এলগরিদম ব্যবহার করুন।

---

## **🚀 সারসংক্ষেপ**

| **PHP Function**              | **Purpose**                                                                        |
| ----------------------------- | ---------------------------------------------------------------------------------- |
| **`password_hash()`**         | Hashes the password securely using bcrypt                                          |
| **`password_verify()`**       | Verifies a password against a hash                                                 |
| **`password_needs_rehash()`** | Checks if the password needs rehashing due to a change in algorithm or cost factor |

---

## **🚀 পরবর্তী ধাপ**

আপনি যদি **PHP Security** সম্পর্কে আরও কিছু জানতে চান বা কোনো নির্দিষ্ট বিষয়ের উপর সাহায্য প্রয়োজন হয়, আমাকে বলুন! 😊