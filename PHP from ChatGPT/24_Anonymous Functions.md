### PHP **Anonymous Functions** (ল্যাম্বডা ফাংশন)  
PHP-তে **Anonymous Functions** (বা **Lambda Functions**) হল এমন ফাংশন যেগুলোর **নাম থাকে না**। এগুলোকে **Closure** ও বলা হয় এবং সাধারণত **ভেরিয়েবলে সংরক্ষণ করা**, **অন্যান্য ফাংশনের আর্গুমেন্ট হিসেবে পাঠানো**, বা **কলব্যাক (callback) হিসেবে ব্যবহার করা** হয়।

---

## **1. Anonymous Function কিভাবে লিখতে হয়?**
সাধারণত `function` কীওয়ার্ড ব্যবহার করেই Anonymous Function ডিফাইন করা হয়, তবে এতে কোনো ফাংশনের নাম থাকে না।

### **সাধারণ Anonymous Function উদাহরণ:**
```php
<?php
// Anonymous Function একটি ভেরিয়েবলে সংরক্ষণ করা
$greet = function($name) {
    return "Hello, $name!";
};

// ফাংশন কল করা
echo $greet("Alice"); // আউটপুট: Hello, Alice!
?>
```
এখানে,  
- `$greet` ভেরিয়েবলে একটি **Anonymous Function** সংরক্ষণ করা হয়েছে।  
- `$greet("Alice")` দিয়ে ফাংশনটি কল করা হয়েছে।

---

## **2. Anonymous Function এর ব্যবহার**
Anonymous Functions সাধারণত **callback function**, **array manipulation**, এবং **closures** হিসেবে ব্যবহৃত হয়।

### **(1) Callback হিসেবে ব্যবহার**
PHP-তে কিছু বিল্ট-ইন ফাংশন আছে যেগুলোতে কলব্যাক ফাংশন প্রয়োজন হয়, যেমন `array_map()`, `array_filter()`, ইত্যাদি।

#### **Anonymous Function ব্যবহার করে `array_map()` উদাহরণ**
```php
<?php
$numbers = [1, 2, 3, 4, 5];

// প্রতিটি উপাদান দ্বিগুণ করার জন্য Anonymous Function ব্যবহার
$doubled = array_map(function($num) {
    return $num * 2;
}, $numbers);

print_r($doubled); // আউটপুট: [2, 4, 6, 8, 10]
?>
```
এখানে `array_map()` ফাংশনের মধ্যে Anonymous Function পাঠানো হয়েছে, যা প্রতিটি উপাদানকে দ্বিগুণ করছে।

#### **Anonymous Function ব্যবহার করে `array_filter()` উদাহরণ**
```php
<?php
$numbers = [1, 2, 3, 4, 5, 6];

// শুধুমাত্র জোড় সংখ্যা ফিল্টার করা
$evenNumbers = array_filter($numbers, function($num) {
    return $num % 2 === 0;
});

print_r($evenNumbers); // আউটপুট: [2, 4, 6]
?>
```
এখানে `array_filter()`-এ Anonymous Function ব্যবহার করে শুধু **জোড় সংখ্যা** নির্বাচন করা হয়েছে।

---

## **3. Closure এবং `use` কীওয়ার্ড**
Anonymous Function গুলো সাধারণত **Closure** হিসেবে কাজ করে, অর্থাৎ তারা বাইরের স্কোপের ভেরিয়েবল সরাসরি ব্যবহার করতে পারে না। কিন্তু **`use` কীওয়ার্ড** ব্যবহার করে বাইরের ভেরিয়েবলকে ফাংশনের ভিতরে ব্যবহার করা যায়।

### **Closure এর উদাহরণ**
```php
<?php
$message = "Hello";

// Anonymous Function বাইরের ভেরিয়েবল `use` করে ব্যবহার করছে
$greet = function($name) use ($message) {
    return "$message, $name!";
};

echo $greet("Alice"); // আউটপুট: Hello, Alice!
?>
```
এখানে,  
- `$message` ভেরিয়েবলটি **Anonymous Function এর বাইরে** ডিফাইন করা হয়েছে।  
- `use ($message)` লিখে আমরা এটি ফাংশনের ভিতরে এনেছি, যাতে এটি `"$message, $name!"` স্ট্রিং তৈরিতে ব্যবহার করা যায়।

---

## **4. Arrow Function (PHP 7.4 থেকে)**
PHP 7.4 এ **Arrow Function** যোগ হয়েছে, যা Anonymous Function এর সংক্ষিপ্ত সংস্করণ।

### **Arrow Function ব্যবহার করা**
```php
<?php
$numbers = [1, 2, 3, 4, 5];

// Arrow Function ব্যবহার করে প্রতিটি উপাদান দ্বিগুণ করা
$doubled = array_map(fn($num) => $num * 2, $numbers);

print_r($doubled); // আউটপুট: [2, 4, 6, 8, 10]
?>
```
**Arrow Function এর সুবিধা:**
- `fn($param) => expression` আকারে লেখা হয়।
- এটি স্বয়ংক্রিয়ভাবে বাইরের স্কোপের ভেরিয়েবল অ্যাক্সেস করতে পারে, `use` কীওয়ার্ড দরকার হয় না।

### **Arrow Function বনাম Anonymous Function তুলনা**
#### **Anonymous Function:**
```php
$multiply = function($a, $b) {
    return $a * $b;
};
echo $multiply(3, 4); // আউটপুট: 12
```
#### **Arrow Function:**
```php
$multiply = fn($a, $b) => $a * $b;
echo $multiply(3, 4); // আউটপুট: 12
```
Arrow Function আরো সংক্ষিপ্ত এবং সহজলভ্য।

---

## **5. Recursive Anonymous Function (নিজেকে নিজে কল করা)**
PHP-তে Anonymous Function গুলো **Recursive (পুনরাবৃত্তিমূলক)** হতে পারে।

### **ফ্যাক্টরিয়াল গণনা Anonymous Function দিয়ে**
```php
<?php
$factorial = function($n) use (&$factorial) {
    return ($n <= 1) ? 1 : $n * $factorial($n - 1);
};

echo $factorial(5); // আউটপুট: 120
?>
```
এখানে,  
- **`use (&$factorial)`** এর মাধ্যমে ফাংশনটি নিজেকেই কল করতে পারছে।

---

## **6. Anonymous Class এর সাথে ব্যবহার**
Anonymous Function **Anonymous Class** এর সাথে ব্যবহার করা যায়।

```php
<?php
$object = new class {
    public $name = "Alice";

    public function greet() {
        return function() {
            return "Hello, " . $this->name . "!";
        };
    }
};

$greetFunction = $object->greet();
echo $greetFunction(); // আউটপুট: Hello, Alice!
?>
```
এখানে Anonymous Function `greet()` মেথডের মাধ্যমে একটি **Closure** রিটার্ন করছে।

---

## **সারাংশ**
✅ **Anonymous Function** নামবিহীন ফাংশন যা ভেরিয়েবলে সংরক্ষণ করা যায়।  
✅ **Callback হিসেবে ব্যবহার করা যায়**, যেমন `array_map()`, `array_filter()` ইত্যাদির জন্য।  
✅ **Closure এর মাধ্যমে বাইরের ভেরিয়েবল `use` করে ব্যবহার করা যায়।**  
✅ **PHP 7.4 থেকে Arrow Function যোগ হয়েছে**, যা সংক্ষিপ্ত এবং সহজ।  
✅ **Recursive Anonymous Function তৈরি করা সম্ভব।**  

### **আপনি যদি বুঝতে না পারেন বা কোনো নির্দিষ্ট বিষয়ে আরও জানতে চান, তাহলে আমাকে জানান!** 😊