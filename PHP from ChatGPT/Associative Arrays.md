# **PHP-এর Associative Arrays সম্পূর্ণ গাইড** 🚀  

PHP-তে **Associative Array** এমন একটি অ্যারে যেখানে **কী (key) এবং মান (value)** থাকে। **Indexed Array-এর মতো সংখ্যা ভিত্তিক ইনডেক্স ব্যবহার না করে, এখানে প্রতিটি মান একটি কাস্টম কী-এর সাথে সংরক্ষণ করা হয়।**  

---

## **📌 1️⃣ Associative Array তৈরি করার ২টি উপায়**  

🔹 **Indexed Array-এর ইনডেক্স সংখ্যা হয়, কিন্তু Associative Array-তে কাস্টম কী থাকে।**  
🔹 **প্রতিটি কী একটি স্ট্রিং বা সংখ্যা হতে পারে।**  

### **🎯 পদ্ধতি ১: `array()` ফাংশন ব্যবহার করে Associative Array তৈরি করা**  
```php
<?php
$person = array("name" => "Rahim", "age" => 25, "city" => "Dhaka");

echo $person["name"]; // Output: Rahim
?>
```

### **🎯 পদ্ধতি ২: সরাসরি `[]` ব্যবহার করে Associative Array তৈরি করা (PHP 5.4+)**  
```php
<?php
$student = [
    "name" => "Karim",
    "class" => "10th",
    "grade" => "A"
];

echo $student["class"]; // Output: 10th
?>
```
✅ **উপরের দুই পদ্ধতিতেই Associative Array তৈরি করা যায়।**  

---

## **📌 2️⃣ Associative Array-এর উপাদান পরিবর্তন করা**  
```php
<?php
$person = ["name" => "Rahim", "age" => 25, "city" => "Dhaka"];
$person["city"] = "Chittagong"; // মান পরিবর্তন করা হলো

echo $person["city"]; // Output: Chittagong
?>
```
✅ **Associative Array-এর মান পরিবর্তন করা সম্ভব।**  

---

## **📌 3️⃣ Associative Array-এর উপাদান যোগ করা (Add Elements)**  
### **🎯 নতুন কী-ভ্যালু যোগ করা:**  
```php
<?php
$person = ["name" => "Rahim", "age" => 25];
$person["country"] = "Bangladesh"; // নতুন মান যোগ করা হলো

print_r($person);
// Output: Array ( [name] => Rahim [age] => 25 [country] => Bangladesh )
?>
```
✅ **নতুন কী-ভ্যালু যোগ করতে `$array["new_key"] = "value";` ব্যবহার করা যায়।**  

---

## **📌 4️⃣ Associative Array-এর উপাদান মুছে ফেলা (Remove Elements)**  
### **🎯 `unset()` ব্যবহার করে নির্দিষ্ট কী-এর উপাদান মুছে ফেলা:**  
```php
<?php
$student = ["name" => "Karim", "class" => "10th", "grade" => "A"];
unset($student["class"]); // "class" কী-এর মান মুছে ফেলা হলো

print_r($student);
// Output: Array ( [name] => Karim [grade] => A )
?>
```
✅ **`unset($array["key"]);` ব্যবহার করে নির্দিষ্ট উপাদান মুছে ফেলা যায়।**  

---

## **📌 5️⃣ Associative Array লুপ করে দেখানো (Iteration over Associative Array)**  
### **🎯 `foreach` লুপ ব্যবহার করে Associative Array লুপ করা**  
```php
<?php
$student = ["name" => "Karim", "class" => "10th", "grade" => "A"];

foreach ($student as $key => $value) {
    echo "$key : $value <br>";
}
?>
```
✅ **`foreach` লুপ ব্যবহার করে সহজে কী-ভ্যালু একসাথে দেখা যায়।**  

---

## **📌 6️⃣ Associative Array-এর দরকারি ফাংশন**  

| ফাংশন                               | কাজ                            |
| --------------------------------- | ----------------------------- |
| `array_keys($array)`              | অ্যারের সব কী গুলো নিয়ে আসে            |
| `array_values($array)`            | অ্যারের সব মান গুলো নিয়ে আসে           |
| `count($array)`                   | অ্যারের উপাদান সংখ্যা গণনা করে          |
| `array_key_exists("key", $array)` | নির্দিষ্ট কী অ্যারেতে আছে কিনা যাচাই করে     |
| `isset($array["key"])`            | নির্দিষ্ট কী-এর মান সেট আছে কিনা চেক করে  |
| `array_merge($array1, $array2)`   | দুটি Associative Array একত্রিত করে |
| `array_flip($array)`              | কী ও মান উল্টে ফেলে                 |

### **🎯 কিছু দরকারি Associative Array ফাংশনের ব্যবহার**  
```php
<?php
$person = ["name" => "Rahim", "age" => 25, "city" => "Dhaka"];

$keys = array_keys($person);
$values = array_values($person);

print_r($keys);   // Output: Array ( [0] => name [1] => age [2] => city )
print_r($values); // Output: Array ( [0] => Rahim [1] => 25 [2] => Dhaka )
?>
```
✅ **`array_keys()` এবং `array_values()` ফাংশন ব্যবহার করে অ্যারের কী ও মান পাওয়া যায়।**  

---

## **📌 7️⃣ Associative Array Sort করা (Sorting)**  

### **🎯 `asort()` - মান অনুযায়ী অ্যারে সাজানো (Ascending Order)**  
```php
<?php
$ages = ["Rahim" => 25, "Karim" => 20, "Salam" => 30];
asort($ages); // মান অনুযায়ী ছোট থেকে বড় হবে

print_r($ages);
// Output: Array ( [Karim] => 20 [Rahim] => 25 [Salam] => 30 )
?>
```

### **🎯 `ksort()` - কী অনুযায়ী অ্যারে সাজানো (Ascending Order)**  
```php
<?php
$ages = ["Rahim" => 25, "Karim" => 20, "Salam" => 30];
ksort($ages); // কী অনুযায়ী ছোট থেকে বড় হবে

print_r($ages);
// Output: Array ( [Karim] => 20 [Rahim] => 25 [Salam] => 30 )
?>
```

### **🎯 `arsort()` - মান অনুযায়ী বিপরীত ক্রমে সাজানো (Descending Order)**  
```php
<?php
$ages = ["Rahim" => 25, "Karim" => 20, "Salam" => 30];
arsort($ages); // মান অনুযায়ী বড় থেকে ছোট হবে

print_r($ages);
// Output: Array ( [Salam] => 30 [Rahim] => 25 [Karim] => 20 )
?>
```
✅ **Sorting ফাংশন ব্যবহার করে Associative Array সাজানো যায়।**  

---

## **🎯 Associative Array অনুশীলনের জন্য চ্যালেঞ্জ 🚀**  
👉 নিচের কোডের আউটপুট কী হবে?  
```php
<?php
$info = ["name" => "Rahim", "age" => 30, "city" => "Dhaka"];
unset($info["age"]);
$info["country"] = "Bangladesh";

print_r($info);
?>
```
✅ **নিজে রান করে দেখো!** 😃  

---

## **🚀 উপসংহার: আপনি এখন Associative Array শিখে ফেলেছেন! 🎉**  
✔ **Associative Array এমন একটি অ্যারে যেখানে Indexed Array-এর মতো সংখ্যাসূচক ইনডেক্স না হয়ে কাস্টম কী (key) থাকে।**  
✔ Associative Array তৈরি করা যায় `array()` অথবা `[]` ব্যবহার করে।  
✔ Associative Array-এর উপাদান **পরিবর্তন, যোগ, মুছে ফেলা ও লুপ করা যায়।**  
✔ Associative Array-এর জন্য **foreach loop এবং বিভিন্ন ফাংশন** রয়েছে।  
✔ Associative Array **Sorting ফাংশন** ব্যবহার করে সাজানো যায়।  

---

## **➡ পরবর্তী কি শিখতে চান?**  
✅ **Multidimensional Array** শিখতে চান? 🚀