# 🚀 **PHP Recursion (Recursive Functions) বিস্তারিত শেখা** 🚀  

## 📌 **Recursion কী?**  
**Recursion** হলো এমন একটি টেকনিক যেখানে **একটি ফাংশন নিজেকে কল করে** নির্দিষ্ট শর্ত পূরণ না হওয়া পর্যন্ত।  

✅ এটি **Loop-এর বিকল্প** হিসেবে ব্যবহার করা হয়।  
✅ সাধারণত **ফ্যাক্টোরিয়াল, ফিবোনাচ্চি সিরিজ, ট্রি ট্রাভার্সাল** ইত্যাদির জন্য ব্যবহার করা হয়।  
✅ **Recursion এর ২টি গুরুত্বপূর্ণ অংশ:**  
1️⃣ **Base Case (স্টপ কন্ডিশন)** – যেখানে রিকারশন থামবে।  
2️⃣ **Recursive Case** – যেখানে ফাংশন নিজেকে কল করবে।  

---

## 🔥 **PHP Recursion এর Syntax**
```php
function recursiveFunction() {
    if (stopCondition) {  // Base Case (Recursion থামানোর শর্ত)
        return;
    }
    recursiveFunction(); // Recursive Call (ফাংশন নিজেকেই কল করছে)
}
```

---

## 🎯 **১. সাধারণ Recursion উদাহরণ**  
```php
<?php
function countdown($n) {
    if ($n <= 0) { // Base Case
        echo "Blastoff!\n";
        return;
    }
    echo "$n\n";
    countdown($n - 1); // Recursive Call
}

countdown(5);
?>
```
✔️ **আউটপুট:**  
```
5
4
3
2
1
Blastoff!
```
✅ **Recursion দিয়ে Countdown তৈরি হলো!**

---

## 🎯 **২. Factorial গণনা (Recursive Function)**
> **Factorial**: `n! = n * (n-1)!`  

```php
<?php
function factorial($n) {
    if ($n == 0) return 1; // Base Case
    return $n * factorial($n - 1); // Recursive Case
}

echo factorial(5);
?>
```
✔️ **আউটপুট:**  
```
120
```
✅ **Factorial `5! = 5 × 4 × 3 × 2 × 1 = 120`**

---

## 🎯 **৩. Fibonacci Series (Recursive Function)**
> **Fibonacci সিরিজ:** `F(n) = F(n-1) + F(n-2)`  
> (যেখানে, `F(0) = 0` এবং `F(1) = 1`)  

```php
<?php
function fibonacci($n) {
    if ($n == 0) return 0; // Base Case 1
    if ($n == 1) return 1; // Base Case 2
    return fibonacci($n - 1) + fibonacci($n - 2); // Recursive Case
}

echo fibonacci(6);
?>
```
✔️ **আউটপুট:**  
```
8
```
✅ **Fibonacci সিরিজ: `0, 1, 1, 2, 3, 5, 8...` (6th Fibonacci Number = 8)**  

---

## 🎯 **৪. Array Sum (Recursive Function)**
```php
<?php
function arraySum($arr, $index = 0) {
    if ($index == count($arr)) return 0; // Base Case
    return $arr[$index] + arraySum($arr, $index + 1); // Recursive Case
}

$numbers = [1, 2, 3, 4, 5];
echo arraySum($numbers);
?>
```
✔️ **আউটপুট:**  
```
15
```
✅ **`1+2+3+4+5 = 15` সঠিক ফলাফল দিয়েছে!**

---

## 🎯 **৫. String Reverse (Recursive Function)**
```php
<?php
function reverseString($str) {
    if (strlen($str) == 0) return ""; // Base Case
    return reverseString(substr($str, 1)) . $str[0]; // Recursive Case
}

echo reverseString("hello");
?>
```
✔️ **আউটপুট:**  
```
olleh
```
✅ **Recursion দিয়ে স্ট্রিং রিভার্স!**

---

## 📌 **Recursive Function এর সুবিধা (Advantages)**
✅ **কোড ছোট ও ক্লিন**  
✅ **Loop ছাড়াই সমস্যার সমাধান করা যায়**  
✅ **কমপ্লেক্স প্রবলেম (ফিবোনাচ্চি, ট্রি ট্রাভার্সাল) সহজ করে**  

---

## ⚠️ **Recursive Function এর সমস্যাসমূহ (Disadvantages)**
❌ **Stack Overflow** হতে পারে যদি Base Case ভুল হয়  
❌ **Performance কম (Iteration থেকে ধীরগতির হতে পারে)**  
❌ **অনেক Recursive Call বেশি মেমোরি ব্যবহার করে**  

### 🛑 **Infinite Recursion (ভুল করে স্টপ কন্ডিশন বাদ দিলে!)**
```php
<?php
function infiniteRecursion($n) {
    echo "$n\n";
    infiniteRecursion($n + 1); // ❌ ভুল! কখনো থামবে না!
}

infiniteRecursion(1);
?>
```
🚨 **এই কোড কখনো থামবে না এবং Stack Overflow হয়ে যাবে!**  
✅ **সমাধান:** Base Case যুক্ত করা!  

---

## 🎯 **Iteration vs Recursion: কোনটা ভালো?**
| **Feature**      | **Iteration (Loop)** | **Recursion**                    |
| ---------------- | -------------------- | -------------------------------- |
| **Speed**        | ✅ ফাস্ট                | ❌ ধীরগতি হতে পারে                     |
| **Memory Usage** | ✅ কম                 | ❌ বেশি (Stack Frames)              |
| **Complexity**   | ❌ বড় ও জটিল হতে পারে     | ✅ ক্লিন ও সহজ                      |
| **Use Case**     | Simple Loops         | Complex (Tree, Graph, Fibonacci) |

✔️ **Simple সমস্যা হলে Iteration ভালো**  
✔️ **Complex সমস্যা হলে Recursion দরকার**  

---

## 🎯 **Recursion কবে ব্যবহার করবেন?**
✅ **Fibonacci Series, Factorial, Exponentiation**  
✅ **Tree Traversal (Binary Tree, File System Search)**  
✅ **Graph Algorithms (DFS, BFS)**  
✅ **Backtracking (Maze Solving, Sudoku Solver)**  
✅ **Divide and Conquer Algorithms (Merge Sort, Quick Sort)**  

---

## 🚀 **শেষ কথা**
✅ **Recursion হলো এমন ফাংশন, যা নিজেকে কল করে**  
✅ **Base Case থাকা বাধ্যতামূলক** (নইলে Stack Overflow হবে)  
✅ **Loop এর বিকল্প হিসেবে অনেক ক্ষেত্রে ব্যবহার হয়**  
✅ **Performance issue হতে পারে, তাই বুঝে ব্যবহার করতে হবে**  

---

### 🎯 **Recursion বুঝতে সমস্যা হলে আমাকে বলো! 😊**