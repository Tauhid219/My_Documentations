# **🚀 PHP-এর `switch` স্টেটমেন্ট সম্পূর্ণ গাইড**  

`switch` স্টেটমেন্ট **একটি ভেরিয়েবলের একাধিক মান পরীক্ষা করতে** ব্যবহৃত হয়। এটি `if...elseif...else` স্টেটমেন্টের চেয়ে **কোডকে সহজ এবং পরিষ্কার** করে তোলে।  

---

## **📌 1️⃣ `switch` স্টেটমেন্টের মূল গঠন (Syntax)**  
```php
switch (ভেরিয়েবল) {
    case মান1:
        // যদি মান1 মিলে যায়, এটি চলবে
        break;
    case মান2:
        // যদি মান2 মিলে যায়, এটি চলবে
        break;
    default:
        // যদি কোনো মানই মিলে না যায়, এটি চলবে
}
```
🔹 **`break`** স্টেটমেন্ট **প্রত্যেক case শেষ করার জন্য ব্যবহার করা হয়**।  
🔹 **`default`** ব্লক **কোনো case না মিললে** এক্সিকিউট হয়।  

---

## **📌 2️⃣ সহজ উদাহরণ: সপ্তাহের দিন চেক করা**  
```php
<?php
$day = "Monday";

switch ($day) {
    case "Saturday":
    case "Sunday":
        echo "It's weekend!";
        break;
    case "Monday":
        echo "It's a weekday!";
        break;
    case "Friday":
        echo "Weekend is coming!";
        break;
    default:
        echo "Invalid day!";
}
?>
```
✅ যদি `$day = "Monday"` হয়, তাহলে আউটপুট হবে:  
```
It's a weekday!
```
✅ যদি `$day = "Sunday"` হয়, তাহলে আউটপুট হবে:  
```
It's weekend!
```

---

## **📌 3️⃣ `if...elseif...else` বনাম `switch`**  
### **🟢 `if...elseif...else` ব্যবহার করে:**
```php
<?php
$fruit = "Mango";

if ($fruit == "Apple") {
    echo "It's an Apple!";
} elseif ($fruit == "Mango") {
    echo "It's a Mango!";
} elseif ($fruit == "Banana") {
    echo "It's a Banana!";
} else {
    echo "Unknown fruit!";
}
?>
```

### **🟢 `switch` ব্যবহার করে (সহজ ও পরিষ্কার কোড):**
```php
<?php
$fruit = "Mango";

switch ($fruit) {
    case "Apple":
        echo "It's an Apple!";
        break;
    case "Mango":
        echo "It's a Mango!";
        break;
    case "Banana":
        echo "It's a Banana!";
        break;
    default:
        echo "Unknown fruit!";
}
?>
```
✅ **`switch` কোডটি সহজ এবং পরিষ্কার**!  

---

## **📌 4️⃣ `switch`-এ `break` না থাকলে কী হবে?**  
```php
<?php
$grade = "B";

switch ($grade) {
    case "A":
        echo "Excellent! ";
    case "B":
        echo "Very Good! ";
    case "C":
        echo "Good!";
        break;
    default:
        echo "Needs Improvement.";
}
?>
```
**আউটপুট:**  
```
Very Good! Good!
```
🔴 **সমস্যা:** `break` না থাকায় **B থেকে শুরু করে C পর্যন্ত সব case চলবে**।  

✅ **সঠিক কোড (break সহ)**:  
```php
<?php
$grade = "B";

switch ($grade) {
    case "A":
        echo "Excellent!";
        break;
    case "B":
        echo "Very Good!";
        break;
    case "C":
        echo "Good!";
        break;
    default:
        echo "Needs Improvement.";
}
?>
```
**সঠিক আউটপুট:**  
```
Very Good!
```

---

## **📌 5️⃣ `switch`-এর `default` ব্লক**  
যদি কোনো case না মিলে, তাহলে `default` ব্লক চালানো হয়।  

### **✅ উদাহরণ:**
```php
<?php
$color = "Purple";

switch ($color) {
    case "Red":
        echo "Color is Red.";
        break;
    case "Blue":
        echo "Color is Blue.";
        break;
    default:
        echo "Unknown Color.";
}
?>
```
**আউটপুট:**  
```
Unknown Color.
```
🔹 `$color = "Purple"` দেওয়ায় `default` ব্লক এক্সিকিউট হয়েছে।  

---

## **📌 6️⃣ `switch`-এ Multiple Case একসাথে ব্যবহার করা**  
```php
<?php
$day = "Sunday";

switch ($day) {
    case "Saturday":
    case "Sunday":
        echo "It's weekend!";
        break;
    default:
        echo "It's a weekday!";
}
?>
```
**আউটপুট:**  
```
It's weekend!
```
✅ **একই কাজের জন্য multiple case একসাথে ব্যবহার করা যায়।**  

---

## **📌 7️⃣ `switch` বনাম `match` (PHP 8+)**
PHP 8-এ **`match`** যুক্ত হয়েছে, যা `switch` থেকে আরও উন্নত এবং **strict comparison (===) ব্যবহার করে।**  

### **✅ `switch` উদাহরণ:**
```php
<?php
$grade = "B";

switch ($grade) {
    case "A":
        echo "Excellent!";
        break;
    case "B":
        echo "Very Good!";
        break;
    default:
        echo "Needs Improvement.";
}
?>
```

### **✅ `match` ব্যবহার করে (PHP 8+):**
```php
<?php
$grade = "B";

$result = match ($grade) {
    "A" => "Excellent!",
    "B" => "Very Good!",
    default => "Needs Improvement."
};

echo $result;
?>
```
✅ **`match` ছোট এবং সহজ!**  

---

## **🎯 অনুশীলনের জন্য চ্যালেঞ্জ 🚀**  
```php
<?php
$month = "March";

switch ($month) {
    case "December":
    case "January":
    case "February":
        echo "It's Winter!";
        break;
    case "March":
    case "April":
    case "May":
        echo "It's Spring!";
        break;
    default:
        echo "Unknown Season!";
}
?>
```
❓ **উপরের কোড রান করলে কী আউটপুট আসবে?** নিজে রান করে দেখো! 😃  

---

## **🚀 উপসংহার: আপনি এখন PHP-এর `switch` স্টেটমেন্ট শিখে ফেলেছেন! 🎉**  
✔ `switch` দিয়ে **একটি ভেরিয়েবলের একাধিক মান পরীক্ষা করা যায়।**  
✔ **`break` ব্যবহার না করলে সব case এক্সিকিউট হতে থাকে।**  
✔ `default` **ব্লক তখনই চলে যখন কোনো case না মিলে।**  
✔ PHP 8-এর `match` **আরও সহজ এবং স্ট্রিক্ট কম্পারিজন করে।**  

---

## **➡ পরবর্তী কি শিখতে চান?**  
✅ **PHP-এর Loops (for, while, foreach) শিখতে চান? 🚀**