# **PHP-এর Constants (কনস্ট্যান্ট) শেখা**  

PHP-তে **Constants** এমন ভ্যারিয়েবল, যেগুলোর **মান একবার সেট করার পর পরিবর্তন করা যায় না।** এগুলো সাধারণত **সিস্টেম বা প্রোগ্রামের নির্দিষ্ট মান সংরক্ষণ করতে ব্যবহৃত হয়।**  

---

## **📌 1️⃣ PHP-তে Constant কী?**
🔹 **Constant মান পরিবর্তন করা যায় না।**  
🔹 **Constant সবসময় GLOBAL SCOPE-এ থাকে।** (ফাংশন বা ক্লাসের ভিতরেও অ্যাক্সেস করা যায়)  
🔹 **Variable-এর আগে `$` চিহ্ন লাগে, কিন্তু Constant-এর জন্য `$` ব্যবহার করা হয় না।**  

---

## **📌 2️⃣ PHP-তে Constant ডিফাইন করার নিয়ম**  

PHP-তে **২টি উপায়ে Constant ডিফাইন করা যায়:**  
1️⃣ `define()` ফাংশন ব্যবহার করে  
2️⃣ `const` কীওয়ার্ড ব্যবহার করে  

---

## **🎯 ১. define() ফাংশন দিয়ে Constant তৈরি করা**  
🔹 `define()` ফাংশন ব্যবহার করে Constant তৈরি করতে হয়।  

🔹 **সিনট্যাক্স:**  
```php
define("CONSTANT_NAME", "value");
```

🔹 **উদাহরণ:**  
```php
<?php
define("SITE_NAME", "MyWebsite");
define("PI", 3.1416);

echo SITE_NAME; // Output: MyWebsite
echo "<br>";
echo PI; // Output: 3.1416
?>
```
✅ `define()` দিয়ে তৈরি করা Constant সব জায়গায় GLOBAL থাকে।  
❌ একবার সেট করলে মান পরিবর্তন করা যায় না।  

---

## **🎯 ২. const কীওয়ার্ড ব্যবহার করে Constant তৈরি করা**  
🔹 `const` কীওয়ার্ড শুধু **class ও function-এর বাইরে ব্যবহার করা যায়।**  

🔹 **সিনট্যাক্স:**  
```php
const CONSTANT_NAME = "value";
```

🔹 **উদাহরণ:**  
```php
<?php
const DB_NAME = "my_database";
const MAX_USERS = 100;

echo DB_NAME; // Output: my_database
echo "<br>";
echo MAX_USERS; // Output: 100
?>
```

✅ `const` দ্রুত কাজ করে কারণ এটি কম্পাইল টাইমে প্রসেস হয়।  
❌ `const` ফাংশনের ভিতরে ব্যবহার করা যায় না, শুধুমাত্র ক্লাস বা স্ক্রিপ্ট লেভেলে ব্যবহার করা যায়।  

---

## **📌 3️⃣ Constants ও Variables-এর পার্থক্য**  

| বৈশিষ্ট্য                | Constants (`define()` / `const`)                   | Variables (`$var`)                             |
| -------------------- | -------------------------------------------------- | ---------------------------------------------- |
| **প্রারম্ভিক চিহ্ন**       | `$` চিহ্ন থাকে না                                       | `$` চিহ্ন থাকে                                     |
| **মান পরিবর্তন করা যায়?** | ❌ পরিবর্তন করা যায় না                                   | ✅ পরিবর্তন করা যায়                                 |
| **Scope**            | সবসময় GLOBAL                                       | সাধারণত LOCAL, তবে `global` কীওয়ার্ড দিয়ে GLOBAL করা যায় |
| **ডিফাইন করার পদ্ধতি**    | `define("NAME", "VALUE")` বা `const NAME = "VALUE"` | `$name = "VALUE"`                              |

---

## **📌 4️⃣ Constant-এর Scope (গ্লোবাল ব্যবহারের সুবিধা)**  
🔹 Constant **সর্বদা GLOBAL থাকে**, তাই ফাংশনের ভিতরে `global` কীওয়ার্ড ব্যবহার ছাড়াই সরাসরি অ্যাক্সেস করা যায়।  

```php
<?php
define("APP_VERSION", "1.0.5");

function showVersion() {
    echo "Application Version: " . APP_VERSION;
}

showVersion(); // Output: Application Version: 1.0.5
?>
```
✅ **ফাংশনের ভিতরেও Constant সরাসরি ব্যবহার করা যায়।**  

---

## **📌 5️⃣ Magic Constants (Built-in Constants)**  
PHP-তে কিছু **Built-in Constants** আছে, যেগুলো **স্বয়ংক্রিয়ভাবে পরিবর্তিত হয়।** এগুলো **Magic Constants** নামে পরিচিত।  

| Constant       | ব্যাখ্যা                  |
| -------------- | --------------------- |
| `__LINE__`     | বর্তমান লাইনের নাম্বার দেয়    |
| `__FILE__`     | বর্তমান ফাইলের পূর্ণ পাথ দেখায় |
| `__DIR__`      | বর্তমান ডিরেক্টরির পাথ দেখায়   |
| `__FUNCTION__` | বর্তমান ফাংশনের নাম দেখায়     |
| `__CLASS__`    | বর্তমান ক্লাসের নাম দেখায়     |

🔹 **উদাহরণ:**  
```php
<?php
echo "Current Line: " . __LINE__ . "<br>";
echo "Current File: " . __FILE__ . "<br>";
echo "Current Directory: " . __DIR__ . "<br>";

function myFunction() {
    echo "Function Name: " . __FUNCTION__ . "<br>";
}

myFunction();
?>
```
🔹 **আউটপুট:**  
```
Current Line: 2  
Current File: /var/www/html/test.php  
Current Directory: /var/www/html  
Function Name: myFunction  
```

---

## **📌 6️⃣ Class-এর মধ্যে Constants ব্যবহার করা**  
🔹 **`const` ক্লাসের ভিতরে ব্যবহার করা যায়, কিন্তু `define()` ব্যবহার করা যায় না।**  

🔹 **উদাহরণ:**  
```php
<?php
class MathConstants {
    const PI = 3.1416;
    const E = 2.718;
    
    public function showConstants() {
        echo "PI: " . self::PI . "<br>";
        echo "Euler's Number: " . self::E . "<br>";
    }
}

$obj = new MathConstants();
$obj->showConstants();
?>
```
🔹 **আউটপুট:**  
```
PI: 3.1416  
Euler's Number: 2.718  
```
✅ `self::PI` দিয়ে ক্লাসের ভিতরে Constant অ্যাক্সেস করতে হয়।  
❌ `const` এর মান পরিবর্তন করা যায় না।  

---

## **📌 7️⃣ Case-Insensitive Constants (পুরনো পদ্ধতি)**  
PHP 7.3 থেকে case-insensitive constants `define()` দিয়ে তৈরি করা **deprecated** হয়েছে।  

```php
define("GREETING", "Hello, World!", true);
echo greeting; // Output: Hello, World!
```
❌ PHP 7.3+ থেকে এটি ব্যবহার না করার পরামর্শ দেওয়া হয়।  

---

## **🎯 উপসংহার (সংক্ষেপে পুনরালোচনা)**  
✔ `define("NAME", "VALUE")` বা `const NAME = "VALUE"` দিয়ে Constant তৈরি করা যায়।  
✔ Constant-এর মান পরিবর্তন করা যায় না।  
✔ Constant সবসময় GLOBAL SCOPE-এ থাকে।  
✔ `const` শুধুমাত্র ক্লাস বা স্ক্রিপ্ট লেভেলে ব্যবহার করা যায়।  
✔ `define()` ফাংশনের ভিতরে ব্যবহার করা যায়, কিন্তু `const` করা যায় না।  
✔ PHP-এর কিছু Built-in Magic Constants আছে যেমন `__LINE__`, `__FILE__`, `__FUNCTION__` ইত্যাদি।  

---

## **🎯 চ্যালেঞ্জ: নিজে চেষ্টা করো!**  
👉 নিচের কোডটি রান করলে কী হবে?  
```php
<?php
define("LANGUAGE", "PHP");
const VERSION = "8.2";

function testConstant() {
    echo LANGUAGE . " Version: " . VERSION;
}

testConstant();
?>
```
✅ নিজে কোড রান করে দেখো, পার্থক্য বুঝতে চেষ্টা করো! 😃  

---

## **পরবর্তী কী শিখতে চাও?**  
PHP-এর **Superglobals ($_GET, $_POST, $_SESSION, $_COOKIE)** শেখা লাগবে? 🚀