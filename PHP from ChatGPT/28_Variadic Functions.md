# 🚀 **PHP Variadic Functions বিস্তারিত শেখা** 🚀  

## 📌 **Variadic Function কী?**  
Variadic Function হলো এমন একটি ফাংশন, যেখানে **অজানা সংখ্যক আর্গুমেন্ট** পাস করা যায়। অর্থাৎ, কতগুলো প্যারামিটার পাঠানো হবে তা আগে থেকে নির্ধারণ করার দরকার নেই।  

✅ **PHP 5.6 থেকে** `func_get_args()`, `func_num_args()` ব্যবহার করে Variadic Function করা যেত।  
✅ **PHP 7.0+ এ** `...` (splat operator) ব্যবহার করে Variadic Function তৈরি করা যায়।  
✅ **PHP 8.0+ এ** Named Arguments সমর্থন করে।  

---

## 🔥 **PHP Variadic Function এর Syntax**
```php
function functionName(...$parameters) {
    // Function Body
}
```
- `...$parameters` = **Variadic Parameter** (অনেক আর্গুমেন্ট একসাথে নেয়)

---

## 🎯 **১. Variadic Function এর সহজ উদাহরণ**
```php
<?php
function sumNumbers(...$numbers) {
    return array_sum($numbers);
}

echo sumNumbers(10, 20, 30, 40); // 100
?>
```
✔️ **আউটপুট:**  
```
100
```
✅ `sumNumbers(10, 20, 30, 40)` → **সব সংখ্যা যোগ করে ১০০ দিয়েছে।**

---

## 🎯 **২. Multiple Parameters সহ Variadic Function**
```php
<?php
function introduce($name, ...$hobbies) {
    echo "Name: $name\n";
    echo "Hobbies: " . implode(", ", $hobbies) . "\n";
}

introduce("Rahim", "Reading", "Coding", "Gaming");
?>
```
✔️ **আউটপুট:**  
```
Name: Rahim
Hobbies: Reading, Coding, Gaming
```
✅ প্রথম প্যারামিটার **`$name`** ও বাকি সব প্যারামিটার **Variadic (`...$hobbies`)** হিসেবে নেয়া হয়েছে।

---

## 🎯 **৩. Variadic Function + Type Hinting (PHP 7+)**
```php
<?php
function multiply(int ...$numbers): int {
    return array_product($numbers);
}

echo multiply(2, 3, 4); // 24
?>
```
✔️ **আউটপুট:**  
```
24
```
✅ **সব আর্গুমেন্ট `int` হতে হবে।**  
✅ **`array_product()` সব সংখ্যা গুণ করে দেয়।**

---

## 🎯 **৪. Variadic Function + Default Parameter**
```php
<?php
function greet($greeting = "Hello", ...$names) {
    foreach ($names as $name) {
        echo "$greeting, $name!\n";
    }
}

greet("Hi", "Alice", "Bob", "Charlie");
?>
```
✔️ **আউটপুট:**  
```
Hi, Alice!
Hi, Bob!
Hi, Charlie!
```
✅ **প্রথম প্যারামিটার `greeting` ডিফল্ট `"Hello"` কিন্তু এখানে `"Hi"` পাঠানো হয়েছে।**  
✅ **বাকি সব `...$names` হিসাবে নেয়া হয়েছে।**

---

## 🎯 **৫. Variadic Function + Associative Array Argument**
```php
<?php
function studentDetails($name, ...$details) {
    echo "Student Name: $name\n";
    foreach ($details as $key => $value) {
        echo ucfirst($key) . ": $value\n";
    }
}

studentDetails("Arif", "age" => 20, "grade" => "A", "city" => "Dhaka");
?>
```
✔️ **আউটপুট:**  
```
Student Name: Arif
Age: 20
Grade: A
City: Dhaka
```
✅ **Associative Array (`key => value`) পাঠানো হয়েছে।**

---

## 🎯 **৬. Variadic Function + Named Arguments (PHP 8.0+)**
```php
<?php
function makePizza(string ...$ingredients) {
    echo "Your pizza has: " . implode(", ", $ingredients) . "\n";
}

makePizza(cheese: "Mozzarella", sauce: "Tomato", topping: "Mushrooms");
?>
```
✔️ **আউটপুট:**  
```
Your pizza has: Mozzarella, Tomato, Mushrooms
```
✅ **Named Arguments ব্যবহার করে Variadic Function-এ মান পাঠানো হয়েছে।**  
✅ **অর্ডার না মিললেও সঠিকভাবে কাজ করে!**

---

## 🎯 **৭. Variadic Function + Spread Operator (`...`)**
```php
<?php
function totalSum(...$numbers) {
    return array_sum($numbers);
}

$values = [5, 10, 15, 20];
echo totalSum(...$values);
?>
```
✔️ **আউটপুট:**  
```
50
```
✅ **Array কে `...` দিয়ে Spread করে আলাদা আর্গুমেন্ট হিসেবে পাঠানো হয়েছে।**

---

## 🎯 **৮. PHP 5.6 এর আগের Variadic Function (পুরনো পদ্ধতি)**
```php
<?php
function oldVariadicFunction() {
    $args = func_get_args();
    print_r($args);
}

oldVariadicFunction(1, 2, 3, "hello");
?>
```
✔️ **আউটপুট:**  
```
Array ( [0] => 1 [1] => 2 [2] => 3 [3] => hello )
```
✅ **`func_get_args()` ফাংশন সব আর্গুমেন্ট নিয়ে `array` দেয়।**  
✅ **নতুন `...` পদ্ধতি বেশি ব্যবহারযোগ্য!**

---

## 🚀 **Variadic Function কবে ব্যবহার করবেন?**
✅ **একই ধরনের অনেক মান ইনপুট দিতে হলে** (যেমন, `sum(10, 20, 30)`)  
✅ **কোনো নির্দিষ্ট সংখ্যক প্যারামিটার না থাকলে** (যেমন, `greet("Hi", "Alice", "Bob")`)  
✅ **ফাংশনের ভেতরে ডাইনামিকভাবে ডাটা প্রসেস করতে হলে**  

---

## ⚠️ **Variadic Function এর সমস্যা**
❌ **খুব বেশি আর্গুমেন্ট দিলে Memory Issue হতে পারে।**  
❌ **প্রথমে ঠিক করতে হবে কোন প্যারামিটার ফিক্সড আর কোনটা Variadic।**  

---

## 🚀 **সংক্ষেপে Variadic Function:**
✔️ `...$parameters` দিয়ে **Variadic Function** তৈরি করা যায়।  
✔️ **Type Hinting** ব্যবহার করা যায় (`int ...$numbers`)।  
✔️ **Spread Operator (`...`)** দিয়ে Array থেকে আর্গুমেন্ট পাঠানো যায়।  
✔️ **PHP 8.0+ এ Named Arguments সাপোর্ট করে।**  
✔️ **পুরনো `func_get_args()` বাদ দিয়ে নতুন `...` পদ্ধতি বেশি ভালো।**  

---

### 🎯 **Variadic Function বুঝতে সমস্যা হলে বলো! 😊**