# **🚀 PHP-এর Null Coalescing Operator (`??`) সম্পূর্ণ গাইড**  

**Null Coalescing Operator (`??`)** PHP 7-এ চালু হয় এবং এটি **Null চেক করার সহজতম উপায়**।  

---

## **📌 1️⃣ `??` Null Coalescing Operator কী?**  
এটি **দুইটি অপারেন্ড (ভেরিয়েবল) নিয়ে কাজ করে** এবং **প্রথম অপারেন্ড যদি `null` হয়, তাহলে দ্বিতীয় অপারেন্ড রিটার্ন করে**।  

🔹 এটি মূলত **ternary (`?:`) এবং `isset()` এর সহজ বিকল্প**।  

### **🎯 Syntax:**  
```php
$result = $var1 ?? $var2;
```
✔ যদি `$var1` সেট থাকে এবং `null` না হয়, তাহলে `$result = $var1` হবে।  
✔ যদি `$var1` `null` হয়, তাহলে `$result = $var2` হবে।  

---

## **📌 2️⃣ `??` ব্যবহার না করে `isset()` দিয়ে Null চেক করা**  
```php
<?php
$name = null;
$result = isset($name) ? $name : "Guest";
echo $result;
?>
```
✅ এখানে **`$name` `null` হওয়ায় `"Guest"` প্রিন্ট হবে।**  

---

## **📌 3️⃣ `??` দিয়ে সহজ উপায়ে Null চেক করা**  
```php
<?php
$name = null;
$result = $name ?? "Guest";
echo $result;
?>
```
**আউটপুট:**  
```
Guest
```
✅ **এটি আগের `isset()` ব্যবহার করার চেয়ে অনেক সংক্ষিপ্ত এবং সহজ।**  

---

## **📌 4️⃣ Multiple Variables চেক করা (Chaining `??`)**  
```php
<?php
$username = null;
$default_name = null;
$final_name = $username ?? $default_name ?? "Guest";

echo $final_name;
?>
```
**আউটপুট:**  
```
Guest
```
✔ **যদি প্রথম ভেরিয়েবল `null` হয়, তাহলে দ্বিতীয়টি চেক হবে।**  
✔ **দ্বিতীয়টিও `null` হলে, তৃতীয়টি ব্যবহৃত হবে।**  

---

## **📌 5️⃣ Array-এর Key চেক করা**  
```php
<?php
$user = ["name" => "Alice"];

$age = $user["age"] ?? "Age not available";
echo $age;
?>
```
**আউটপুট:**  
```
Age not available
```
✔ **`$user["age"]` key টি না থাকায় `"Age not available"` প্রিন্ট হয়েছে।**  

---

## **📌 6️⃣ Form Handling-এ `??` ব্যবহার**  
```php
<?php
$submitted_name = $_POST['name'] ?? "No name provided";
echo $submitted_name;
?>
```
✅ **যদি ইউজার `name` ইনপুট না দেয়, তাহলে `"No name provided"` দেখাবে।**  

---

## **📌 7️⃣ `??` বনাম Ternary Operator (`?:`)**  
#### **✅ Ternary Operator (`?:`)**
```php
<?php
$name = null;
$result = $name ?: "Guest";
echo $result;
?>
```
**আউটপুট:**  
```
Guest
```
✔ এটি কাজ করে, কিন্তু `false` এবং `0` কে `null` এর মতো বিবেচনা করে।  

#### **✅ `??` Null Coalescing Operator**
```php
<?php
$name = 0;
$result = $name ?? "Guest";
echo $result;
?>
```
**আউটপুট:**  
```
0
```
✔ **`??` শুধুমাত্র `null` চেক করে, `false` বা `0` চেক করে না।**  

---

## **🎯 অনুশীলনের জন্য চ্যালেঞ্জ 🚀**  
```php
<?php
$user_data = ["email" => "user@example.com"];
$username = $user_data["username"] ?? "No username";
echo $username;
?>
```
❓ **উপরের কোড রান করলে কী আউটপুট আসবে?** নিজে রান করে দেখো! 😃  

---

## **🚀 উপসংহার: আপনি এখন PHP-এর `??` Null Coalescing Operator শিখে ফেলেছেন! 🎉**  
✔ এটি **`isset()` এর সহজ বিকল্প**।  
✔ **`null` হলে ডিফল্ট মান সেট করতে ব্যবহৃত হয়।**  
✔ **Multiple variables চেক করা যায় (Chaining `??`)।**  
✔ **Ternary (`?:`) এর চেয়ে নিরাপদ কারণ এটি `0` বা `false` চেক করে না।**  

---

## **➡ পরবর্তী কি শিখতে চান?**  
✅ **PHP-এর Logical Operators (&&, ||, !) শিখতে চান? 🚀**