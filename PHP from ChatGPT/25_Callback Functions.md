# 🔥 **PHP Callback Functions বিস্তারিত শেখা** 🔥  

**Callback Function** হলো **একটি ফাংশন যা অন্য ফাংশনের আর্গুমেন্ট হিসেবে পাঠানো হয় এবং পরে সেই ফাংশন থেকে কল করা হয়**।  

➡️ PHP-তে Callback Function মূলত **Higher-Order Functions** এর সাথে ব্যবহার করা হয়, যেমন **`array_map()`**, **`array_filter()`**, **`usort()`** ইত্যাদির সাথে।  

➡️ PHP-তে Callback Function তৈরি করা যায় **নরমাল ফাংশন, অ্যানোনিমাস ফাংশন, এবং অ্যারো ফাংশন ব্যবহার করে**।  

---

## 🔹 **1. Callback Function কিভাবে কাজ করে?**
👉 **একটি সাধারণ উদাহরণ যেখানে একটি ফাংশন অন্য ফাংশনকে আর্গুমেন্ট হিসেবে নেয়:**  

```php
<?php
function myFunction($name) {
    return "Hello, $name!";
}

function callFunction($callback, $name) {
    return $callback($name); // এখানে কলব্যাক ফাংশন কল করা হচ্ছে
}

echo callFunction("myFunction", "John");
?>
```
✔️ **আউটপুট:**  
```
Hello, John!
```
✅ **কিভাবে কাজ করলো?**  
1. **`myFunction()`** ফাংশন `name` নেয় এবং একটি **"Hello, Name!"** রিটার্ন করে।  
2. **`callFunction()`** ফাংশনের মধ্যে **callback ফাংশন (myFunction)** পাঠানো হয়।  
3. `callFunction()` **ডাইনামিকভাবে কলব্যাক ফাংশন এক্সিকিউট করে**।  

---

## 🔹 **2. Anonymous Function দিয়ে Callback Function**
👉 Anonymous Function বা **Closure** ব্যবহার করে Callback Function করা যায়:  

```php
<?php
function callFunction($callback) {
    echo $callback("John");
}

callFunction(function($name) {
    return "Hello, $name!";
});
?>
```
✔️ **আউটপুট:**  
```
Hello, John!
```
✅ **এখানে `myFunction()` ব্যবহার না করেই সরাসরি Anonymous Function পাস করা হয়েছে!**  

---

## 🔹 **3. PHP-তে Built-in Callback Function ব্যবহার**
PHP-তে অনেক বিল্ট-ইন ফাংশন আছে যেখানে Callback Function ব্যবহার করা যায়।

### ✅ **`array_map()` দিয়ে প্রতিটি এলিমেন্টে Callback Function প্রয়োগ**
```php
<?php
$numbers = [1, 2, 3, 4];

$squaredNumbers = array_map(function($num) {
    return $num * $num;
}, $numbers);

print_r($squaredNumbers);
?>
```
✔️ **আউটপুট:**  
```
Array ( [0] => 1 [1] => 4 [2] => 9 [3] => 16 )
```
✅ **`array_map()` প্রতিটি এলিমেন্টের উপর `callback function` প্রয়োগ করেছে।**  

---

### ✅ **`array_filter()` দিয়ে নির্দিষ্ট শর্তে ফিল্টার করা**
```php
<?php
$numbers = [1, 2, 3, 4, 5, 6];

$evenNumbers = array_filter($numbers, function($num) {
    return $num % 2 == 0; // শুধু জোড় সংখ্যা রাখবে
});

print_r($evenNumbers);
?>
```
✔️ **আউটপুট:**  
```
Array ( [1] => 2 [3] => 4 [5] => 6 )
```
✅ **`array_filter()` শুধুমাত্র জোড় সংখ্যা ফিল্টার করেছে।**  

---

### ✅ **`usort()` দিয়ে অ্যারে সর্ট করা Callback Function দিয়ে**
```php
<?php
$people = [
    ["name" => "Alice", "age" => 25],
    ["name" => "Bob", "age" => 22],
    ["name" => "Charlie", "age" => 30]
];

usort($people, function($a, $b) {
    return $a['age'] - $b['age']; // বয়স অনুযায়ী সর্ট করা হচ্ছে
});

print_r($people);
?>
```
✔️ **আউটপুট:**  
```
Array (
    [0] => Array ( [name] => Bob [age] => 22 )
    [1] => Array ( [name] => Alice [age] => 25 )
    [2] => Array ( [name] => Charlie [age] => 30 )
)
```
✅ **`usort()` ব্যবহার করে বয়স অনুযায়ী সাজানো হয়েছে।**  

---

## 🔹 **4. ক্লাসের মধ্যে Callback Function (`call_user_func()`)**
👉 **PHP-তে `call_user_func()` দিয়ে Callback Function কল করা যায়।**  
```php
<?php
class MyClass {
    public function sayHello($name) {
        return "Hello, $name!";
    }
}

$obj = new MyClass();

echo call_user_func([$obj, "sayHello"], "John");
?>
```
✔️ **আউটপুট:**  
```
Hello, John!
```
✅ **এখানে `call_user_func()` ব্যবহার করে ক্লাসের মেথডকে কল করা হয়েছে।**  

---

## 🔹 **5. Static Method কে Callback Function হিসেবে পাঠানো**
```php
<?php
class MathOperations {
    public static function square($num) {
        return $num * $num;
    }
}

$numbers = [1, 2, 3, 4];
$squaredNumbers = array_map(['MathOperations', 'square'], $numbers);

print_r($squaredNumbers);
?>
```
✔️ **আউটপুট:**  
```
Array ( [0] => 1 [1] => 4 [2] => 9 [3] => 16 )
```
✅ **Static Method `square()` কে `array_map()` এর মধ্যে Callback Function হিসেবে ব্যবহার করা হয়েছে।**  

---

## 🎯 **সংক্ষেপে গুরুত্বপূর্ণ বিষয়গুলো**
| বিষয়                                  | বর্ণনা                                      |
| ------------------------------------ | ----------------------------------------- |
| **নরমাল Callback Function**           | `functionName` দিয়ে সরাসরি পাঠানো হয়             |
| **Anonymous Callback Function**      | `function() {}` ব্যবহার করে ইনলাইন ফাংশন পাঠানো হয় |
| **`array_map()` ব্যবহার**              | প্রতিটি এলিমেন্টে ফাংশন প্রয়োগ করতে                   |
| **`array_filter()` ব্যবহার**           | শর্ত অনুযায়ী কিছু এলিমেন্ট ফিল্টার করতে                |
| **`usort()` ব্যবহার**                  | অ্যারে সর্ট করতে                               |
| **`call_user_func()` ব্যবহার**         | ক্লাসের মেথডকে ডাইনামিকভাবে কল করতে                  |
| **Static Method কে Callback হিসেবে পাঠানো** | `['ClassName', 'MethodName']` ব্যবহার করে    |

---

## 🚀 **শেষ কথা**
PHP-তে **Callback Function খুবই গুরুত্বপূর্ণ** কারণ এটি **ডাইনামিক ফাংশন এক্সিকিউশন**, **কোড রিইউজেবল করা**, এবং **Higher-Order Functions** তৈরি করতে সাহায্য করে।  

✅ **আপনি যদি Callback Function নিয়ে আরও জানতে চান, তাহলে বলুন! 😊**