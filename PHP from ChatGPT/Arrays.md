# **PHP-এর Arrays সম্পূর্ণ গাইড** 🚀  

PHP-তে **Array** হলো একধরনের ডাটা স্ট্রাকচার যেখানে **একাধিক মান একটি ভেরিয়েবলে সংরক্ষণ করা যায়।** এটি **একই ধরনের ডাটা একসাথে সংরক্ষণ ও ব্যবস্থাপনা করতে সাহায্য করে।**  

---

## **📌 1️⃣ PHP-তে Array কী?**
🔹 Array হল **একাধিক মান** (values) সংরক্ষণের জন্য **একটি ভেরিয়েবল।**  
🔹 Array-তে **index (কী)** ব্যবহার করে **প্রতিটি মান অ্যাক্সেস করা হয়।**  
🔹 PHP-তে **৩ প্রকারের অ্যারে** আছে:  
1️⃣ **Indexed Array** (সংখ্যা দ্বারা ইন্ডেক্সড)  
2️⃣ **Associative Array** (কী-ভিত্তিক ইন্ডেক্সড)  
3️⃣ **Multidimensional Array** (নেস্টেড অ্যারে)  

---

## **📌 2️⃣ PHP-তে Indexed Array (সাধারণ অ্যারে)**  
Indexed Array-তে **সংখ্যা (0, 1, 2, ...) ব্যবহার করে মানগুলো সংরক্ষণ ও অ্যাক্সেস করা হয়।**  

### **🎯 Indexed Array তৈরি করার ২টি উপায়:**  
```php
<?php
// ১. array() ফাংশন ব্যবহার করে
$fruits = array("Apple", "Banana", "Mango");

// ২. সরাসরি [] ব্যবহার করে (PHP 5.4+)
$colors = ["Red", "Green", "Blue"];

echo $fruits[0];  // Output: Apple
echo "<br>";
echo $colors[1];  // Output: Green
?>
```
✅ **Index সর্বদা 0 থেকে শুরু হয়।**  

---

## **📌 3️⃣ Indexed Array লুপ করে দেখানো (foreach, for loop)**  
```php
<?php
$numbers = [10, 20, 30, 40, 50];

// for loop ব্যবহার করে
for ($i = 0; $i < count($numbers); $i++) {
    echo "Index $i: " . $numbers[$i] . "<br>";
}

// foreach loop ব্যবহার করে
foreach ($numbers as $num) {
    echo "Value: $num <br>";
}
?>
```

---

## **📌 4️⃣ Associative Array (Key-Value অ্যারে)**  
🔹 Indexed Array-তে সংখ্যার বদলে **নিজের পছন্দমতো কী (key)** ব্যবহার করা যায়।  

### **🎯 Associative Array তৈরি করার ২টি উপায়:**  
```php
<?php
// array() ফাংশন ব্যবহার করে
$student = array("name" => "Rahim", "age" => 22, "city" => "Dhaka");

// সরাসরি [] ব্যবহার করে (PHP 5.4+)
$person = ["name" => "Karim", "age" => 25, "city" => "Chittagong"];

echo $student["name"];  // Output: Rahim
echo "<br>";
echo $person["city"];   // Output: Chittagong
?>
```

---

## **📌 5️⃣ Associative Array লুপ করে দেখানো (foreach loop)**  
```php
<?php
$student = ["name" => "Rahim", "age" => 22, "city" => "Dhaka"];

foreach ($student as $key => $value) {
    echo "$key : $value <br>";
}
?>
```
🔹 **foreach লুপ ব্যবহার করলে key এবং value একসাথে পাওয়া যায়।**  

---

## **📌 6️⃣ Multidimensional Array (একাধিক স্তরের অ্যারে)**  
🔹 **Multidimensional Array** হচ্ছে **অ্যারের ভিতরে অ্যারে।**  
🔹 এটি **টেবিল বা ম্যাট্রিক্স ডাটা সংরক্ষণে উপযোগী।**  

### **🎯 ২D Multidimensional Array তৈরি করা**  
```php
<?php
$students = [
    ["Rahim", 22, "Dhaka"],
    ["Karim", 25, "Chittagong"],
    ["Jamal", 20, "Rajshahi"]
];

echo "Name: " . $students[0][0] . ", Age: " . $students[0][1] . ", City: " . $students[0][2];
?>
```
🔹 **$students[0][0] → "Rahim"**  
🔹 **$students[1][2] → "Chittagong"**  

---

## **📌 7️⃣ Multidimensional Array লুপ করে দেখানো (Nested foreach loop)**  
```php
<?php
$students = [
    ["name" => "Rahim", "age" => 22, "city" => "Dhaka"],
    ["name" => "Karim", "age" => 25, "city" => "Chittagong"],
    ["name" => "Jamal", "age" => 20, "city" => "Rajshahi"]
];

foreach ($students as $student) {
    foreach ($student as $key => $value) {
        echo "$key : $value | ";
    }
    echo "<br>";
}
?>
```
🔹 **এতে প্রতিটি উপাদান সুন্দরভাবে প্রিন্ট হবে।**  

---

## **📌 8️⃣ PHP-এর কিছু গুরুত্বপূর্ণ Array ফাংশন**  

| ফাংশন                             | কাজ                         |
| ------------------------------- | -------------------------- |
| `count($array)`                 | অ্যারের এলিমেন্ট সংখ্যা গণনা করে      |
| `array_push($array, $value)`    | অ্যারের শেষে নতুন মান যোগ করে       |
| `array_pop($array)`             | অ্যারের শেষ উপাদান মুছে ফেলে         |
| `array_shift($array)`           | অ্যারের প্রথম উপাদান সরিয়ে ফেলে      |
| `array_unshift($array, $value)` | অ্যারের শুরুতে নতুন মান যোগ করে      |
| `array_merge($arr1, $arr2)`     | দুটি অ্যারে একত্রিত করে            |
| `array_keys($array)`            | অ্যারের সকল কী রিটার্ন করে         |
| `array_values($array)`          | অ্যারের সকল মান রিটার্ন করে        |
| `in_array($value, $array)`      | নির্দিষ্ট মান অ্যারেতে আছে কিনা যাচাই করে |
| `array_search($value, $array)`  | নির্দিষ্ট মানের ইনডেক্স খুঁজে দেয়      |

### **🎯 উদাহরণ**  
```php
<?php
$numbers = [10, 20, 30, 40, 50];

echo count($numbers); // Output: 5
echo "<br>";

array_push($numbers, 60);
print_r($numbers); // Output: [10, 20, 30, 40, 50, 60]
?>
```

---

## **🎯 উপসংহার (সংক্ষেপে পুনরালোচনা)**  
✔ PHP-তে **Indexed, Associative ও Multidimensional** অ্যারে আছে।  
✔ **Indexed Array** সংখ্যায় ইনডেক্সড হয়।  
✔ **Associative Array** কী-ভিত্তিক ইনডেক্সড হয়।  
✔ **Multidimensional Array** একাধিক স্তরের ডাটা সংরক্ষণ করতে পারে।  
✔ `foreach` লুপ ব্যবহার করে সহজে অ্যারে লুপ করা যায়।  
✔ PHP-এর অনেক **Array ফাংশন** আছে, যা অ্যারে পরিচালনা করতে সাহায্য করে।  

---

## **🚀 চ্যালেঞ্জ: নিজে চেষ্টা করো!**  
👉 নিচের কোডের আউটপুট কী হবে?  
```php
<?php
$numbers = [5, 10, 15, 20];
array_shift($numbers);
array_push($numbers, 25);
print_r($numbers);
?>
```
✅ **নিজে রান করে দেখো!** 😃  

---

## **পরবর্তী কী শিখতে চাও?**  
PHP-এর **Superglobals ($_GET, $_POST, $_SESSION, $_COOKIE)** শেখা লাগবে? 🚀