PHP-তে **ডাটা টাইপ** (Data Types) খুব গুরুত্বপূর্ণ কারণ এটি নির্ধারণ করে যে কোন ধরণের মান একটি ভেরিয়েবল ধারণ করতে পারে। PHP হল **loosely typed language**, অর্থাৎ ভেরিয়েবলের ডাটা টাইপ সরাসরি নির্দিষ্ট করতে হয় না, বরং PHP স্বয়ংক্রিয়ভাবে সেট করে।

---

## **PHP-এর ৮টি প্রধান Data Type**
PHP-তে ডাটা টাইপ দুই ভাগে ভাগ করা যায়:
1. **Scalar Types (একক মান)**  
   - **(1) String** – টেক্সট ডাটা  
   - **(2) Integer** – পূর্ণসংখ্যা  
   - **(3) Float (Double)** – দশমিক সংখ্যা  
   - **(4) Boolean** – সত্য বা মিথ্যা  

2. **Compound Types (একাধিক মান ধারণ করতে পারে)**  
   - **(5) Array** – একাধিক মান সংরক্ষণ করে  
   - **(6) Object** – অবজেক্ট-ভিত্তিক ডাটা  

3. **Special Types (বিশেষ ধরণের মান)**  
   - **(7) NULL** – কোনো মান নেই  
   - **(8) Resource** – এক্সটার্নাল রিসোর্স (যেমন: ডাটাবেস কানেকশন)  

---

## **১. String (স্ট্রিং)**
স্ট্রিং হল **টেক্সট ডাটা** যা **ডাবল কোটেশন ("") বা সিঙ্গেল কোটেশন ('')** এর মধ্যে থাকে।  

🔹 **উদাহরণ:**  
```php
<?php
$name = "Hello, World!";
echo $name; // Output: Hello, World!
?>
```

✅ ডাবল কোটেশন ব্যবহার করলে **ভেরিয়েবল interpolation** করা যায়:  
```php
<?php
$place = "Dhaka";
echo "I live in $place"; // Output: I live in Dhaka
?>
```
❌ সিঙ্গেল কোটেশনে **ভেরিয়েবল কাজ করবে না**:  
```php
<?php
$place = "Dhaka";
echo 'I live in $place'; // Output: I live in $place (ভেরিয়েবল প্রিন্ট হবে না)
?>
```

---

## **২. Integer (পূর্ণসংখ্যা)**
Integer হল **বিন্দু ছাড়া সংখ্যা** যা ধনাত্মক বা ঋণাত্মক হতে পারে।  

🔹 **উদাহরণ:**  
```php
<?php
$num1 = 100;
$num2 = -50;
echo $num1 + $num2; // Output: 50
?>
```
🔹 **Integer-এর শর্তসমূহ:**  
- দশমিক বিন্দু থাকা যাবে না  
- ধনাত্মক বা ঋণাত্মক হতে পারে  
- PHP_INT_MAX এবং PHP_INT_MIN দিয়ে সর্বোচ্চ ও সর্বনিম্ন মান পাওয়া যায়  

```php
<?php
echo PHP_INT_MAX; // সর্বোচ্চ ইন্টিজার মান দেখাবে
?>
```

---

## **৩. Float (Double)**
Float বা **Double** হল **দশমিক সংখ্যা**।  

🔹 **উদাহরণ:**  
```php
<?php
$price = 10.99;
$pi = 3.1416;
echo $price + $pi; // Output: 14.1316
?>
```

✅ **Scientific Notation সমর্থন করে**  
```php
<?php
$bigNum = 1.2e3; // 1.2 × 10^3 = 1200
echo $bigNum; // Output: 1200
?>
```

---

## **৪. Boolean (বুলিয়ান)**
Boolean এর মান **true বা false** হতে পারে। এটি সাধারণত শর্তমূলক লজিক ও কন্ডিশনে ব্যবহার হয়।  

🔹 **উদাহরণ:**  
```php
<?php
$isPHPFun = true;
$hasError = false;

if ($isPHPFun) {
    echo "PHP is fun!"; // Output: PHP is fun!
}
?>
```
✅ **বুলিয়ান ভ্যালু চেক করার সময় PHP স্বয়ংক্রিয়ভাবে কিছু ভ্যালুকে `false` হিসেবে ধরে:**  
| **Falsy Values** | **Description** |
| ---------------- | --------------- |
| `0`              | সংখ্যা 0           |
| `0.0`            | দশমিক সংখ্যা 0.0    |
| `""`             | খালি স্ট্রিং          |
| `"0"`            | স্ট্রিং "0"         |
| `NULL`           | শূন্য মান          |
| `[]`             | খালি অ্যারে          |

---

## **৫. Array (অ্যারে)**
Array একাধিক মান একসাথে সংরক্ষণ করতে ব্যবহার করা হয়।  

🔹 **Indexed Array (সাধারণ অ্যারে)**  
```php
<?php
$fruits = ["Apple", "Banana", "Mango"];
echo $fruits[0]; // Output: Apple
?>
```

🔹 **Associative Array (Key-Value Pair)**  
```php
<?php
$person = ["name" => "Alice", "age" => 25];
echo $person["name"]; // Output: Alice
?>
```

🔹 **Multidimensional Array (নেস্টেড অ্যারে)**  
```php
<?php
$students = [
    ["Alice", 25],
    ["Bob", 30]
];
echo $students[1][0]; // Output: Bob
?>
```

---

## **৬. Object (অবজেক্ট)**
Object হল একটি ক্লাসের ইনস্ট্যান্স যা **প্রোপার্টি (ভেরিয়েবল) ও মেথড (ফাংশন)** ধারণ করতে পারে।  

🔹 **উদাহরণ:**  
```php
<?php
class Car {
    public $brand;
    
    function setBrand($brand) {
        $this->brand = $brand;
    }
    
    function getBrand() {
        return $this->brand;
    }
}

$myCar = new Car();
$myCar->setBrand("Toyota");
echo $myCar->getBrand(); // Output: Toyota
?>
```

---

## **৭. NULL (নাল)**
`NULL` হল একটি বিশেষ ডাটা টাইপ যা বোঝায় **কোনো ভেরিয়েবলে মান নেই**।  

🔹 **উদাহরণ:**  
```php
<?php
$var = NULL;
echo $var; // Output: কিছুই দেখাবে না
?>
```
🔹 **ভেরিয়েবল unset করলে সেটি NULL হয়ে যায়:**  
```php
<?php
$var = "Hello";
unset($var); // ভেরিয়েবল মুছে ফেলা হয়
var_dump($var); // Output: NULL
?>
```

---

## **৮. Resource (রিসোর্স)**
Resource ডাটা টাইপ **এক্সটার্নাল রিসোর্স (যেমন: ডাটাবেস, ফাইল, ইমেজ প্রসেসিং)** এর জন্য ব্যবহৃত হয়।  

🔹 **উদাহরণ (ডাটাবেস কানেকশন)**  
```php
<?php
$conn = mysqli_connect("localhost", "root", "", "testDB");
var_dump($conn); // এটি একটি resource টাইপ রিটার্ন করবে
?>
```
👉 Resource সাধারণত ফাংশনের মাধ্যমে তৈরি হয় এবং PHP নিজে থেকেই এগুলো ম্যানেজ করে।

---

## **PHP-তে Data Type চেক করার উপায়**
PHP-তে `var_dump()` ফাংশন ব্যবহার করে কোনো ভেরিয়েবলের ডাটা টাইপ চেক করা যায়।  

🔹 **উদাহরণ:**  
```php
<?php
$var = 10.5;
var_dump($var); // Output: float(10.5)
?>
```

🔹 **`gettype()` ফাংশনও ব্যবহার করা যায়:**  
```php
<?php
$var = "Hello";
echo gettype($var); // Output: string
?>
```

---

## **শেষ কথা**
✅ PHP-তে **৮টি ডাটা টাইপ** আছে  
✅ PHP **loosely typed language**, তাই টাইপ নির্ধারণ করতে হয় না  
✅ `var_dump()` ব্যবহার করে টাইপ চেক করা যায়  

---

### **পরবর্তী কী শিখতে চাও?**
👉 PHP-এর Type Casting, Type Juggling, অথবা অন্য কোনো বিষয় বুঝতে চাইলে বলো! 😊