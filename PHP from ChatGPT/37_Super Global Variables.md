PHP-তে `$_GET`, `$_POST`, `$_REQUEST`, এবং `$_SERVER` সুপারগ্লোবাল ভেরিয়েবল ব্যবহার করে HTTP request এবং সার্ভারের তথ্য ম্যানেজ করা হয়। এগুলো নিয়ে বিস্তারিত আলোচনা করছি।  

---

## 1️⃣ **PHP `$_GET` – GET Request Handling**
**কাজ:**  
🔹 `$_GET` ব্যবহার করে URL-এর query parameters (ডাটা) রিসিভ করা হয়।  
🔹 সাধারণত সার্চ অপারেশন, লিঙ্কের মাধ্যমে ডাটা পাঠানো ইত্যাদির জন্য ব্যবহৃত হয়।  

### **উদাহরণ:**  
```php
// URL: example.com/index.php?name=Hasan&age=25
$name = $_GET['name'];
$age = $_GET['age'];

echo "Name: $name, Age: $age";
```
### **ব্যাখ্যা:**  
- URL-এর `?name=Hasan&age=25` অংশে `name` এবং `age` নামে দুইটি কুইরি প্যারামিটার পাঠানো হয়েছে।  
- `$_GET['name']` দিয়ে `name` এর মান `Hasan` পাওয়া যাবে।  
- `$_GET['age']` দিয়ে `age` এর মান `25` পাওয়া যাবে।  

---

## 2️⃣ **PHP `$_POST` – POST Request Handling**
**কাজ:**  
🔹 `$_POST` ব্যবহার করে ফর্ম ডাটা বা অন্য কোন তথ্য সার্ভারে পাঠানো হয়।  
🔹 এটি URL-এ ডাটা না দেখিয়ে গোপনে পাঠায়, তাই সংবেদনশীল ডাটা পাঠাতে ব্যবহৃত হয় (যেমন পাসওয়ার্ড)।  

### **উদাহরণ:**  

#### **HTML Form (`form.html`)**
```html
<form action="submit.php" method="post">
    Name: <input type="text" name="name">
    <button type="submit">Submit</button>
</form>
```
#### **PHP File (`submit.php`)**
```php
$name = $_POST['name'];
echo "Hello, $name!";
```
### **ব্যাখ্যা:**  
- ইউজার যখন ফর্মে `name` ইনপুট দিয়ে সাবমিট করবে, তখন `submit.php`-তে `$_POST['name']` দ্বারা সেই ডাটা রিসিভ করা যাবে।  

---

## 3️⃣ **PHP `$_REQUEST` – GET ও POST উভয় Handle করা**
**কাজ:**  
🔹 `$_REQUEST` ব্যবহার করে `GET` এবং `POST` উভয় মেথডের ডাটা একসাথে পাওয়া যায়।  
🔹 তবে এটি সিকিউরিটি ঝুঁকি তৈরি করতে পারে, তাই নির্দিষ্ট মেথড ব্যবহার করাই ভালো।  

### **উদাহরণ:**
```php
$name = $_REQUEST['name'];
echo "Hello, $name!";
```
- এটি `GET` বা `POST` যেকোনো পদ্ধতিতে পাঠানো `name` প্যারামিটার গ্রহণ করবে।  

---

## 4️⃣ **PHP `$_SERVER` – সার্ভার সম্পর্কিত তথ্য পাওয়া**
**কাজ:**  
🔹 `$_SERVER` সুপারগ্লোবাল ভেরিয়েবল ব্যবহার করে সার্ভারের বিভিন্ন তথ্য সংগ্রহ করা যায়।  
🔹 এটি সাধারণত সার্ভার এনভায়রনমেন্ট ও ক্লায়েন্ট সংক্রান্ত তথ্যের জন্য ব্যবহৃত হয়।  

### **উদাহরণ:**  

#### **(1) রিকুয়েস্ট মেথড চেক করা**
```php
echo $_SERVER['REQUEST_METHOD']; // "GET" বা "POST" দেখাবে
```
- এটি দেখাবে ক্লায়েন্ট কোন HTTP মেথড ব্যবহার করছে (`GET`, `POST` ইত্যাদি)।

#### **(2) ইউজারের IP Address বের করা**
```php
echo $_SERVER['REMOTE_ADDR'];
```
- এটি ইউজারের IP Address রিটার্ন করবে।  

#### **(3) বর্তমান স্ক্রিপ্টের ফাইল পাথ দেখা**
```php
echo $_SERVER['SCRIPT_FILENAME'];
```
- এটি বর্তমানে চলমান PHP স্ক্রিপ্টের ফাইল লোকেশন দেখাবে।

#### **(4) ব্রাউজার User-Agent চেক করা**
```php
echo $_SERVER['HTTP_USER_AGENT'];
```
- এটি ইউজারের ব্রাউজার ও ডিভাইস সংক্রান্ত তথ্য দেবে।  

#### **(5) বর্তমান পেজের URL পাওয়া**
```php
echo $_SERVER['REQUEST_URI'];
```
- এটি বর্তমান পেজের URI (URL-এর অংশ) দেবে।  

---

## **সংক্ষেপে পার্থক্য:**
| Superglobal | কাজ                                       |
| ----------- | ---------------------------------------- |
| `$_GET`     | URL থেকে ডাটা নেয়                             |
| `$_POST`    | ফর্ম বা অন্য মাধ্যম থেকে ডাটা নেয়                  |
| `$_REQUEST` | GET ও POST উভয়ই গ্রহণ করে (কম ব্যবহার করা ভালো) |
| `$_SERVER`  | সার্ভার ও ক্লায়েন্ট সংক্রান্ত তথ্য দেয়                |

---

### **🎯 গুরুত্বপূর্ণ টিপস**
✅ **`$_GET` ব্যবহার করুন যখন URL-এর মাধ্যমে ডাটা পাঠাতে হয়।**  
✅ **`$_POST` ব্যবহার করুন সংবেদনশীল ডাটা বা বড় ডাটা পাঠানোর জন্য।**  
✅ **`$_SERVER` ব্যবহার করুন সার্ভারের তথ্য জানতে বা কাস্টম রিকুয়েস্ট হ্যান্ডল করতে।**  
✅ **`$_REQUEST` ব্যবহার না করাই ভালো, কারণ এটি নিরাপত্তার জন্য ঝুঁকিপূর্ণ হতে পারে।**  

এখন তুমি চাইলে এগুলোর সাথে সিকিউরিটি, AJAX, এবং API কিভাবে ব্যবহার করা যায় সেটাও জানতে পারো। 🚀