# **PHP-এর Indexed Arrays সম্পূর্ণ গাইড** 🚀  

PHP-তে **Indexed Array** হলো **একটি সাধারণ অ্যারে** যেখানে প্রতিটি উপাদান (element) **সংখ্যাসূচক ইনডেক্স (numeric index)** দ্বারা সংরক্ষিত ও অ্যাক্সেস করা হয়।  

---

## **📌 1️⃣ Indexed Array তৈরি করার ২টি উপায়**  

🔹 **Indexed Array-তে প্রতিটি উপাদানের একটি স্বয়ংক্রিয় সংখ্যা ইনডেক্স থাকে।**  
🔹 **ইনডেক্স সংখ্যা 0 থেকে শুরু হয়।**  

### **🎯 পদ্ধতি ১: `array()` ফাংশন ব্যবহার করে Indexed Array তৈরি করা**  
```php
<?php
$fruits = array("Apple", "Banana", "Mango");

echo $fruits[0]; // Output: Apple
?>
```

### **🎯 পদ্ধতি ২: সরাসরি `[]` ব্যবহার করে Indexed Array তৈরি করা (PHP 5.4+)**  
```php
<?php
$colors = ["Red", "Green", "Blue"];

echo $colors[1]; // Output: Green
?>
```
✅ **উপরের দুই পদ্ধতিতেই Indexed Array তৈরি করা যায়।**  

---

## **📌 2️⃣ Indexed Array-এর উপাদান পরিবর্তন করা**  
Indexed Array-এর **কোনো নির্দিষ্ট উপাদান পরিবর্তন করতে চাইলে, তার ইনডেক্স ব্যবহার করে মান সেট করা যায়।**  
```php
<?php
$fruits = ["Apple", "Banana", "Mango"];
$fruits[1] = "Orange"; // Banana এর পরিবর্তে Orange সেট করা হলো

echo $fruits[1]; // Output: Orange
?>
```
✅ **Indexed Array-এর মান পরিবর্তন করা সম্ভব।**  

---

## **📌 3️⃣ Indexed Array-এর উপাদান যোগ করা (Add Elements)**  
### **🎯 `array_push()` ফাংশন ব্যবহার করে নতুন উপাদান যোগ করা:**  
```php
<?php
$animals = ["Cat", "Dog"];
array_push($animals, "Elephant", "Tiger");

print_r($animals);
// Output: Array ( [0] => Cat [1] => Dog [2] => Elephant [3] => Tiger )
?>
```
✅ **`array_push()` ব্যবহার করে একাধিক মান যোগ করা যায়।**  

### **🎯 সরাসরি নতুন মান অ্যাসাইন করেও উপাদান যোগ করা যায়:**  
```php
<?php
$numbers = [10, 20, 30];
$numbers[] = 40; // নতুন মান যোগ করা

print_r($numbers);
// Output: Array ( [0] => 10 [1] => 20 [2] => 30 [3] => 40 )
?>
```
✅ **`$array[] = value;` ব্যবহার করেও নতুন উপাদান যোগ করা যায়।**  

---

## **📌 4️⃣ Indexed Array-এর উপাদান মুছে ফেলা (Remove Elements)**  
### **🎯 `array_pop()` ফাংশন ব্যবহার করে শেষ উপাদান মুছে ফেলা:**  
```php
<?php
$colors = ["Red", "Green", "Blue"];
array_pop($colors); // শেষ উপাদান (Blue) মুছে যাবে

print_r($colors);
// Output: Array ( [0] => Red [1] => Green )
?>
```
✅ **`array_pop()` শুধু শেষ উপাদান সরায়।**  

### **🎯 `unset()` ব্যবহার করে নির্দিষ্ট ইনডেক্সের উপাদান মুছে ফেলা:**  
```php
<?php
$numbers = [5, 10, 15, 20];
unset($numbers[1]); // Index 1 (10) মুছে যাবে

print_r($numbers);
// Output: Array ( [0] => 5 [2] => 15 [3] => 20 )
?>
```
✅ **`unset()` নির্দিষ্ট উপাদান মুছে ফেললেও ইনডেক্স ঠিক থাকে।**  

---

## **📌 5️⃣ Indexed Array লুপ করে দেখানো (Iteration over Indexed Array)**  
### **🎯 `for` লুপ ব্যবহার করে Indexed Array লুপ করা**  
```php
<?php
$fruits = ["Apple", "Banana", "Mango"];

for ($i = 0; $i < count($fruits); $i++) {
    echo "Index $i: " . $fruits[$i] . "<br>";
}
?>
```
✅ **`count($array)` ব্যবহার করলে অ্যারের দৈর্ঘ্য পাওয়া যায়।**  

### **🎯 `foreach` লুপ ব্যবহার করে Indexed Array লুপ করা**  
```php
<?php
$fruits = ["Apple", "Banana", "Mango"];

foreach ($fruits as $fruit) {
    echo "Fruit: $fruit <br>";
}
?>
```
✅ **`foreach` লুপ সহজ ও পরিস্কারভাবে Indexed Array ট্রাভার্স করতে ব্যবহার করা হয়।**  

---

## **📌 6️⃣ Indexed Array-এর কিছু দরকারি ফাংশন**  

| ফাংশন                             | কাজ                         |
| ------------------------------- | -------------------------- |
| `count($array)`                 | অ্যারের উপাদান সংখ্যা গণনা করে       |
| `array_push($array, $value)`    | অ্যারের শেষে নতুন উপাদান যোগ করে     |
| `array_pop($array)`             | অ্যারের শেষ উপাদান সরিয়ে ফেলে        |
| `array_shift($array)`           | অ্যারের প্রথম উপাদান সরিয়ে ফেলে      |
| `array_unshift($array, $value)` | অ্যারের শুরুতে নতুন উপাদান যোগ করে    |
| `in_array($value, $array)`      | নির্দিষ্ট মান অ্যারেতে আছে কিনা যাচাই করে |
| `array_search($value, $array)`  | নির্দিষ্ট মানের ইনডেক্স খুঁজে দেয়      |

### **🎯 কিছু দরকারি Indexed Array ফাংশনের ব্যবহার**  
```php
<?php
$numbers = [5, 10, 15, 20];

echo count($numbers); // Output: 4

if (in_array(10, $numbers)) {
    echo "10 আছে!";
}

$index = array_search(15, $numbers);
echo "15-এর ইনডেক্স: " . $index;
?>
```
✅ **`in_array()` ব্যবহার করে উপাদান খুঁজে বের করা যায়।**  
✅ **`array_search()` নির্দিষ্ট মানের ইনডেক্স দেয়।**  

---

## **🎯 Indexed Array অনুশীলনের জন্য চ্যালেঞ্জ 🚀**  
👉 নিচের কোডের আউটপুট কী হবে?  
```php
<?php
$numbers = [10, 20, 30, 40];
array_shift($numbers);
array_push($numbers, 50);
print_r($numbers);
?>
```
✅ **নিজে রান করে দেখো!** 😃  

---

## **🚀 উপসংহার: আপনি এখন Indexed Array শিখে ফেলেছেন! 🎉**  
✔ **Indexed Array হল এমন অ্যারে, যেখানে প্রতিটি মান সংখ্যাসূচক ইনডেক্স দ্বারা অ্যাক্সেস করা যায়।**  
✔ Indexed Array তৈরি করা যায় `array()` অথবা `[]` ব্যবহার করে।  
✔ Indexed Array-এর উপাদান **পরিবর্তন, যোগ, মুছে ফেলা ও লুপ করা যায়।**  
✔ Indexed Array-এর জন্য **for loop, foreach loop এবং বিভিন্ন ফাংশন** রয়েছে।  

---

## **➡ পরবর্তী কি শিখতে চান?**  
✅ **Associative Array** বা **Multidimensional Array** শিখতে চান? 🚀