## **🍪 PHP Cookies বিস্তারিত শেখো**  

Cookies হলো **ছোট ডাটা ফাইল**, যা ব্রাউজারে সংরক্ষণ করা হয় এবং সার্ভার পরবর্তী অনুরোধে এই ডাটা ব্যবহার করতে পারে। এগুলো সাধারণত **ইউজারের পছন্দ, লগইন তথ্য, অথবা ট্র্যাকিং ডাটা সংরক্ষণে** ব্যবহৃত হয়।  

---

## **✅ 1️⃣ PHP-তে কুকি কীভাবে কাজ করে?**  
Cookies **ব্রাউজারে সংরক্ষণ হয়** এবং প্রতিবার যখন ইউজার সেই ওয়েবসাইট লোড করে, তখন এই কুকি সার্ভারে পাঠানো হয়।  

কুকির বৈশিষ্ট্য:  
- **Key-Value পেয়ার** হিসেবে ডাটা সংরক্ষণ করে  
- **এক্সপায়ারি টাইম সেট করা যায়**  
- **শুধু নির্দিষ্ট ডোমেইনের জন্য প্রযোজ্য**  

---

## **✅ 2️⃣ PHP-তে কুকি সেট করা**  

```php
setcookie("username", "JohnDoe", time() + (86400 * 7), "/");
```
**ব্যাখ্যা:**  
- `"username"` → কুকির নাম  
- `"JohnDoe"` → কুকির মান (value)  
- `time() + (86400 * 7)` → ৭ দিনের জন্য কুকি সংরক্ষণ  
- `"/"` → কুকি **সাইটের সব পেজেই** অ্যাক্সেসযোগ্য  

**❗ গুরুত্বপূর্ণ:** `setcookie()` অবশ্যই **HTML আউটপুটের আগে** লিখতে হবে।  

---

## **✅ 3️⃣ PHP-তে কুকি রিড করা**  

```php
if (isset($_COOKIE["username"])) {
    echo "Welcome, " . $_COOKIE["username"];
} else {
    echo "Cookie is not set!";
}
```
**ব্যাখ্যা:**  
- `$_COOKIE` হলো **সুপার গ্লোবাল ভেরিয়েবল** যা কুকির তথ্য সংরক্ষণ করে।  
- `isset($_COOKIE["username"])` → চেক করে কুকি সেট করা আছে কিনা।  

---

## **✅ 4️⃣ PHP-তে কুকি ডিলিট করা**  

```php
setcookie("username", "", time() - 3600, "/"); // কুকির এক্সপায়ারি সময় পেছনে নিয়ে যাওয়া
```
**ব্যাখ্যা:**  
- `time() - 3600` → কুকির সময় **১ ঘণ্টা আগের** সেট করা হয়েছে, যা এটিকে ব্রাউজার থেকে মুছে ফেলবে।  

---

## **✅ 5️⃣ কুকির মেয়াদ (Expiration) নিয়ন্ত্রণ**  

### **👉 সেশন কুকি (ব্রাউজার বন্ধ হলে মুছে যাবে)**  
```php
setcookie("session_cookie", "TemporaryData", 0, "/");
```
📌 **`0` দিলে** ব্রাউজার বন্ধ করার পর কুকি মুছে যাবে।  

### **👉 লং-টাইম কুকি (১ মাসের জন্য সংরক্ষণ)**  
```php
setcookie("long_cookie", "SavedData", time() + (86400 * 30), "/");
```
📌 **`86400 * 30`** = ৩০ দিনের জন্য কুকি থাকবে।  

---

## **✅ 6️⃣ কুকির অন্যান্য অপশন**  
```php
setcookie("secure_cookie", "SecureData", time() + 3600, "/", "example.com", true, true);
```
**অপশনাল প্যারামিটার:**  
1️⃣ **Path (`"/"`)** → পুরো সাইটে কুকি অ্যাক্সেসযোগ্য  
2️⃣ **Domain (`"example.com"`)** → কুকি শুধুমাত্র নির্দিষ্ট ডোমেইনে কাজ করবে  
3️⃣ **Secure (`true`)** → শুধুমাত্র HTTPS সংযোগে কাজ করবে  
4️⃣ **HttpOnly (`true`)** → JavaScript থেকে কুকি অ্যাক্সেস করা যাবে না (সিকিউরিটি বাড়ায়)  

---

## **✅ 7️⃣ কুকির ব্যবহার ক্ষেত্র**
🔹 **ইউজার লগইন ম্যানেজ করা** (Remember Me ফিচার)  
🔹 **ভিজিটর প্রেফারেন্স সংরক্ষণ করা** (Dark mode, ভাষা সেটিংস)  
🔹 **ইউজার ট্র্যাকিং** (Analytics, বিজ্ঞাপন)  

---

## **✅ 8️⃣ উদাহরণ: Remember Me ফিচার (লগইন সেভ করা)**  

### **👉 `login.php` (লগইন ফর্ম)**  
```php
<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $username = $_POST["username"];
    $password = $_POST["password"];

    if ($username == "admin" && $password == "1234") {
        setcookie("remember_user", $username, time() + (86400 * 7), "/"); // 7 দিনের জন্য কুকি সেট
        echo "Login successful!";
    } else {
        echo "Invalid credentials!";
    }
}
?>
<form method="post">
    Username: <input type="text" name="username"><br>
    Password: <input type="password" name="password"><br>
    <button type="submit">Login</button>
</form>
```

### **👉 `dashboard.php` (অটো-লগইন চেক করা)**  
```php
<?php
if (isset($_COOKIE["remember_user"])) {
    echo "Welcome back, " . $_COOKIE["remember_user"];
} else {
    echo "Please log in.";
}
?>
```

✅ **এতে ইউজার একবার লগইন করলে, কুকির মাধ্যমে ৭ দিন পর্যন্ত লগইন থাকা নিশ্চিত হবে।**  

---

## **🚀 সারসংক্ষেপ**
| **কুকি অপারেশন**  | **PHP কোড**                                                                   |
| ------------- | ---------------------------------------------------------------------------- |
| **কুকি সেট করা**  | `setcookie("name", "value", time() + 3600, "/");`                            |
| **কুকি রিড করা**  | `$_COOKIE["name"]`                                                           |
| **কুকি ডিলিট করা** | `setcookie("name", "", time() - 3600, "/");`                                 |
| **Secure কুকি** | `setcookie("name", "value", time() + 3600, "/", "example.com", true, true);` |

---

## **🎯 পরবর্তী ধাপ**
✅ **কুকি vs সেশন** শিখতে চাও? 🔥  
✅ **সিকিউরিটি:** কুকির **XSS এবং CSRF নিরাপত্তা** নিয়ে জানতে চাও? 🔐  

বলো, কোন টপিক নিয়ে আরও বিস্তারিত জানতে চাও? 😃