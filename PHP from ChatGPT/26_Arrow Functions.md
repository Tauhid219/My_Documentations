# 🚀 **PHP Arrow Functions (=>) বিস্তারিত শেখা** 🚀  

## 📌 **Arrow Function কি?**  
PHP 7.4 থেকে **Arrow Functions** যোগ করা হয়েছে, যা **Anonymous Function (Closure)** এর সহজতর ফরম্যাট।  

✅ এটি **Lambda Function** এর মতোই কাজ করে, তবে এক লাইনের জন্য ব্যবহার করা হয়।  
✅ এটি **Parent Scope থেকে Variable ইনহেরিট করতে পারে** `use` ছাড়াই!  
✅ Syntax **শর্ট এবং ক্লিন** হয়।  

---

## 🔥 **Arrow Function এর Syntax**
Arrow Function লিখতে হয়:  

```php
fn (parameters) => expression;
```
✔️ **`fn` keyword** দিয়ে শুরু হয়।  
✔️ **Return keyword ব্যবহার করতে হয় না**, এক্সপ্রেশন **স্বয়ংক্রিয়ভাবে রিটার্ন হয়**।  

---

## 📌 **Arrow Function vs Anonymous Function**
### ✅ **Anonymous Function (Closure) Example**
```php
<?php
$multiply = function($a, $b) {
    return $a * $b;
};

echo $multiply(5, 3);
?>
```
✔️ **আউটপুট:**  
```
15
```

### ✅ **Arrow Function দিয়ে একই কাজ করা**
```php
<?php
$multiply = fn($a, $b) => $a * $b;

echo $multiply(5, 3);
?>
```
✔️ **আউটপুট:**  
```
15
```

✅ **Arrow Function অনেক ছোট ও ক্লিন!**  

---

## 📌 **Arrow Function এর সুবিধা**
1️⃣ **কোড ছোট এবং ক্লিন**  
2️⃣ **Parent Scope থেকে Variable ইনহেরিট করতে পারে `use` ছাড়াই**  
3️⃣ **Anonymous Function থেকে ফাস্টার**  
4️⃣ **এক লাইনের ফাংশনের জন্য পারফেক্ট**  

---

## 🎯 **Parent Scope থেকে Variable ইনহেরিট করা**
👉 **Anonymous Function এ `use` দিয়ে ইনহেরিট করতে হয়:**  
```php
<?php
$factor = 2;

$double = function($n) use ($factor) {
    return $n * $factor;
};

echo $double(5);
?>
```
✔️ **আউটপুট:**  
```
10
```

👉 **Arrow Function এ `use` লাগবে না!**  
```php
<?php
$factor = 2;

$double = fn($n) => $n * $factor;

echo $double(5);
?>
```
✔️ **আউটপুট:**  
```
10
```
✅ **Arrow Function অটোমেটিক Parent Scope থেকে Variable নেয়!**  

---

## 📌 **Arrow Function ব্যবহার: Practical Examples**  

### ✅ **1. Array Map এর সাথে ব্যবহার**
```php
<?php
$numbers = [1, 2, 3, 4];

$squaredNumbers = array_map(fn($n) => $n * $n, $numbers);

print_r($squaredNumbers);
?>
```
✔️ **আউটপুট:**  
```
Array ( [0] => 1 [1] => 4 [2] => 9 [3] => 16 )
```

---

### ✅ **2. Array Filter এর সাথে ব্যবহার**
```php
<?php
$numbers = [1, 2, 3, 4, 5, 6];

$evenNumbers = array_filter($numbers, fn($n) => $n % 2 == 0);

print_r($evenNumbers);
?>
```
✔️ **আউটপুট:**  
```
Array ( [1] => 2 [3] => 4 [5] => 6 )
```
✅ **শুধু জোড় সংখ্যা রেখে দিল!**  

---

### ✅ **3. Sort করার জন্য `usort()` এর সাথে ব্যবহার**
```php
<?php
$people = [
    ["name" => "Alice", "age" => 25],
    ["name" => "Bob", "age" => 22],
    ["name" => "Charlie", "age" => 30]
];

usort($people, fn($a, $b) => $a['age'] <=> $b['age']);

print_r($people);
?>
```
✔️ **আউটপুট:**  
```
Array (
    [0] => Array ( [name] => Bob [age] => 22 )
    [1] => Array ( [name] => Alice [age] => 25 )
    [2] => Array ( [name] => Charlie [age] => 30 )
)
```
✅ **বয়স অনুযায়ী সাজানো হয়েছে।**  

---

## 📌 **Arrow Function এর সীমাবদ্ধতা (Limitations)**
❌ **কেবলমাত্র এক্সপ্রেশন (expression) ব্যবহার করা যায়**  
✔️ নিচের মতো **ব্লক `{}` ব্যবহার করা যায় না!**  
```php
fn($x) => { return $x * 2; } // ❌ ভুল!  
```

❌ **মাল্টিপল স্টেটমেন্ট ব্যবহার করা যায় না**  
✔️ **শুধু এক্সপ্রেশন হতে হবে!**  
```php
fn($x) => print($x);  // ✅ ঠিক আছে  
```

---

## 🔥 **Arrow Function vs Traditional Function: কোনটা কবে ব্যবহার করবেন?**
| **Feature**            | **Arrow Function (=>)**      | **Anonymous Function (Closure)**  |
| ---------------------- | ---------------------------- | --------------------------------- |
| **Syntax**             | `fn($x) => $x * 2;`          | `function($x) { return $x * 2; }` |
| **Variable Scope**     | Parent Scope থেকে `use` ছাড়াই নেয় | `use` দিয়ে ইনহেরিট করতে হয়             |
| **Multi-line Support** | ❌ শুধু এক্সপ্রেশন সাপোর্ট করে         | ✅ মাল্টিপল স্টেটমেন্ট সাপোর্ট করে            |
| **Performance**        | ✅ একটু ফাস্ট                    | ❌ তুলনামূলক স্লো                       |
| **Usage**              | ✅ Simple ও শর্টকোডের জন্য        | ✅ Complex এবং মাল্টিপল স্টেটমেন্টের জন্য    |

✔️ **Arrow Function শুধু এক লাইনের জন্য ব্যবহার করুন!**  
✔️ **অনেক লজিক থাকলে Anonymous Function ব্যবহার করা ভালো!**  

---

## 🚀 **শেষ কথা**
✅ **PHP Arrow Function ছোট, ক্লিন ও পারফরম্যান্স ভালো**  
✅ **Anonymous Function এর বিকল্প নয়, বরং শর্টকাট ভার্সন**  
✅ **শুধু এক্সপ্রেশন ব্যবহার করা যায়, মাল্টিপল স্টেটমেন্ট না!**  

---

### 🎯 **তুমি যদি আরও ডিটেল জানতে চাও, তাহলে বলো! 😊**