PHP-তে JSON (JavaScript Object Notation) প্রসেসিং খুবই দরকারি, কারণ এটি API, ডাটা স্টোরেজ, ও ডাটা ট্রান্সফারের জন্য বহুল ব্যবহৃত ফরম্যাট। PHP JSON প্রসেসিং মূলত চারটি ধাপে করা হয়:  

1. **JSON Encode (PHP অ্যারে → JSON)**  
2. **JSON Decode (JSON → PHP অ্যারে বা অবজেক্ট)**  
3. **JSON ফাইল লেখা ও পড়া**  
4. **JSON ফরম্যাট মডিফাই করা**  

---

## ১. JSON Encode (PHP অ্যারে → JSON)  

PHP-তে `json_encode()` ব্যবহার করে অ্যারে বা অবজেক্টকে JSON ফরম্যাটে রূপান্তর করা হয়।  

```php
<?php
$data = [
    "id" => 1,
    "name" => "John Doe",
    "email" => "john@example.com"
];

$json = json_encode($data, JSON_PRETTY_PRINT);
echo $json;
?>
```

**ফলাফল:**  
```json
{
    "id": 1,
    "name": "John Doe",
    "email": "john@example.com"
}
```

👉 `JSON_PRETTY_PRINT` ব্যবহার করলে JSON ফরম্যাট সুন্দরভাবে সাজানো হয়।

---

## ২. JSON Decode (JSON → PHP অ্যারে বা অবজেক্ট)  

JSON ডাটা PHP-তে অ্যারে বা অবজেক্টে রূপান্তর করতে `json_decode()` ব্যবহার করা হয়।  

```php
<?php
$json = '{
    "id": 1,
    "name": "John Doe",
    "email": "john@example.com"
}';

// JSON কে অ্যারেতে কনভার্ট করা (true দিলে অ্যারে রিটার্ন করে)
$dataArray = json_decode($json, true);

// JSON কে অবজেক্টে কনভার্ট করা (default রিটার্ন object)
$dataObject = json_decode($json);

print_r($dataArray);
echo "<br>";
echo $dataObject->name; // অবজেক্ট হলে এমনভাবে অ্যাক্সেস করতে হবে
?>
```

**ফলাফল:**  
```php
Array
(
    [id] => 1
    [name] => John Doe
    [email] => john@example.com
)
John Doe
```

---

## ৩. JSON ফাইল লেখা ও পড়া  

PHP JSON ফাইল তৈরি ও পড়ার জন্য `file_put_contents()` এবং `file_get_contents()` ব্যবহার করা হয়।  

### **JSON ফাইলে লেখা (Writing JSON to a File)**  

```php
<?php
$data = [
    ["id" => 1, "name" => "John Doe", "email" => "john@example.com"],
    ["id" => 2, "name" => "Jane Doe", "email" => "jane@example.com"]
];

$json = json_encode($data, JSON_PRETTY_PRINT);
file_put_contents("data.json", $json);

echo "JSON ফাইল সফলভাবে লেখা হয়েছে!";
?>
```

✔ `data.json` ফাইল তৈরি হবে এবং এতে JSON ডাটা থাকবে।

---

### **JSON ফাইল থেকে ডাটা পড়া (Reading JSON from a File)**  

```php
<?php
$json = file_get_contents("data.json");
$data = json_decode($json, true);

foreach ($data as $user) {
    echo "ID: " . $user['id'] . " | Name: " . $user['name'] . " | Email: " . $user['email'] . "<br>";
}
?>
```

**ফলাফল:**  
```
ID: 1 | Name: John Doe | Email: john@example.com  
ID: 2 | Name: Jane Doe | Email: jane@example.com  
```

---

## ৪. JSON ফরম্যাট মডিফাই করা  

ধরা যাক, আমরা `data.json` ফাইল থেকে একজন ইউজারের নাম পরিবর্তন করতে চাই।  

```php
<?php
$filename = "data.json";
$json = file_get_contents($filename);
$data = json_decode($json, true);

// ID 2 এর নাম পরিবর্তন করা
foreach ($data as &$user) {
    if ($user['id'] == 2) {
        $user['name'] = "Jane Doe (Updated)";
    }
}

// আবার JSON ফাইল লিখে আপডেট করা
file_put_contents($filename, json_encode($data, JSON_PRETTY_PRINT));

echo "JSON ফাইল সফলভাবে আপডেট হয়েছে!";
?>
```

✔ `data.json` ফাইলের **ID 2** এর নাম আপডেট হয়ে যাবে।

---

## ৫. JSON ফাইল ব্রাউজারে ডাউনলোড করানো  

```php
<?php
$data = [
    ["id" => 1, "name" => "John Doe", "email" => "john@example.com"],
    ["id" => 2, "name" => "Jane Doe", "email" => "jane@example.com"]
];

$json = json_encode($data, JSON_PRETTY_PRINT);

header('Content-Type: application/json');
header('Content-Disposition: attachment; filename="download.json"');

echo $json;
?>
```

✔ এই স্ক্রিপ্ট চালালে **download.json** নামে ফাইল ব্রাউজারে ডাউনলোড হবে।

---

## ৬. JSON ডাটা থেকে নির্দিষ্ট মান খোঁজা  

```php
<?php
$json = '[
    {"id": 1, "name": "John Doe", "email": "john@example.com"},
    {"id": 2, "name": "Jane Doe", "email": "jane@example.com"}
]';

$data = json_decode($json, true);

// নির্দিষ্ট ID 2 কে খুঁজে বের করা
foreach ($data as $user) {
    if ($user['id'] == 2) {
        echo "User Found: " . $user['name'];
    }
}
?>
```

✔ যদি ID 2 থাকে, তাহলে **User Found: Jane Doe** প্রিন্ট হবে।

---

## 📌 **শেষ কথা**  

✅ **PHP অ্যারে → JSON:** `json_encode()`  
✅ **JSON → PHP অ্যারে/অবজেক্ট:** `json_decode()`  
✅ **JSON ফাইলে লেখা:** `file_put_contents()`  
✅ **JSON ফাইল থেকে পড়া:** `file_get_contents()`  
✅ **JSON ফরম্যাট মডিফাই:** `json_encode()`, `json_decode()`  

এখন বলো, তুমি কি আরও গভীরভাবে কিছু শিখতে চাও, নাকি কোনো নির্দিষ্ট প্রজেক্টে JSON ব্যবহার করতে চাও? 😊