# **PHP-তে Type Hinting শেখা**  

PHP-তে **Type Hinting (Type Declarations)** ব্যবহার করে আমরা **ফাংশনের প্যারামিটারের জন্য নির্দিষ্ট ডাটা টাইপ নির্ধারণ করতে পারি।** এটি **ভুল টাইপের ইনপুট প্রতিরোধ করে** এবং **কোডকে আরও নির্ভরযোগ্য ও পাঠযোগ্য করে।**  

---

## **১. PHP-তে Type Hinting কী?**  
Type Hinting মানে হল **ফাংশনের প্যারামিটার বা রিটার্ন ভ্যালুর জন্য নির্দিষ্ট টাইপ ডিক্লেয়ার করা।**  
PHP 7 থেকে এটি আরও শক্তিশালী হয়েছে, কারণ এতে **Scalar Types (int, float, bool, string)** যোগ করা হয়েছে।  

**🔹 Type Hinting-এর সুবিধা:**  
✅ টাইপ-সংক্রান্ত ভুল কমায়  
✅ কোডকে আরও পরিষ্কার এবং বাগ-মুক্ত করে  
✅ IDE ও Static Analysis Tools-এর সাহায্যে উন্নত কোড চেকিং সম্ভব  

---

## **২. PHP-তে যে টাইপগুলোর জন্য Type Hinting করা যায়**  
PHP-তে নিম্নলিখিত টাইপগুলোতে **Type Hinting (Type Declaration) করা যায়:**  

| **টাইপ** | **ব্যাখ্যা** |
|----------|------------|
| `int` | পূর্ণসংখ্যা (Integer) |
| `float` | দশমিক সংখ্যা (Floating Point Number) |
| `bool` | বুলিয়ান (True/False) |
| `string` | টেক্সট বা স্ট্রিং |
| `array` | অ্যারে |
| `object` | অবজেক্ট |
| `callable` | কলব্যাক ফাংশন |
| `self` | একই ক্লাসের অবজেক্ট |
| `parent` | প্যারেন্ট ক্লাসের অবজেক্ট |
| `?Type` | Nullable টাইপ (PHP 7.1+) |
| `mixed` | যেকোনো টাইপ গ্রহণ করবে (PHP 8.0+) |
| `static` | বর্তমান ক্লাসের অবজেক্ট (PHP 8.0+) |
| `never` | কখনও কোনো ভ্যালু রিটার্ন করবে না (PHP 8.1+) |

---

## **৩. Scalar Type Hinting (int, float, string, bool)**  
PHP 7 থেকে আমরা `int`, `float`, `string`, এবং `bool` ব্যবহার করতে পারি।  

🔹 **উদাহরণ:**  

```php
<?php
function addNumbers(int $a, int $b): int {
    return $a + $b;
}

echo addNumbers(5, 10); // Output: 15
?>
```
✔️ এখানে `int $a` এবং `int $b` এর মান **শুধু integer হতে পারবে।**  
✔️ `: int` নির্দেশ করে যে **ফাংশনের রিটার্ন টাইপও integer হবে।**  
❌ যদি `addNumbers("5", "10")` পাঠাই, PHP এটি **স্বয়ংক্রিয়ভাবে integer-এ কনভার্ট করে নেবে (coercion)।**

---

## **৪. Strict Type Mode ব্যবহার করা (Type Coercion বন্ধ করা)**  
PHP ডিফল্টভাবে **টাইপ পরিবর্তন (Type Coercion) করে।** তবে আমরা `declare(strict_types=1);` ব্যবহার করে এটি বন্ধ করতে পারি।  

🔹 **উদাহরণ:**  
```php
<?php
declare(strict_types=1);

function multiply(float $x, float $y): float {
    return $x * $y;
}

echo multiply(4.5, 2); // Output: 9.0
echo multiply("4.5", "2"); // ❌ Fatal Error (string পাঠানো যাবে না)
?>
```
✔️ এখানে `declare(strict_types=1);` ব্যবহার করার ফলে **string পাঠালে Error হবে।**  

---

## **৫. Array Type Hinting**  
🔹 **উদাহরণ:**  

```php
<?php
function printNames(array $names) {
    foreach ($names as $name) {
        echo $name . "\n";
    }
}

printNames(["Alice", "Bob", "Charlie"]); // ✅ ঠিক আছে
printNames("Alice"); // ❌ Fatal Error
?>
```
✔️ `array $names` মানে **শুধুমাত্র অ্যারে পাঠানো যাবে।**  
❌ `printNames("Alice");` দিলে **Error হবে।**  

---

## **৬. Object Type Hinting**  
PHP 7 থেকে নির্দিষ্ট **ক্লাসের অবজেক্ট টাইপ** ব্যবহার করা যায়।  

🔹 **উদাহরণ:**  

```php
<?php
class User {
    public string $name;
    public function __construct(string $name) {
        $this->name = $name;
    }
}

function greet(User $user): string {
    return "Hello, " . $user->name;
}

$user1 = new User("Alice");
echo greet($user1); // Output: Hello, Alice
?>
```
✔️ `User $user` মানে **শুধুমাত্র `User` ক্লাসের অবজেক্ট গ্রহণ করবে।**  

---

## **৭. Nullable Type Hinting (`?Type`)**  
PHP 7.1 থেকে `?` দিয়ে **nullable টাইপ** করা যায়।  

🔹 **উদাহরণ:**  
```php
<?php
function setAge(?int $age): string {
    if ($age === null) {
        return "Age is not provided.";
    }
    return "Age is $age years.";
}

echo setAge(25);  // Output: Age is 25 years.
echo setAge(null); // Output: Age is not provided.
?>
```
✔️ `?int` মানে **এই প্যারামিটার `int` অথবা `null` হতে পারে।**  

---

## **৮. Return Type Hinting**  
PHP 7 থেকে **ফাংশনের রিটার্ন টাইপ** নির্ধারণ করা যায়।  

🔹 **উদাহরণ:**  
```php
<?php
function divide(int $a, int $b): float {
    return $a / $b;
}

echo divide(10, 4); // Output: 2.5
?>
```
✔️ `: float` নির্দেশ করে যে **ফাংশন শুধুমাত্র `float` রিটার্ন করবে।**  

---

## **৯. PHP 8-এর নতুন Type Hinting ফিচার**  
✅ **Mixed Type Hinting (PHP 8.0+)**  
```php
function processData(mixed $data): mixed {
    return $data;
}
```
✔️ `mixed` টাইপ **যেকোনো ডাটা গ্রহণ করতে পারে।**  

✅ **Union Types (PHP 8.0+)**  
```php
function sum(int|float $a, int|float $b): int|float {
    return $a + $b;
}
```
✔️ `int|float` মানে **এই প্যারামিটার integer অথবা float হতে পারে।**  

✅ **Never Return Type (PHP 8.1+)**  
```php
function stopExecution(): never {
    exit("Execution stopped!");
}
```
✔️ `never` মানে **ফাংশন কখনও কোনো ভ্যালু রিটার্ন করবে না।**  

---

## **সংক্ষেপে গুরুত্বপূর্ণ পয়েন্ট**  
✅ PHP 7+ এ **Scalar Type Hinting (`int`, `float`, `string`, `bool`)** যোগ হয়েছে।  
✅ `declare(strict_types=1);` ব্যবহার করলে **Type Coercion বন্ধ হবে।**  
✅ **Array, Object, Nullable (`?Type`), এবং Return Type Hinting** ব্যবহার করা যায়।  
✅ PHP 8+ এ **Union Types, Mixed, এবং Never Return Type** এসেছে।  

---

## **পরবর্তী কী শিখতে চাও?**  
PHP-এর **Magic Methods, Traits, বা OOP (Object-Oriented Programming)** শিখতে চাও? 😊