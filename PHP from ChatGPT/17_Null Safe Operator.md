# 🚀 **PHP-এর Null Safe Operator (`?->`) সম্পূর্ণ গাইড**  

PHP 8-এ **Null Safe Operator (`?->`)** যোগ করা হয়েছে, যা **Method Chaining-এ `null` চেক করার সহজ উপায়**। এটি `isset()` বা `if` ব্যবহার না করেই **Object Property বা Method-এ `null` চেক করতে দেয়**।  

---

## **📌 1️⃣ Null Safe Operator কী?**  
`?->` **Null চেক করে এবং যদি ভেরিয়েবল `null` হয়, তাহলে `null` রিটার্ন করে, কোনো Error দেয় না।**  

### **🎯 Syntax:**  
```php
$variable?->property;
$variable?->method();
```
✔ **যদি `$variable` `null` হয়, তাহলে `null` রিটার্ন করবে, Error দিবে না।**  
✔ **যদি `$variable` `null` না হয়, তাহলে `property` বা `method` কল হবে।**  

---

## **📌 2️⃣ Null Safe Operator ব্যবহার না করে আগের পদ্ধতি**  
PHP 8-এর আগে **`null` চেক করতে `if` বা `isset()` ব্যবহার করতে হতো**।  
```php
<?php
class User {
    public function getName() {
        return "Alice";
    }
}

$user = null;

if ($user !== null) {
    $name = $user->getName();
} else {
    $name = null;
}

echo $name;
?>
```
**আউটপুট:**  
```
(null)
```
✔ এটি কাজ করে, কিন্তু **কোড বড় এবং জটিল হয়ে যায়।**  

---

## **📌 3️⃣ Null Safe Operator (`?->`) ব্যবহার করে সহজ সমাধান**  
```php
<?php
class User {
    public function getName() {
        return "Alice";
    }
}

$user = null;
$name = $user?->getName();

echo $name;
?>
```
**আউটপুট:**  
```
(null)
```
✅ **এটি আগের চেয়ে অনেক সহজ এবং ক্লিন!**  

---

## **📌 4️⃣ Property Access-এ `?->` ব্যবহার**  
```php
<?php
class Profile {
    public $age = 25;
}

class User {
    public ?Profile $profile = null;
}

$user = new User();

$age = $user->profile?->age;

echo $age;
?>
```
**আউটপুট:**  
```
(null)
```
✔ **যেহেতু `$user->profile` `null`, তাই `$user->profile?->age` `null` রিটার্ন করবে।**  

---

## **📌 5️⃣ Nested Object-এর সাথে `?->` ব্যবহার**  
```php
<?php
class Address {
    public function getCity() {
        return "Dhaka";
    }
}

class User {
    public ?Address $address = null;
}

$user = new User();
$city = $user->address?->getCity();

echo $city;
?>
```
**আউটপুট:**  
```
(null)
```
✔ **যেহেতু `$user->address` `null`, তাই `getCity()` কল না হয়ে `null` রিটার্ন হবে।**  

---

## **📌 6️⃣ Array-এর সাথে `?->` ব্যবহার করা যাবে না**  
```php
<?php
$data = null;
echo $data?["key"]; // ❌ এটি Error দিবে
?>
```
🔴 **Error:** `Cannot use nullsafe operator on array`  
✅ **সঠিক পদ্ধতি:**  
```php
<?php
$data = null;
echo $data["key"] ?? "Default Value"; // ✅ `??` ব্যবহার করুন
?>
```

---

## **🎯 অনুশীলনের জন্য চ্যালেঞ্জ 🚀**  
```php
<?php
class Order {
    public function getTotal() {
        return 500;
    }
}

$order = null;
$total = $order?->getTotal();

echo $total;
?>
```
❓ **উপরের কোড রান করলে কী আউটপুট আসবে?** নিজে রান করে দেখো! 😃  

---

## **🚀 উপসংহার: আপনি এখন PHP-এর `?->` Null Safe Operator শিখে ফেলেছেন! 🎉**  
✔ এটি **Method Chaining-এ `null` চেকের সহজ উপায়**।  
✔ **`null` হলে Error না দিয়ে `null` রিটার্ন করে।**  
✔ **Object Property এবং Method-এর জন্য কাজ করে।**  
✔ **Array-এ কাজ করে না, এজন্য `??` ব্যবহার করতে হয়।**  

---

## **➡ পরবর্তী কি শিখতে চান?**  
✅ **PHP-এর Logical Operators (&&, ||, !) শিখতে চান? 🚀**