PHP-তে **File Uploads** শেখা খুব গুরুত্বপূর্ণ, কারণ ওয়েব অ্যাপ্লিকেশনগুলোর অনেক ক্ষেত্রেই ইউজারদের ফাইল আপলোড করার প্রয়োজন হয়, যেমনঃ প্রোফাইল ছবি, ডকুমেন্ট, PDF, ইত্যাদি।  

আমি ধাপে ধাপে PHP-তে ফাইল আপলোড করার নিয়ম দেখাবো।  

---

## 1️⃣ **PHP File Upload করার জন্য প্রয়োজনীয় সেটআপ**  
ফাইল আপলোড করতে হলে HTML ফর্মে কিছু স্পেশাল সেটআপ করতে হয়ঃ  
- `method="post"` দিতে হবে।  
- `enctype="multipart/form-data"` দিতে হবে।  
- `input` ফিল্ডে `type="file"` দিতে হবে।  

---

## 2️⃣ **একটি বেসিক PHP File Upload Form**  
```html
<form action="upload.php" method="post" enctype="multipart/form-data">
    <label>Select a file to upload:</label>
    <input type="file" name="file">
    <button type="submit" name="submit">Upload</button>
</form>
```
👉 **এখানে:**  
- `enctype="multipart/form-data"` থাকার ফলে ফাইল ডাটা সার্ভারে পাঠানো যাবে।  
- `input type="file"` দিয়ে ফাইল নির্বাচন করা যাবে।  

---

## 3️⃣ **PHP-তে ফাইল আপলোড প্রক্রিয়া (`upload.php`)**  
```php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    if (isset($_FILES['file'])) {
        $file = $_FILES['file'];

        // ফাইলের তথ্য সংগ্রহ করা
        $fileName = $file['name'];
        $fileTmpName = $file['tmp_name'];
        $fileSize = $file['size'];
        $fileError = $file['error'];
        $fileType = $file['type'];

        // আপলোড ফোল্ডার
        $uploadDir = "uploads/";

        // যদি কোনো সমস্যা না থাকে
        if ($fileError === 0) {
            $destination = $uploadDir . basename($fileName);
            
            if (move_uploaded_file($fileTmpName, $destination)) {
                echo "File uploaded successfully!";
            } else {
                echo "File upload failed.";
            }
        } else {
            echo "Error in file upload!";
        }
    } else {
        echo "No file uploaded.";
    }
}
```
👉 **এখানে:**  
- `$_FILES['file']` দিয়ে ফাইলের তথ্য নেওয়া হয়েছে।  
- `move_uploaded_file()` ব্যবহার করে ফাইল `uploads/` ফোল্ডারে সংরক্ষণ করা হয়েছে।  

---

## 4️⃣ **ফাইল আপলোডের সময় ভ্যালিডেশন করা (File Type & Size চেক করা)**  
ফাইল আপলোডের সময় কিছু যাচাই করা দরকার, যেমনঃ  
✅ **সঠিক ফাইল টাইপ (image, pdf, etc.)**  
✅ **সঠিক ফাইল সাইজ (২MB-এর বেশি না হওয়া)**  

### **(1) Updated `upload.php` Code with Validation**  
```php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    if (isset($_FILES['file'])) {
        $file = $_FILES['file'];

        $fileName = $file['name'];
        $fileTmpName = $file['tmp_name'];
        $fileSize = $file['size'];
        $fileError = $file['error'];
        $fileType = $file['type'];

        // অনুমোদিত ফাইল টাইপ লিস্ট
        $allowedTypes = ['image/jpeg', 'image/png', 'image/gif', 'application/pdf'];

        // আপলোড ফোল্ডার
        $uploadDir = "uploads/";
        $destination = $uploadDir . basename($fileName);

        // ফাইল টাইপ চেক করা
        if (!in_array($fileType, $allowedTypes)) {
            echo "Only JPG, PNG, GIF, and PDF files are allowed.";
        } 
        // ফাইল সাইজ চেক করা (২MB-এর বেশি না হওয়া)
        elseif ($fileSize > 2 * 1024 * 1024) {
            echo "File size must be less than 2MB.";
        } 
        // ফাইল আপলোড প্রসেস
        else {
            if (move_uploaded_file($fileTmpName, $destination)) {
                echo "File uploaded successfully!";
            } else {
                echo "File upload failed.";
            }
        }
    } else {
        echo "No file uploaded.";
    }
}
```
👉 **এখানে:**  
- `in_array($fileType, $allowedTypes)` দিয়ে ফাইল টাইপ চেক করা হয়েছে।  
- `if ($fileSize > 2 * 1024 * 1024)` দিয়ে ২MB-এর বেশি হলে বাতিল করা হয়েছে।  

---

## 5️⃣ **ফাইলের নাম পরিবর্তন করে সংরক্ষণ করা (Prevent Overwriting)**  
একই নামে ফাইল আপলোড হলে আগের ফাইল ওভাররাইট হয়ে যেতে পারে। তাই আমরা ফাইলের নাম পরিবর্তন করতে পারি।

### **Updated `upload.php` with Unique Name**
```php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    if (isset($_FILES['file'])) {
        $file = $_FILES['file'];

        $fileName = $file['name'];
        $fileTmpName = $file['tmp_name'];
        $fileSize = $file['size'];
        $fileError = $file['error'];
        $fileType = $file['type'];

        $allowedTypes = ['image/jpeg', 'image/png', 'image/gif', 'application/pdf'];
        $uploadDir = "uploads/";

        if (!in_array($fileType, $allowedTypes)) {
            echo "Only JPG, PNG, GIF, and PDF files are allowed.";
        } elseif ($fileSize > 2 * 1024 * 1024) {
            echo "File size must be less than 2MB.";
        } else {
            // ইউনিক নাম তৈরি করা
            $fileExt = pathinfo($fileName, PATHINFO_EXTENSION);
            $newFileName = uniqid("file_", true) . "." . $fileExt;
            $destination = $uploadDir . $newFileName;

            if (move_uploaded_file($fileTmpName, $destination)) {
                echo "File uploaded successfully as $newFileName";
            } else {
                echo "File upload failed.";
            }
        }
    } else {
        echo "No file uploaded.";
    }
}
```
👉 **এখানে:**  
✅ `uniqid("file_", true)` ব্যবহার করে ইউনিক নাম তৈরি করা হয়েছে।  
✅ আগের ফাইলের সাথে ওভাররাইট হওয়ার সমস্যা আর থাকবে না।  

---

## 6️⃣ **ফাইল আপলোডের জন্য `uploads/` ফোল্ডার তৈরির প্রয়োজনীয়তা**
আপনার `uploads/` ফোল্ডার অবশ্যই থাকতে হবে, না হলে ফাইল আপলোড হবে না।  
আপনি এটি ম্যানুয়ালি তৈরি করতে পারেন বা PHP দিয়ে অটোমেটিক তৈরি করতে পারেনঃ  

```php
$uploadDir = "uploads/";

if (!is_dir($uploadDir)) {
    mkdir($uploadDir, 0777, true);
}
```
👉 এটি যদি `uploads/` ফোল্ডার না থাকে, তাহলে সেটি তৈরি করে দেবে।  

---

## **🎯 গুরুত্বপূর্ণ টিপস**
✔ **শুধু নির্দিষ্ট টাইপের ফাইল অনুমোদন করুন (JPG, PNG, PDF ইত্যাদি)।**  
✔ **ফাইল সাইজের সীমা নির্ধারণ করুন (যেমনঃ 2MB)।**  
✔ **SQL Injection ও XSS অ্যাটাক প্রতিরোধের জন্য ফাইলের নাম নিরাপদ করুন।**  
✔ **ইউনিক ফাইল নাম ব্যবহার করুন যাতে আগের ফাইল ওভাররাইট না হয়।**  
✔ **`uploads/` ফোল্ডার যেন `writable` হয় (`chmod 777 uploads/`)।**  

---

## **✅ পরবর্তী ধাপ**
এখন তুমি চাইলে **Ajax File Upload**, **Multiple File Upload**, এবং **Laravel File Upload** নিয়েও জানতে পারো! 🚀