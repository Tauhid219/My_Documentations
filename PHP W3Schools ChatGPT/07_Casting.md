## **🔹 PHP Casting সম্পূর্ণ গাইড**  

PHP Casting দ্বারা **একটি ভেরিয়েবলকে এক ডাটা টাইপ থেকে অন্য ডাটা টাইপে পরিবর্তন** করা যায়। এটি গুরুত্বপূর্ণ যখন আপনাকে **ইনপুট ভ্যালু ম্যানিপুলেট করতে হয় বা সঠিক ডাটা টাইপে কনভার্ট করতে হয়।**  

এই গাইডে আপনি শিখবেন:  
✅ Casting কী?  
✅ PHP-তে বিভিন্ন ধরনের Casting  
✅ Practical উদাহরণ  

---

## **🔹 1️⃣ PHP Casting কী?**  
PHP হল **loosely typed language**, যার মানে হলো **ভেরিয়েবলের টাইপ ডায়নামিকভাবে পরিবর্তন হতে পারে।**  

🔸 ধরুন, আপনি `"123"` (string) কে **integer** এ কনভার্ট করতে চান, তাহলে **casting ব্যবহার করতে পারেন।**  
```php
$number = (int) "123"; // String থেকে Integer-এ কনভার্ট হবে
echo $number; // Output: 123
```

---

## **🔹 2️⃣ PHP-তে বিভিন্ন ধরনের Casting**  
PHP-তে নিচের **7টি মেইন Casting টাইপ** আছে:  

| Casting টাইপ            | সিনট্যাক্স                          | উদাহরণ                                            |
| ---------------------- | ------------------------------- | ------------------------------------------------ |
| Integer Casting        | `(int)`, `(integer)`            | `(int) "123"` → `123`                            |
| Float (Double) Casting | `(float)`, `(double)`, `(real)` | `(float) "123.45"` → `123.45`                    |
| String Casting         | `(string)`                      | `(string) 123` → `"123"`                         |
| Boolean Casting        | `(bool)`, `(boolean)`           | `(bool) 0` → `false`, `(bool) 1` → `true`        |
| Array Casting          | `(array)`                       | `(array) "hello"` → `["hello"]`                  |
| Object Casting         | `(object)`                      | `(object) ["name" => "John"]` → `{name: "John"}` |
| Null Casting           | `(unset)`                       | `(unset) "Hello"` → `NULL`                       |

---

## **🔹 3️⃣ Integer Casting (String → Integer)**  
👉 একটি String বা Float-কে Integer-এ কনভার্ট করতে `(int)` বা `(integer)` ব্যবহার করা হয়:  
```php
$value1 = (int) "123"; 
$value2 = (int) "123.99"; 
$value3 = (int) "Hello"; 

echo $value1; // 123
echo $value2; // 123 (decimal অংশ কেটে যায়)
echo $value3; // 0 (string invalid হলে 0 হয়)
```
🔹 **সংখ্যাসূচক স্ট্রিং হলে তা integer-এ রূপান্তর হয়।**  
🔹 **স্ট্রিং নম্বর না হলে আউটপুট `0` হবে।**  

---

## **🔹 4️⃣ Float (Double) Casting (String → Float)**  
👉 `(float)`, `(double)`, বা `(real)` ব্যবহার করে **Floating Point Number-এ** কনভার্ট করা যায়।  
```php
$value1 = (float) "123.45";  
$value2 = (double) 150;  

echo $value1; // 123.45
echo $value2; // 150.0
```
🔹 **Integer ও String-এর দশমিক সংখ্যা স্বাভাবিকভাবেই কনভার্ট হয়।**  

---

## **🔹 5️⃣ String Casting (Number → String)**  
👉 `(string)` ব্যবহার করলে **সংখ্যাকে স্ট্রিং-এ কনভার্ট** করা যায়।  
```php
$value1 = (string) 123; 
$value2 = (string) 45.67; 
$value3 = (string) true;

echo $value1; // "123"
echo $value2; // "45.67"
echo $value3; // "1" (true মানে "1" হয়)
```
🔹 **Boolean true হলে `"1"` এবং false হলে `""` (empty string) হয়।**  

---

## **🔹 6️⃣ Boolean Casting (Number → Boolean)**  
👉 `(bool)` বা `(boolean)` ব্যবহার করলে **সংখ্যা ও স্ট্রিং-কে boolean-এ কনভার্ট** করা যায়।  
```php
$value1 = (bool) 123; 
$value2 = (bool) 0; 
$value3 = (bool) "Hello"; 
$value4 = (bool) ""; 

echo $value1; // true
echo $value2; // false
echo $value3; // true
echo $value4; // false
```
🔹 **শূন্য (0) এবং ফাঁকা স্ট্রিং `false`, বাকি সব `true` হয়।**  

---

## **🔹 7️⃣ Array Casting (String/Object → Array)**  
👉 `(array)` ব্যবহার করলে **স্ট্রিং, অবজেক্ট বা অন্যান্য ডাটা টাইপকে array বানানো যায়।**  
```php
$value1 = (array) "hello"; 
$value2 = (array) 123; 

print_r($value1); // ["hello"]
print_r($value2); // [123]
```
🔹 **একটি স্ট্রিং বা সংখ্যা array-তে কনভার্ট করলে এটি একটি মাত্র এলিমেন্টের অ্যারে হয়।**  

---

## **🔹 8️⃣ Object Casting (Array → Object)**  
👉 `(object)` ব্যবহার করলে **array থেকে object বানানো যায়।**  
```php
$data = ["name" => "John", "age" => 30]; 
$obj = (object) $data;

echo $obj->name; // John
echo $obj->age;  // 30
```
🔹 **Associative array কে object-এ কনভার্ট করলে property হিসেবে এক্সেস করা যায়।**  

---

## **🔹 9️⃣ Null Casting (Variable → NULL)**  
👉 `(unset)` ব্যবহার করলে **কোনো ভেরিয়েবলকে NULL-এ কনভার্ট করা যায় (Deprecated in PHP 8.0)**  
```php
$value = (unset) "Hello";
var_dump($value); // NULL
```
🔹 **এই Casting আর ব্যবহার করা হয় না, পরিবর্তে `null` সেট করুন।**  

---

## **🔹 1️⃣0️⃣ Casting ব্যবহার করে Mathematical Operations**  
PHP Casting অনেক ক্ষেত্রে **গাণিতিক অপারেশনেও সাহায্য করে।**  
```php
$sum = (int) "50" + (int) "20.5";  
echo $sum; // 70 (20.5 থেকে দশমিক অংশ বাদ যাবে)
```
🔹 **String → Integer কনভার্ট হয়ে `50 + 20 = 70` হয়।**  

---

## **🔥 সংক্ষেপে PHP Casting Cheat Sheet**  
✅ **Integer Casting:** `(int) "123"` → `123`  
✅ **Float Casting:** `(float) "45.67"` → `45.67`  
✅ **String Casting:** `(string) 123` → `"123"`  
✅ **Boolean Casting:** `(bool) 0` → `false`, `(bool) 1` → `true`  
✅ **Array Casting:** `(array) "hello"` → `["hello"]`  
✅ **Object Casting:** `(object) ["name"=>"John"]` → `{name: "John"}`  
✅ **Null Casting:** `(unset) "Hello"` → `NULL` (Deprecated in PHP 8.0)  

---

এটি ছিল **PHP Casting-এর সম্পূর্ণ গাইড**! 😊  
আপনার যদি কোনো প্রশ্ন থাকে, জানাতে ভুলবেন না! 🚀