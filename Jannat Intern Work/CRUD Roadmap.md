### **Laravel CRUD Learning Roadmap for Intern** 🚀  

তোমার ইন্টার্ন যাতে **নিজে নিজে খুঁজে শিখতে পারে**, তাই ধাপে ধাপে গাইডলাইন দিলাম। এতে প্রতিটি ধাপে কী কী করতে হবে এবং কী কী শিখতে হবে তা থাকবে।  

---

## **Step 1: What is CRUD? (Basic Concept)**  
✅ CRUD মানে **Create, Read, Update, Delete**। Laravel-এর মাধ্যমে কীভাবে ডাটাবেসে এই অপারেশন করা যায়, তা বুঝতে হবে।  
✅ **গবেষণা করো:** Laravel-এ CRUD কীভাবে কাজ করে? (Google / Laravel Documentation)  

---

## **Step 2: Laravel Project Setup**  
✅ Laravel নতুন করে ইনস্টল করো (যদি করা না থাকে)।  
✅ **গবেষণা করো:** Laravel Project কীভাবে সেটআপ করতে হয়?  

---

## **Step 3: Database & Migration**  
✅ **Migration তৈরি করো:**  
- একটি **posts** টেবিল তৈরি করো যেখানে **id, title, content, created_at, updated_at** থাকবে।  

✅ **গবেষণা করো:** Laravel Migrations কী এবং কীভাবে কাজ করে?  

---

## **Step 4: Model তৈরি করো**  
✅ **Post** নামে একটি Model তৈরি করো।  
✅ **গবেষণা করো:** Laravel Model কী এবং এটি কীভাবে ডাটাবেসের সাথে কাজ করে?  

---

## **Step 5: Route & Controller তৈরি করো**  
✅ **PostController** নামে একটি Controller তৈরি করো।  
✅ এই Controller-এ CRUD অপারেশনের জন্য নিচের মেথডগুলো যোগ করো:  
- `index()` → সব পোস্ট দেখাবে  
- `create()` → নতুন পোস্ট তৈরি করার ফর্ম দেখাবে  
- `store()` → ফর্ম থেকে ডাটা নিয়ে ডাটাবেসে সংরক্ষণ করবে  
- `show($id)` → নির্দিষ্ট পোস্ট দেখাবে  
- `edit($id)` → নির্দিষ্ট পোস্ট এডিট করার ফর্ম দেখাবে  
- `update($id)` → এডিট করা ডাটা আপডেট করবে  
- `destroy($id)` → পোস্ট ডিলিট করবে  

✅ **গবেষণা করো:** Laravel Controller কীভাবে কাজ করে?  

---

## **Step 6: View (Blade Templates) তৈরি করো**  
✅ **Blade Templates ব্যবহার করে নিচের ফাইল তৈরি করো:**  
1. `index.blade.php` → সব পোস্ট দেখাবে  
2. `create.blade.php` → নতুন পোস্ট যুক্ত করার ফর্ম  
3. `edit.blade.php` → পোস্ট এডিট করার ফর্ম  
4. `show.blade.php` → নির্দিষ্ট পোস্ট দেখাবে  

✅ **গবেষণা করো:** Laravel Blade কী এবং কীভাবে ব্যবহার করতে হয়?  

---

## **Step 7: Form Handling & Validation**  
✅ **Laravel Request Validation ব্যবহার করো।**  
✅ ফর্ম সাবমিট করলে, ডাটা ভ্যালিড কিনা চেক করো।  
✅ **গবেষণা করো:** Laravel Form Validation কীভাবে করতে হয়?  

---

## **Step 8: Eloquent ORM & Query Builder**  
✅ **Eloquent ব্যবহার করে CRUD অপারেশন করো।**  
✅ **গবেষণা করো:** Laravel Eloquent কী এবং এটি কীভাবে ব্যবহার করতে হয়?  

---

## **Step 9: Flash Messages & Error Handling**  
✅ **Session Flash Messages দেখাও:**  
- সফল হলে **"Post created successfully!"** দেখাবে।  
- সফলভাবে ডিলিট হলে **"Post deleted successfully!"** দেখাবে।  

✅ **গবেষণা করো:** Laravel Flash Messages কীভাবে দেখানো যায়?  

---

## **Step 10: CRUD Project Deployment (Bonus)**  
✅ সব ঠিকঠাক হলে, এটি **GitHub**-এ আপলোড করো।  
✅ **গবেষণা করো:** Laravel Project কীভাবে GitHub-এ আপলোড করতে হয়?  

---

### **Final Task for Intern 🎯**  
🛠 **একটি Simple Blog App বানাও যেখানে:**  
- পোস্ট CRUD অপারেশন করা যাবে।  
- সব পোস্টের লিস্ট দেখা যাবে।  
- নির্দিষ্ট পোস্ট দেখা যাবে।  
- নতুন পোস্ট যোগ করা যাবে।  
- পোস্ট এডিট ও ডিলিট করা যাবে।  

✅ **উত্তর খুঁজে শিখে শেখা নিশ্চিত করো!** 😊  

---

তোমার ইন্টার্ন যদি এই স্টেপগুলো ঠিকভাবে অনুসরণ করে, তাহলে Laravel CRUD-এ দক্ষ হয়ে যাবে! 🚀   
এই ডকুমেন্ট কি ঠিক আছে, নাকি কিছু যোগ করতে বলবে? 😃