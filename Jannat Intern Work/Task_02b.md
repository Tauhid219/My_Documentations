একদম! আগের মতোই সাজানো, হাতে-কলমে কাজ শেখানোর জন্য ফাটাফাটি একটা টাস্ক দিলাম। এই প্রশ্নপত্র এমনভাবে সাজানো হয়েছে, যেন ইন্টার্ন হাতে কলমে Laravel অ্যাপ বানিয়ে, ব্যাসিক থেকে রিলেশনশিপ, মাইগ্রেশন আর পারফরম্যান্স অপ্টিমাইজেশন সব শিখে ফেলে। 

---

### 📄 **Laravel Task Assignment (Intern Level)**  
**Duration:** 5 days  
**Objective:** Apply core Laravel concepts in a real-world scenario.

---

## 📝 **Project Brief**  
**Project Name:** **Library Management System**  
তোমার কাজ হচ্ছে একটা ছোট্ট লাইব্রেরি ম্যানেজমেন্ট সিস্টেম তৈরি করা। এখানে বই, লেখক, এবং ইউজার থাকবে। ইউজাররা বই ধার নিতে পারবে। 

---

## 🎯 **Task Breakdown**

### **১. Database Basics & Migrations**
- **টাস্ক ১.১:** নতুন Laravel প্রজেক্ট তৈরি করো।  
- **টাস্ক ১.২:** নিচের টেবিল গুলোর জন্য মাইগ্রেশন তৈরি করো।  
  - `authors` টেবিল: `id`, `name`, `bio`, `created_at`, `updated_at`  
  - `books` টেবিল: `id`, `title`, `description`, `author_id (FK)`, `published_at`, `created_at`, `updated_at`  
  - `users` (Laravel default ইউজার টেবিল থাকবে)।  
  - `book_user` (pivot table): `id`, `book_id (FK)`, `user_id (FK)`, `borrowed_at`, `returned_at`  

### **২. Basic Eloquent Model & MVC (Controller -> Model -> View)**
- **টাস্ক ২.১:** Eloquent Model তৈরি করো: `Author`, `Book`, `User`  
- **টাস্ক ২.২:** Controller গুলো তৈরি করো:  
  - `AuthorController` -> CRUD  
  - `BookController` -> CRUD  
  - `BorrowController` -> ইউজার কীভাবে বই ধার নেবে, সেটি হ্যান্ডেল করবে।  
- **টাস্ক ২.৩:** View গুলো তৈরি করো (Blade template)  
  - লেখকদের তালিকা দেখাবে।  
  - বইয়ের তালিকা দেখাবে।  
  - ইউজার কোন বই ধার নিয়েছে, তার তালিকা দেখাবে।  

### **৩. Eloquent Relationships (belongsTo / hasMany / belongsToMany)**
- **টাস্ক ৩.১:**  
  - `Author` hasMany `Book`  
  - `Book` belongsTo `Author`  
  - `User` belongsToMany `Book` (through `book_user` table)  
  - `Book` belongsToMany `User`  
- **টাস্ক ৩.২:**  
  - কোন লেখকের কতগুলো বই আছে, তা দেখাবে।  
  - কোন ইউজার কতো বই ধার নিয়েছে, তা দেখাবে।  

### **৪. Eager Loading এবং N+1 Query Problem**
- **টাস্ক ৪.১:**  
  - লেখকদের তালিকা দেখানোর সময় তাদের প্রত্যেকের সব বই দেখাবে। এখানে **Eager Loading** ইউজ করো (`with()`)।  
  - ইউজারদের তালিকা দেখানোর সময় তাদের ধার করা বইগুলোর নাম দেখাবে।  
- **টাস্ক ৪.২:**  
  - N+1 প্রোব্লেম ডেমো দেখাও:  
    - `Author::all()` নিয়ে প্রত্যেকের বই আলাদাভাবে লোড করলে কতগুলো query হচ্ছে।  
  - তারপর Eager Loading দিয়ে query count কমাও।  
  - এই পার্থক্যটা README ফাইলে লিখে দাও।  

---

## ✅ **Bonus (Optional কিন্তু দারুণ!)**
- Soft Deletes ইউজ করে দেখাও `Book` মডেলে।  
- Query Scopes ইউজ করে দেখাও, যেমন: `availableBooks()`  
- Form Request Validation ইউজ করো, যখন বই ধার নিচ্ছে ইউজার।  

---

## 📂 **Submission Requirement**
1. GitHub repo (with clear commits & README file):  
   - README এ বুঝিয়ে লেখো তুমি কী কী করলে।  
2. Database ER Diagram (Optional but preferred)।  
3. Project walkthrough short video (Optional, but a plus)।  

---

## 🕐 **Deadline**  
আগামী **৫ দিনের** মধ্যে, তবে সময় লাগলে আগেভাগে জানাবে। ডেডলাইন মিস করলে কফি মাইনাস। ☕ ❌ (কাজ শেষ করলে আবার কফি ফ্রি, হাহা!)  

---

## 💡 **Tips**
- Artisan command ইউজ করো: `php artisan make:model -mcr`  
- Seeder ও Factory দিয়ে dummy ডেটা ইনসার্ট করতে পারো।  
- SQL Query log দেখে Query Optimization শেখার চেষ্টা করো।  

---

### 🚀 **Ready? Go code like a champ!**

---

কেমন লাগলো? আর কোনো অংশ বাড়াতে বা কমাতে বলো! 😎