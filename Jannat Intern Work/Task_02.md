

# 📄 **Laravel Task Assignment**  
  
**Objective:** Basic Laravel CRUD, Migrations, Eloquent Relationships, এবং Eager Loading শেখা।  

---

## 📝 **Project Brief**  
**Project Name:** **Student Course Management**  
তোমার কাজ হচ্ছে একটা সিম্পল অ্যাপ বানানো, যেখানে স্টুডেন্টরা কোর্সে এনরোল করতে পারবে।  

---

## 🎯 **Task Breakdown**

### **১. Database Basics & Migrations**
- **টাস্ক ১.১:** নতুন Laravel প্রজেক্ট তৈরি করো।  
- **টাস্ক ১.২:** নিচের টেবিল গুলোর জন্য মাইগ্রেশন তৈরি করো।  
  - `students` টেবিল: `id`, `name`, `email`, `created_at`, `updated_at`  
  - `courses` টেবিল: `id`, `title`, `description`, `created_at`, `updated_at`  
  - `course_student` (pivot table): `id`, `student_id (FK)`, `course_id (FK)`, `enrolled_at`  

---

### **২. Basic Eloquent Model & MVC (Controller -> Model -> View)**  
- **টাস্ক ২.১:** Eloquent Model তৈরি করো: `Student`, `Course`  
- **টাস্ক ২.২:** Controller গুলো তৈরি করো:  
  - `StudentController` -> CRUD  
  - `CourseController` -> CRUD  
  - `EnrollmentController` -> স্টুডেন্টদের কোর্সে এনরোল করানোর জন্য।  
- **টাস্ক ২.৩:** View গুলো তৈরি করো (Blade template):  
  - সব স্টুডেন্টের তালিকা দেখাবে।  
  - সব কোর্সের তালিকা দেখাবে।  
  - কে কোন কোর্সে এনরোল করেছে, সেটার তালিকা দেখাবে।  

---

### **৩. Eloquent Relationships (belongsToMany)**
- **টাস্ক ৩.১:**  
  - `Student` belongsToMany `Course`  
  - `Course` belongsToMany `Student`  
- **টাস্ক ৩.২:**  
  - এক স্টুডেন্ট কতগুলো কোর্সে এনরোল করেছে, সেটা দেখাও।  
  - এক কোর্সে কয়জন স্টুডেন্ট এনরোল করেছে, সেটাও দেখাও।  

---

### **৪. Eager Loading এবং N+1 Query Problem (Easy Mode)**
- **টাস্ক ৪.১:**  
  - স্টুডেন্টের তালিকা দেখানোর সময় তাদের এনরোল করা কোর্স গুলো **Eager Loading** দিয়ে দেখাও (`with()`)।  
- **টাস্ক ৪.২:**  
  - Eager Loading এর আগে-পরে কতগুলো query হচ্ছে, সেটা `dd(DB::getQueryLog())` দিয়ে দেখে নিও।  

---

## ✅ **Bonus (Optional but Fun!)**
- Dummy ডেটা ফ্যাক্টরি আর সিডার দিয়ে ইনসার্ট করো।  
- Blade Template এ Bootstrap/Tailwind যোগ করে লুক ভালো করো।  
- Validation যুক্ত করো (যাতে একই স্টুডেন্ট একই কোর্সে দুইবার এনরোল করতে না পারে)।  