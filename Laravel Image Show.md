Laravel অ্যাপে **public** ফোল্ডারে ছবি সংরক্ষণ করে সেগুলোকে দেখানোর জন্য কয়েকটি ধাপ অনুসরণ করতে হবে। নিচে বিস্তারিতভাবে আলোচনা করা হলো—  

---

## ✅ **ধাপ ১: Public ফোল্ডারে ছবি আপলোড করুন**  
Laravel-এর **public** ফোল্ডারে সাধারণত স্ট্যাটিক ফাইল (CSS, JS, images ইত্যাদি) রাখা হয়।  

আপনি `public/images/` নামে একটি ফোল্ডার তৈরি করে সেখানে ছবি সংরক্ষণ করতে পারেন।  

📁 **ফোল্ডার স্ট্রাকচার:**  
```
laravel_project/
│── public/
│   ├── images/
│   │   ├── myphoto.jpg
│── resources/
│── routes/
│── app/
```

---

## ✅ **ধাপ ২: Blade টেমপ্লেটে ছবি দেখানো**  
আপনি Blade ফাইলে **asset() ফাংশন** ব্যবহার করে ছবির URL জেনারেট করতে পারেন।  

```blade
<img src="{{ asset('images/myphoto.jpg') }}" alt="My Photo">
```

এটি ব্রাউজারে **http://localhost:8000/images/myphoto.jpg** হিসেবে রেন্ডার হবে।

---

## ✅ **ধাপ ৩: CSS ফাইল থেকে ছবি লোড করা**  
যদি CSS ফাইলের মাধ্যমে ছবি লোড করতে চান, তাহলে নিচের মতো ব্যবহার করতে হবে—  

📌 **CSS ফাইল (`public/css/style.css`)**
```css
body {
    background-image: url('../images/background.jpg');
    background-size: cover;
}
```

📌 **Blade ফাইল (`resources/views/welcome.blade.php`)**
```blade
<link rel="stylesheet" href="{{ asset('css/style.css') }}">
```

---

## ✅ **ধাপ ৪: ডাটাবেস থেকে ছবি সংরক্ষণ ও দেখানো**  
অনেক সময় ইউজারদের আপলোড করা ছবি **storage/app/public/** ফোল্ডারে রাখা হয়, এবং সেগুলো **public/storage/** এর মাধ্যমে এক্সেস করা হয়।

### 📌 **Step 1: Laravel Storage Link তৈরি করুন**  
আপনার ছবি যদি **storage/app/public/** ফোল্ডারে থাকে, তাহলে **storage link** তৈরি করতে হবে—  

```sh
php artisan storage:link
```
এটি **public/storage/** কে **storage/app/public/** এর সাথে লিঙ্ক করবে।

---

### 📌 **Step 2: ফর্ম দিয়ে ছবি আপলোড করুন**  
আপনি একটি ফর্ম তৈরি করতে পারেন যেখানে ইউজার ছবি আপলোড করতে পারবে।  

📌 **Blade ফাইল (`resources/views/upload.blade.php`)**
```blade
<form action="{{ route('upload.image') }}" method="POST" enctype="multipart/form-data">
    @csrf
    <input type="file" name="photo">
    <button type="submit">Upload</button>
</form>
```

---

### 📌 **Step 3: Laravel Controller দিয়ে ছবি সংরক্ষণ করুন**  
📌 **Controller (`app/Http/Controllers/ImageController.php`)**
```php
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Storage;

public function uploadImage(Request $request)
{
    $request->validate([
        'photo' => 'required|image|mimes:jpeg,png,jpg,gif|max:2048',
    ]);

    $path = $request->file('photo')->store('public/images');

    return back()->with('success', 'Image uploaded successfully!')->with('image', Storage::url($path));
}
```

---

### 📌 **Step 4: Route তৈরি করুন**  
📌 **Routes (`routes/web.php`)**
```php
use App\Http\Controllers\ImageController;

Route::post('/upload-image', [ImageController::class, 'uploadImage'])->name('upload.image');
```

---

### 📌 **Step 5: ছবি দেখানো**  
যদি আপনি ডাটাবেসে ছবির URL সংরক্ষণ করেন, তাহলে এটি Blade ফাইলে দেখাতে পারেন—  

```blade
<img src="{{ asset('storage/images/myphoto.jpg') }}" alt="Uploaded Image">
```

---

## 🎯 **সংক্ষেপে:**
1. **Static Image**: `public/images/` ফোল্ডারে রেখে `{{ asset('images/myphoto.jpg') }}` দিয়ে দেখানো যায়।  
2. **User Uploaded Image**: `storage/app/public/`-এ রাখলে `php artisan storage:link` চালিয়ে `{{ asset('storage/images/myphoto.jpg') }}` দিয়ে দেখানো যায়।  
3. **CSS থেকে লোড**: `background-image: url('../images/background.jpg');` ব্যবহার করা যায়।  

এভাবে Laravel-এ public ফোল্ডার থেকে সহজেই ছবি লোড করতে পারবেন! 🚀😊